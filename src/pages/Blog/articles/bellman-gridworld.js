const article = {
  slug: 'bellman-gridworld',
  date: '2026-08-12 19:00',
  name: 'Bellman Equation in a 2×2 Grid World',
  description: '最近在学习西湖大学赵世钰老师的强化学习的数学原理,前三节课完整介绍了强化学习的概念以及bellman equation,bellman optimality equation。这里用 2×2 grid world总结一下前三节的内容:把策略迭代和价值迭代两条路完整走一遍.',
  tags: ['RL'],
  category: 'Course Review',
  author: 'shannon',
  takeaway: '两条路殊途同归:策略迭代"评价 + 改进"交替(期望方程做平均、最大选动作),价值迭代只用最优方程直接取最大。都算出最优价值 V* 后,用 Q 的 argmax 读出最优策略。决策靠 Q,评价靠 V。',
  detail: String.raw`

## 1. 一个具体的 2×2 World

![2×2 grid world 地图](/images/gridworld-map.png)

规则:

- 状态 $s = \text{row}\cdot 2 + \text{col}$,共 4 个: (0,0)、(0,1)、(1,0)、(1,1)
- **目标** (1,1):每一步结束时**停在它上面 +1**
- **禁区** (0,1):每一步结束时**停在它上面 -1**
- **撞墙**(想走出界):**-1**
- 正常移动到其它格子:**0**
- **没有终止状态**:游戏不结束,你一直走下去
- 动作 5 个: up / down / left / right / stay;撞墙(出界)→ 原地不动,但算撞墙、-1
- 折扣因子 $\gamma = 0.9$

> **奖励约定**:先看这一步是不是撞墙——是就 **-1**;不是,就看这一步结束时你所在的格子:目标 **+1**、禁区 **-1**、其它 **0**。奖励定义在转移上,stay 也是一次转移(到同一个格子),所以停在目标格每步都 +1。这个约定不唯一,但必须自洽,后面的每个数字都建立在它上面。

## 2. 三个先决概念:Return / Policy / Value

- **Return(回报)**: $G_t = r_t + \gamma\, r_{t+1} + \gamma^2 r_{t+2} + \cdots$。折扣让"更远的奖励"在当下更不值钱。
- **Policy(策略)**: $\pi(a|s)$,状态 $s$ 下选动作 $a$ 的概率。价值永远依附于某个策略而存在。
- **State value**: $V_\pi(s) = \mathrm{E}\big[ G_t \mid s_t = s \big]$,从 $s$ 出发按 $\pi$ 行动的期望回报。
- **Action value**: $Q_\pi(s,a) = \mathrm{E}\big[ G_t \mid s_t = s, a_t = a \big]$,先强制做动作 $a$、之后按 $\pi$ 行动的期望回报。

**全篇最核心的一个式子**(一步备份):做完动作 $a$ 后,后面按当前认识继续,那么

$$
Q_\pi(s,a) = R(s,a) + \gamma\, V_\pi(s')
$$

即:动作的价值 = 这一步的立即奖励 + 折扣后的下一格价值。后面两条算法都建立在它上面。

## 3. 核心关系:决策靠 Q,评价靠 V

四行公式把 V 和 Q 的关系钉死:

$$
Q_\pi(s,a) = R(s,a) + \gamma\sum_{s'} P(s'|s,a)\, V_\pi(s')
$$

—— 一个动作的价值

$$
V_\pi(s) = \sum_a \pi(a|s)\, Q_\pi(s,a)
$$

—— 状态价值 = 动作价值按策略加权平均

$$
V_*(s) = \max_a Q_*(s,a)
$$

—— 最优状态价值 = 最好的动作价值

$$
\pi_*(s) = \arg\max_a Q_*(s,a)
$$

—— 最优策略 = 每步选 Q 最大的动作

**V 只告诉你"状态多好",不告诉你怎么走。** 要走对路必须比较每个动作——那就要 Q。所以:评价一个策略看 V,做决策看 Q,最优策略永远是 $\arg\max_a Q$。

接下来两条算法,都会先算出 V,再用 Q 的 $\arg\max$ 读出策略。

## 4. 路径一:策略迭代 Policy Iteration

策略迭代是一个"评价 → 改进 → 再评价 → 再改进……"的循环,直到策略不再变。

### 4.1 起点:均匀随机策略

初始策略**没有任何对错,只是迭代的起点**:

$$
\pi_0(a|s) = \dfrac{1}{5},\qquad \forall\, s, \forall\, a
$$

### 4.2 评价:用期望方程算当前策略的价值

**Bellman 期望方程**——一个状态的价值 = 各动作"立即奖励 + 折扣后的后继价值"按策略概率**加权平均**:

$$
V_\pi(s) = \sum_a \pi(a|s)\left[\, R(s,a) + \gamma\sum_{s'} P(s'|s,a)\, V_\pi(s') \,\right]
$$

确定性环境 $P(s'|s,a)=1$,且 $\pi(a|s)=1/5$,把 **4 个状态的全部方程写出来**(没有终止状态,4 个都要写):

$$
\begin{aligned}
V(0) &= \dfrac{1}{5}\Big[\,(-1+0.9\,V(0)) + (0+0.9\,V(2)) \\
&\quad + (-1+0.9\,V(0)) + (-1+0.9\,V(1)) + (0+0.9\,V(0)) \,\Big] \\
V(1) &= \dfrac{1}{5}\Big[\,(-1+0.9\,V(1)) + (+1+0.9\,V(3)) \\
&\quad + (0+0.9\,V(0)) + (-1+0.9\,V(1)) + (-1+0.9\,V(1)) \,\Big] \\
V(2) &= \dfrac{1}{5}\Big[\,(0+0.9\,V(0)) + (-1+0.9\,V(2)) \\
&\quad + (-1+0.9\,V(2)) + (+1+0.9\,V(3)) + (0+0.9\,V(2)) \,\Big] \\
V(3) &= \dfrac{1}{5}\Big[\,(-1+0.9\,V(1)) + (-1+0.9\,V(3)) \\
&\quad + (0+0.9\,V(2)) + (-1+0.9\,V(3)) + (+1+0.9\,V(3)) \,\Big]
\end{aligned}
$$

$V(0)$ 那条的 5 项,逐项对照动作(状态编号 $0{=}(0,0)$, $1{=}(0,1)$ 禁区, $2{=}(1,0)$, $3{=}(1,1)$ 目标):

| 动作 | 奖励 | 下一格 s′ | 这一项 |
|---|---|---|---|
| up(撞墙) | -1 | (0,0) | $-1 + 0.9\,V(0)$ |
| down | 0 | (1,0) | $0 + 0.9\,V(2)$ |
| left(撞墙) | -1 | (0,0) | $-1 + 0.9\,V(0)$ |
| right(进禁区) | -1 | (0,1) | $-1 + 0.9\,V(1)$ |
| stay | 0 | (0,0) | $0 + 0.9\,V(0)$ |

$V(2)$ 的关键项是 right(进目标 +1),$V(1)$ 的关键项是 down(从禁区逃进目标 +1),$V(3)$ 的关键项是 stay(留在目标 +1)。**每组都是联立方程、不是一条公式**:每条 $V(s)$ 都在等号两边(自环),单独算不出来。收进矩阵就是线性系统:

$$
\mathbf{V} = \mathbf{R} + \gamma\,\mathbf{P}\,\mathbf{V}
$$

$$
\mathbf{P} = \begin{bmatrix} 0.6 & 0.2 & 0.2 & 0 \\ 0.2 & 0.6 & 0 & 0.2 \\ 0.2 & 0 & 0.6 & 0.2 \\ 0 & 0.2 & 0.2 & 0.6 \end{bmatrix}, \qquad
\mathbf{R} = \begin{bmatrix} -0.6 \\ -0.4 \\ -0.2 \\ -0.4 \end{bmatrix}
$$

$P$ 第 0 行(从 (0,0) 出发): up/left/stay 三个动作回 (0,0),概率 $3/5=0.6$;down 到 (1,0) 为 0.2;right 进禁区 (0,1) 为 0.2。第 1、2、3 行同理,没有全零的行(因为没有终止)。$R$ 是每个状态按 1/5 平均的立即奖励,比如 $R(0)=(-1+0-1-1+0)/5=-0.6$。

这个 4×4 方程组可以直接解,维度增大之后需要使用迭代方法。收敛后得到 $\pi_0$(均匀随机)的价值 $V^{\pi_0}$:

![均匀随机策略下的 V_π 热力图](/images/vpi-heatmap.png)

四个全是负的:$V(0)\approx-4.34$、$V(1)\approx-4.10$、$V(2)\approx-3.66$、$V(3)\approx-3.90$。随机乱走又撞墙又踩禁区,越走越亏。

### 4.3 改进:比较动作,挑最好的

有了 $V^{\pi_0}$,用核心式子 $Q(s,a)=R(s,a)+0.9\,V(s')$ 算**每个动作**的价值,取 $\arg\max$。$V^{\pi_0}=[-4.34,\,-4.10,\,-3.66,\,-3.90]$,以 (0,0) 为例(5 个动作一个不省):

| 动作 | $Q^{\pi_0}(0,a) = R + 0.9\,V^{\pi_0}(s')$ | 值 |
|---|---|---|
| up(撞墙) | $-1 + 0.9 \times (-4.34) = -1 - 3.91$ | -4.91 |
| down | $0 + 0.9 \times (-3.66) = -3.29$ | **-3.29** |
| left(撞墙) | $-1 + 0.9 \times (-4.34)$ | -4.91 |
| right(进禁区) | $-1 + 0.9 \times (-4.10) = -1 - 3.69$ | -4.69 |
| stay | $0 + 0.9 \times (-4.34)$ | -3.91 |

最大的是 **down**(-3.29)→ 新策略 $\pi_1(0,0)=\text{down}$。

(1,0) 同样全算:

| 动作 | $Q^{\pi_0}(2,a) = R + 0.9\,V^{\pi_0}(s')$ | 值 |
|---|---|---|
| up | $0 + 0.9 \times (-4.34)$ | -3.91 |
| down(撞墙) | $-1 + 0.9 \times (-3.66) = -1 - 3.29$ | -4.29 |
| left(撞墙) | $-1 + 0.9 \times (-3.66)$ | -4.29 |
| right(进目标) | $+1 + 0.9 \times (-3.90) = 1 - 3.51$ | **-2.51** |
| stay | $0 + 0.9 \times (-3.66)$ | -3.29 |

最大的是 **right**(-2.51)→ $\pi_1(1,0)=\text{right}$。同样算 (0,1)(→down)和 (1,1)(→stay)。于是改进后的策略:

$$
\pi_1 = \{\,(0,0)\to\text{down},\ (0,1)\to\text{down},\ (1,0)\to\text{right},\ (1,1)\to\text{stay} \,\}
$$

注意:改进只看"哪个动作最好",不看概率——**策略从"五个动作各 1/5"变成"确定走一步"**。

### 4.4 循环:再评价、再改进,直到策略不变

用新的确定性策略 $\pi_1$ 重新评价。此时每个状态只有一个动作,方程更简单:

$$
\begin{aligned}
V(0) &= 0 + 0.9\,V(2) &\qquad \text{(only down)}\\
V(1) &= +1 + 0.9\,V(3) &\qquad \text{(only down)}\\
V(2) &= +1 + 0.9\,V(3) &\qquad \text{(only right)}\\
V(3) &= +1 + 0.9\,V(3) &\qquad \text{(only stay)}
\end{aligned}
$$

从 $V(3)$ 解起:$V(3)=1+0.9\,V(3)$ → $V(3)=10$;代回去 $V(2)=10$、$V(1)=10$、$V(0)=9$。所以

$$
V^{\pi_1} = [\,9,\ 10,\ 10,\ 10\,]
$$

再改进:用 $V^{\pi_1}$ 算 $Q^{\pi_1}$,每个状态的 $\arg\max$ 还是 down / down / right / stay——**策略没变 → 收敛**。策略迭代结束。

## 5. 路径二:价值迭代 Value Iteration

策略迭代要先"评价一个策略"再"改进";价值迭代**跳过策略**,直接从最优方程迭代价值。

### 5.1 最优方程:把平均换成最大

把期望方程的"按 $\pi$ 加权平均"换成"取最大":

$$
V_*(s) = \max_a\left[\, R(s,a) + \gamma\sum_{s'} P(s'|s,a)\, V_*(s') \,\right]
$$

带 $\max$ 变成非线性,没有解析解,只能迭代:

$$
V_{k+1}(s) = \max_a\left[\, R(s,a) + \gamma\, V_k(s') \,\right],\qquad V_0 = \mathbf{0}
$$

### 5.2 迭代到收敛

**第 1 轮**( $V=\mathbf{0}$,只剩立即奖励):

$$
V(0)=\max(-1,\,0,\,-1,\,-1,\,0)=0,\qquad
V(1)=\max(-1,\,+1,\,0,\,-1,\,-1)=+1
$$

$$
V(2)=\max(0,\,-1,\,-1,\,+1,\,0)=+1,\qquad
V(3)=\max(-1,\,-1,\,0,\,-1,\,+1)=+1
$$

只有能进目标 / 已在目标的三个格子 (0,1)、(1,0)、(1,1) 拿到 +1。

**第 2 轮**:把第 1 轮算出的 $V_1$ 代进去($V_1=[0,1,1,1]$,即 $V_1(0)=0$、$V_1(1)=1$、$V_1(2)=1$、$V_1(3)=1$)。规则:每个动作的价值 = 奖励 + 0.9 × $V_1$(下一格),取最大。

**(0,0)** 的五个动作逐个算:

| 动作 | 下一格 s′ | $R + 0.9\,V_1(s')$ | 值 |
|---|---|---|---|
| up(撞墙) | (0,0) | $-1 + 0.9 \times V_1(0) = -1 + 0.9\times 0$ | -1 |
| down | (1,0) | $0 + 0.9 \times V_1(2) = 0 + 0.9\times 1$ | **0.9** |
| left(撞墙) | (0,0) | $-1 + 0.9 \times V_1(0)$ | -1 |
| right(禁区) | (0,1) | $-1 + 0.9 \times V_1(1) = -1 + 0.9\times 1$ | -0.1 |
| stay | (0,0) | $0 + 0.9 \times V_1(0)$ | 0 |

$\max=0.9$(down)→ $V_2(0,0)=0.9$。

**(1,1)** 目标格:

| 动作 | 下一格 s′ | $R + 0.9\,V_1(s')$ | 值 |
|---|---|---|---|
| up(禁区) | (0,1) | $-1 + 0.9 \times V_1(1) = -1 + 0.9\times 1$ | -0.1 |
| down(撞墙) | (1,1) | $-1 + 0.9 \times V_1(3) = -1 + 0.9\times 1$ | -0.1 |
| left | (1,0) | $0 + 0.9 \times V_1(2) = 0 + 0.9\times 1$ | 0.9 |
| right(撞墙) | (1,1) | $-1 + 0.9 \times V_1(3)$ | -0.1 |
| stay(目标) | (1,1) | $+1 + 0.9 \times V_1(3) = 1 + 0.9\times 1$ | **1.9** |

$\max=1.9$(stay)→ $V_2(1,1)=1.9$。

(0,1) 的 down、(1,0) 的 right 同样算得 1.9。于是 $V_2=[0.9,\,1.9,\,1.9,\,1.9]$。价值开始"从目标往外传"。

**一直迭代到数值不再变 → 收敛**:

![V* 与最优策略](/images/vstar-policy.png)

$$
V_*(0)=9,\qquad V_*(1)=10,\qquad V_*(2)=10,\qquad V_*(3)=10
$$

为什么目标格是 10?因为它可以每步停着拿 +1,折现求和是几何级数:

$$
V_*(3) = 1 + 0.9\times 1 + 0.9^2\times 1 + \cdots = \dfrac{1}{1-0.9} = 10
$$

而 $V_*(2)=10$(进目标再停),$V_*(0)=9$(走到 (1,0) 再进目标)。**没有终止、没有每步成本,最优就是走到目标然后永远待着**——这也是为什么真实 RL 任务通常要定义终止或每步成本。

### 5.3 提取:用 Q 读出策略

价值迭代只给了 $V_*$,策略要用 Q 的 $\arg\max$ 读出来。用 $V_*=[9,10,10,10]$ 算 $Q_*$,以 (0,0) 为例:

| 动作 | $Q_*(0,a) = R + \gamma\, V_*(s')$ | 值 |
|---|---|---|
| up(撞墙) | $-1 + 0.9 \times V_*(0) = -1 + 0.9\times 9$ | 7.1 |
| down | $0 + 0.9 \times V_*(2) = 0 + 0.9\times 10$ | **9** |
| left(撞墙) | $-1 + 0.9 \times V_*(0)$ | 7.1 |
| right(进禁区) | $-1 + 0.9 \times V_*(1) = -1 + 0.9\times 10$ | 8 |
| stay | $0 + 0.9 \times V_*(0)$ | 8.1 |

$\max=9$ → $\pi_*(0,0)=\text{down}$。(1,0) 同理,right 得 10 → $\pi_*(1,0)=\text{right}$;(0,1) 得 down、(1,1) 得 stay:

$$
\pi_* = \{\,(0,0)\to\text{down},\ (0,1)\to\text{down},\ (1,0)\to\text{right},\ (1,1)\to\text{stay} \,\}
$$

![Q* 与 argmax](/images/qstar-argmax.png)

从 (0,0) 出发: **down → (1,0),right → (1,1) 目标,然后 stay 待着,每步 +1**。

## 6. 两条路的关系与小结

- **策略迭代** = 评价(期望方程,平均)+ 改进(最大)交替,循环到策略不变;
- **价值迭代** = 只用最优方程(最大),直接迭代到 $V_*$,再贪心读出策略;
- 两者得到**同一套** $V_*=[9,10,10,10]$ 和 $\pi_*=\{\text{down},\text{down},\text{right},\text{stay}\}$。

共同点:都先算 **state value V**,再用 **Q 的 $\arg\max$** 决定每个格子走哪——这就是"**评价靠 V,决策靠 Q**"。

两条值得注意的点:

- **(0,1) 禁区也有价值 10**:因为禁区不是终止,可以从它一步 down 逃进目标。价值顺着"能到达目标的路径"流过去,哪怕起点在禁区。
- **最优策略是"到达并停留"**:没有终止、没有每步成本,停着也能白拿 +1,所以不会离开目标。
- **初始策略**:任意,通常均匀随机;价值迭代甚至不需要策略。`,
}

export default article
