const article = {
  slug: 'ondevice-fc',
  date: '2026-08-18',
  name: '基于可靠奖励的 SFT + GRPO 强化学习,让小模型在手机上学会调函数',
  description: '手机上的 agent 需要调用函数:用户说"定个闹钟",小模型要输出对应的 JSON 调用。我用 DroidCall 数据,在一张 RTX 5090 上把 Qwen2.5-3B 的函数调用准确率从 21% 训练到 51%。SFT 提高 4 个百分点,后续提升主要来自 GRPO 和奖励函数。',
  tags: ['RL'],
  category: 'Project',
  author: 'shannon',
  detail: `手机端 agent 的一个基础要求是让小模型正确调用函数。用户说"帮我定明天早上 7 点半的闹钟",模型需要输出 \`{"ACTION_SET_ALARM": {"EXTRA_HOUR": 7, "EXTRA_MINUTES": 30}}\`,系统再执行这段 JSON。模型规模需要控制在手机内存可承受的范围内,同时还要支持隐私保护、流量受限和离线场景。本文测试 3B 模型能否把自然语言请求转换成准确的函数调用。

**数据长什么样。** 任务和数据用的是 **DroidCall**,一个把安卓 intent 调用抽象成函数调用的基准。它预定义了 24 个函数(定闹钟、发邮件、搜网页、挑联系人……),每个函数是这样一份定义——名字、一段描述、参数表(每个参数标了类型、是否必填、默认值)。比如答案里会反复出现的定闹钟,摘自真实的 api.jsonl:

\`\`\`
ACTION_SET_ALARM
  EXTRA_HOUR:    int,  必填  闹钟小时,24 小时制
  EXTRA_MINUTES: int,  必填  分钟
  EXTRA_MESSAGE: str,  选填  备注,默认空
  EXTRA_DAYS:    list, 选填  响铃日期,默认 None
  EXTRA_VIBRATE: bool, 选填  是否震动,默认 false
\`\`\`

约 1 万条样本,每条是一个三元组:(用户请求, 相关函数定义, 标准调用)。一条简单的长这样(论文原例):

\`\`\`
query:   "Wake me up at 8:30"
answers: [{"id": 0, "name": "ACTION_SET_ALARM",
           "arguments": {"EXTRA_HOUR": 8, "EXTRA_MINUTE": 30}}]
\`\`\`

模型要做的:看到 query,从函数列表里选出 ACTION_SET_ALARM,再把"8:30"拆成小时=8、分钟=30 填进参数。难的不是格式,是这些细节——8:30 怎么拆、可选参数要不要补、几个候选函数里选哪个。数据分两档生成:simple 是单函数单调用(上面这种);complex 要多函数组合,甚至一个调用的参数引用前一个调用的返回值(比如先"挑联系人",再把拿到的 URI 填进"发邮件")。数据是拿 GPT-4-turbo 生成的,按相似度去重,评测留出 200 条,不参与训练。

**方法:先做 SFT,再做 GRPO。** 模型是 Qwen2.5-3B-Instruct,训练只修改一小部分参数(LoRA 低秩微调),整个 pipeline 使用一张 RTX 5090(32GB)。第一阶段用 SFT 学习输出格式,第二阶段用 GRPO 优化调用正确性。

**SFT:学习格式,但不能直接优化执行结果。** SFT 使用一批"指令-回复"对微调预训练模型,使其按照示例格式回答。完整流程如下图所示(来源:CSDN《一文读懂大模型SFT微调技术:原理、方法与实践》):

![SFT 训练流程:预训练模型 + 指令-回复数据 → 有监督微调(来源:CSDN《一文读懂大模型SFT微调技术:原理、方法与实践》)](/images/sft-flow.jpg)

第一步,**把数据变成对话格式**。DroidCall 原始样本是 (query, tools, answers) 三元组,\`build_fc_sft.py\` 把它拼成三段对话:system 包含 24 个函数的 JSON 定义,user 是用户请求(比如 "Wake me up at 8:30"),assistant 是标准调用 JSON。SFT 沿用模型预训练时的"系统+用户+助手"结构,只把函数调用任务填入对应位置。

第二步,**只改一小部分参数(LoRA)**。Qwen2.5-3B 三十亿参数,全量微调显存和时长都不划算。LoRA 的做法:主模型全部冻结,只在每层注意力/前馈的投影矩阵旁边挂一个低秩旁路(r=16),训练只更新这个旁路。旁路参数少得可怜,所以单卡 5090 就够。下面这张图是 LoRA 原始论文的 Figure 1,它想说的就是"重参数化":**训练时预训练权重 W 冻着不动,只学两个低秩小矩阵 A、B;用的时候把 W + BA 合回一个大矩阵,推理速度完全不受影响**:

第三步,**拿交叉熵把"下一 token"往标准答案上推**。给定 system+user,模型逐个预测 assistant 的 token:第一个该是 \`[\`,然后 \`{\`、\`"name"\`……每一步输出一个概率分布,和标准答案的下一个 token 比:对,损失低;错,损失高。梯度顺着把概率调对。3 个 epoch,学习率 1.41e-5。

这一套下来,模型学会了"看到请求就输出合法 JSON 调用"这个格式。**但 SFT 优化的是"文本像不像标准答案",不是"这次调用能不能成"。** 25% 就是这么来的:格式全对,但函数选错、参数填错的情况不少。这就轮到 GRPO 了。

SFT 放在 GRPO 之前,是因为 RL 需要对模型生成的结果打分。如果模型还不能稳定输出合法调用,奖励函数就难以提供有效信号。SFT 先建立基本格式,GRPO 再根据调用正确性进行优化。

**但 GRPO 只是个放大器,放大什么取决于奖励函数给什么信号。** 这句话是全项目最重要的一句。奖励给不出信号,GRPO 白推;给错信号,GRPO 把错的放大。所以力气主要花在奖励函数上,不是训练参数上。这也正好是 Code-R1 那篇文章的结论:reward quality matters more than reward quantity——可靠的奖励,比奖励的数量重要。

**奖励函数长什么样。** 三个设计,单个看都不起眼,合起来决定成败。

第一,**兼容多种输出格式**。模型可能把调用放在 \`<think>\` 中、夹在说明文字中、使用 \`action\` 字段名,或输出 \`FUNC(ARG=v)\` 形式。解析器如果只接受一种格式,就会把一部分语义正确的输出判为 0 分。因此解析器先从这些形式中提取函数名和参数。

第二,**语义打分,不是字符串比对**。解析出来之后按规则打分:函数名对 +0.4,参数对 +0.6,满分 1.0;函数名错直接 0 分(参数再巧合也不算)。参数是宽容的深度比较:值为 None 的可选参数可以省略、字典键递归比较、列表**顺序无关**(调闹钟传 {hour, minute} 还是 {minute, hour} 都是同一次调用)。为什么用 0.4/0.6 而不是非对即错?因为二元奖励太陡:调用半对(函数对了参数错了)和完全乱写一样是 0 分,模型从"半对"里学不到任何东西。语义分把它拆成 0.4,模型才知道"函数名选对是有价值的",信号才学得动。

第三,**多调用链按序对齐、部分给分**。一条请求可能要求连续两个调用,只做对一个,给 0.5,而不是 0。这教会它链式和顺序。

**GRPO:根据奖励优化输出。** 语言模型原本学习预测下一个 token,RL 则提高高奖励输出的概率。对一条问题,GRPO 分为四步:

第一步,**采样**。让模型**按当前的自己**,对同一条 prompt 生成 G=8 个候选回答。

第二步,**打分**。8 个回答每个用奖励函数打分,得到 r₁…r₈(0 到 1)。注意这里奖励就是**上面那个宽容语义评分器**,是规则函数,不是学出来的奖励模型——它可验证,不用再训一个打分模型。

第三步,**组内归一化**。8 个回答放在一起比,每个回答的优势 = (自己的分 − 组平均) ÷ 组标准差:

$$

\\hat{A}_i = \\frac{r_i - \\mathrm{mean}(\\mathbf{r})}{\\mathrm{std}(\\mathbf{r})}

$$

这一步把绝对奖励转换为组内相对奖励:高于组内平均的回答优势为正,低于平均的回答优势为负。不同批次的整体难度可能不同,组内标准化可以减小这种尺度变化。"Group Relative" 指的就是以同组回答作为相对基准。

第四步,**更新**。优势为正的回答,里面的 token 概率被往高推;为负的,往低压(策略梯度)。然后回到第一步,反复 400 步,再续训到 1000 步。

**GRPO 与 PPO 的主要区别。** PPO 需要额外训练 Critic 网络,GRPO 用组内平均奖励构造基准。下面是《The Illustrated GRPO》(abderrahmanskiredj.github.io/the-illustrated-grpo)给出的四步流程:**采样多个回答 → 计算奖励 → 组内标准化 advantage → 裁剪更新**:

![GRPO 算法流程:采样多个回答 → 奖励 → 组内标准化 → 裁剪更新(来源:The Illustrated GRPO)](/images/grpo-principle.png)

PPO 算优势用的是 GAE(广义优势估计),每一步都要估计"当前状态值多少钱"的 V(s):

$$

\\delta_t = r_t + \\gamma V(s_{t+1}) - V(s_t)

$$

而这个 V(s) 本身是一个要训练的模型(Critic),专门预测"从这个状态往后还能拿多少分"。所以 PPO 同时训两个模型:做决定的 Actor + 估分的 Critic。Critic 占差不多一倍的显存,多一份前向和反向。

GRPO **不训练 Critic,而是把同组 G 个回答的平均分作为基准线**。平均分近似表示当前策略在这道题上的通常水平,单个回答与平均值的差异构成优势。这样可以减少一个模型的显存和计算开销,论文将其描述为 "significantly reducing training resources"。这也是单张 5090 能训练 3B 模型的原因之一。

从偏好学习的角度看,回答 A 高于组内平均,意味着它优于多数同组候选,模型会提高这类回答的概率,而不是拟合某个固定的绝对分数。(PPO 的 KL 惩罚加在奖励里,GRPO 的 KL 直接进入损失,两者都用于限制策略偏移。)

**KL 约束。** 更新时限制新模型与 SFT 模型之间的差异(beta=0.005)。没有这一约束,模型可能利用奖励函数的缺陷,例如生成格式正确但函数错误的调用,或不断增加无关文本。这类现象称为"奖励破解",KL 约束用于限制策略偏移。

**结果:从 21% 到 51%。** 评测在 DroidCall 留出的 200 条测试上做,评分器就是上面那个奖励函数:

| 配置 | exact acc | mean reward |
|---|---|---|
| 出厂 Qwen2.5-3B-Instruct | 42/200 = 21.0% | 0.358 |
| + SFT(DroidCall, 3 epochs) | 50/200 = 25.0% | 0.395 |
| + GRPO(400 步) | 95/200 = 47.5% | 0.649 |
| + GRPO(续训到 1000 步) | 102/200 = 51.0% | 0.684 |

这里有两个指标。exact acc 要求函数名和参数全部正确(可选参数可以省略);mean reward 会给部分正确的调用计分。结果显示,SFT 使准确率提高 4 个百分点,GRPO 阶段带来了更大的增量。

**模型规模选择:3B。** 端侧模型通常集中在 1B~4B:Meta 为 Llama 3.2 提供 1B 和 3B 两档,3B 量化后约 2GB;Google Gemma 3n 的有效参数为 2B/4B;Qwen2.5 的较小规格为 0.5B/1.5B/3B。这个范围主要受手机内存、功耗和散热限制:3B 量化后约 2GB,7B 通常需要 4GB 以上内存。

选择 3B 而不是 1.5B,是因为函数调用需要识别意图、从 24 个函数中选择目标并填写参数。Qwen 官方博客指出,"even models with just 3 billion parameters are now delivering highly competitive results"。DroidCall 论文也使用 Qwen2.5-3B 和 Gemma2-2B,微调后接近或超过 GPT-4o。3B 因而是在内存占用和任务能力之间的一种折中。

这个 3B 模型量化成 INT4 后约为 2GB,与 Llama 3.2-3B 量化后的体积接近,可以装入 8GB 内存的手机。DroidCall 论文还提供了端到端 Android 演示,使用 mllm 运行微调后的模型,完成定闹钟和发邮件等操作。

真要部署,大概的流程有流程四步。

**第一步,把 LoRA 合回主模型。** 训练时只训了低秩旁路(adapter),部署时得先合并成完整模型。

**第二步,量化。** FP16 的 3B 模型约 6GB,对手机内存压力较大;量化成 INT4 后约 2GB。llama.cpp 等框架支持 1.5 到 8 位整数量化,可以在体积、速度和精度之间选择。

**第三步,转格式。** 不同推理框架要不同格式:llama.cpp 用 GGUF、MLX 用自己的、ExecuTorch 转 .pte。

**第四步,选推理框架。** 主流几条路:

- **llama.cpp**:使用 C++ 编写,可在 CPU/GPU 上运行,支持 Android 和 iOS,GGUF 工具与社区资源较多;
- **MLX**:Apple 官方框架,针对苹果芯片设计;
- **ExecuTorch**:PyTorch 官方端侧方案,能把部分计算委派给手机 NPU/GPU,是 Meta 在 Instagram、WhatsApp 上做端侧推理的生产引擎;
- **MNN(阿里)、NCNN(腾讯)**:移动端优化的国产框架。MNN 带一个 Android LLM Chat 应用,还新增了对高通 Hexagon DSP/NPU 的加速后端。

现代手机还提供 NPU/DSP,例如骁龙 Hexagon 和 Apple Neural Engine,部分框架可以把计算委派给这些后端。量化可能降低精度,尤其是参数值必须准确的函数调用任务,因此需要单独评测。实际部署还要处理缓存、流式输出和功耗控制等问题。

评测评分器与训练奖励函数相同,因此 51% 表示该评分器定义下的准确率,不等于系统执行后的用户满意度。评分器比严格的字符串比对更能容纳格式差异,但仍只是代理指标:它可以验证调用是否符合规范,不能验证调用是否完全符合用户的真实意图。这是可验证奖励的边界。

**补充结果。** ① 400 步到 1000 步之间,准确率从 47.5% 提高到 51%,尚未进入明显平台期。② GRPO 采样使用 TRL 的进程内生成(n=8),没有使用 vLLM;3B 模型可以在一张 32GB 显卡上完成整个 pipeline。③ 代码、权重和数据已开源:github.com/xilon-my/ondevice-fc`,
}

export default article
