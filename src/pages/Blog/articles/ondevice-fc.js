const article = {
  slug: 'ondevice-fc',
  date: '2026-08-18',
  name: '我自己微调了一个 3B 小模型',
  description: '手机上的 agent 得会调函数:用户说"定个闹钟",小模型要输出一个 JSON 调用。我用 DroidCall 数据,在一张 RTX 5090 上把 Qwen2.5-3B 的函数调用准确率从 21% 训到 51%。SFT 只涨了 4 个点,GRPO 直接翻倍——功夫全在奖励函数上。',
  tags: ['RL', 'Agent', 'Function-Calling', 'LLM'],
  category: 'Project',
  author: 'shannon',
  detail: `**这次做的是手机上跑 agent 的前置问题:小模型会不会调函数。** 场景是这样的:用户说一句"帮我定明天早上 7 点半的闹钟",模型要输出一个函数调用——\`{"ACTION_SET_ALARM": {"EXTRA_HOUR": 7, "EXTRA_MINUTES": 30}}\`——系统拿到 JSON 再去执行。要求模型小(3B 级别,手机内存才装得下),而且不能全靠云端:隐私、流量、离线都要能跑。那么问题来了,这么小的模型,能不能把"一句人话"变成"一次准确的函数调用"?

**数据长什么样。** 任务和数据用的是 **DroidCall**,一个把安卓 intent 调用抽象成函数调用的基准。它预定义了 24 个函数(定闹钟、发邮件、搜网页、挑联系人……),每个函数是这样一份定义——名字、一段描述、参数表(每个参数标了类型、是否必填、默认值)。比如答案里会反复出现的定闹钟,摘自真实的 api.jsonl:

\`\`\`
ACTION_SET_ALARM
  EXTRA_HOUR:    int,  必填  闹钟小时,24 小时制
  EXTRA_MINUTES: int,  必填  分钟
  EXTRA_MESSAGE: str,  选填  备注,默认空
  EXTRA_DAYS:    list, 选填  响铃日期,默认 None
  EXTRA_VIBRATE: bool, 选填  是否震动,默认 false
\`\`\`

约 1 万条样本,每条是一个三元组:(用户请求, 相关函数定义, 标准调用)。一条简单的长这样(论文原例):

\`\`\`
query:   "Wake me up at 8:30"
answers: [{"id": 0, "name": "ACTION_SET_ALARM",
           "arguments": {"EXTRA_HOUR": 8, "EXTRA_MINUTE": 30}}]
\`\`\`

模型要做的:看到 query,从函数列表里选出 ACTION_SET_ALARM,再把"8:30"拆成小时=8、分钟=30 填进参数。难的不是格式,是这些细节——8:30 怎么拆、可选参数要不要补、几个候选函数里选哪个。数据分两档生成:simple 是单函数单调用(上面这种);complex 要多函数组合,甚至一个调用的参数引用前一个调用的返回值(比如先"挑联系人",再把拿到的 URI 填进"发邮件")。数据是拿 GPT-4-turbo 生成的,按相似度去重,评测留出 200 条,不参与训练。

**方法:先 SFT 热身,再 GRPO 强化。** 模型是 Qwen2.5-3B-Instruct,训练只改一小部分参数(LoRA 低秩微调),整个 pipeline 只占一张 RTX 5090(32GB)。训练分两段:第一段 SFT,让模型先学会"输出长这样";第二段 GRPO,让模型学会"这样输出才对"。先看 SFT。

**SFT 热身:教会"格式",教不会"对错"。** 先把它具体干了什么拆开,再说它为什么不称职。SFT 在干的事,可以看下面这张图(来源:CSDN《SFT-指令微调是什么?SFT的流程是怎样的?》):大模型先预训练出通用语言能力,再用一批"指令-回复"对做有监督微调,让它学会照着人类意图作答:

![SFT:预训练后,用指令-回复对有监督微调(来源:CSDN,智泊AI《SFT-指令微调是什么?SFT的流程是怎样的?》)](/images/sft-process.png)

第一步,**把数据变成模型认识的对话**。DroidCall 原始样本是 (query, tools, answers) 三元组,\`build_fc_sft.py\` 把它拼成三段对话:system 是"你有这些函数可以用"加上 24 个函数的 JSON 定义,user 是用户请求(比如 "Wake me up at 8:30"),assistant 是标准调用 JSON。这一步值得说:模型从生下来见到的就是这种"系统+用户+助手"的结构,SFT 只是把任务装进它已经会的格式里,它才学得动。

第二步,**只改一小部分参数(LoRA)**。Qwen2.5-3B 三十亿参数,全量微调显存和时长都不划算。LoRA 的做法:主模型全部冻结,只在每层注意力/前馈的投影矩阵旁边挂一个低秩旁路(r=16),训练只更新这个旁路。旁路参数少得可怜,所以单卡 5090 就够。下面这张图是 LoRA 原始论文的 Figure 1,它想说的就是"重参数化":**训练时预训练权重 W 冻着不动,只学两个低秩小矩阵 A、B;用的时候把 W + BA 合回一个大矩阵,推理速度完全不受影响**:

![LoRA 重参数化:只训练低秩矩阵 A 和 B(来源:LoRA 论文,arXiv 2106.09685,Figure 1)](/images/lora-reparam.svg)

第三步,**拿交叉熵把"下一 token"往标准答案上推**。给定 system+user,模型逐个预测 assistant 的 token:第一个该是 \`[\`,然后 \`{\`、\`"name"\`……每一步输出一个概率分布,和标准答案的下一个 token 比:对,损失低;错,损失高。梯度顺着把概率调对。3 个 epoch,学习率 1.41e-5。

这一套下来,模型学会了"看到请求就输出合法 JSON 调用"这个格式。**但 SFT 优化的是"文本像不像标准答案",不是"这次调用能不能成"。** 25% 就是这么来的:格式全对,但函数选错、参数填错的情况不少。这就轮到 GRPO 了。

为什么要先 SFT 再 GRPO,不能直接上 RL?因为 RL 是"自己生成、按结果给分、往高分推"。如果模型连合法的调用都产不出(输出一坨散文),奖励函数无从打分,信号是零,推不动。先 SFT 把"能稳定产出合法调用"这件事定下来,RL 才有东西可推。

**但 GRPO 只是个放大器,放大什么取决于奖励函数给什么信号。** 这句话是全项目最重要的一句。奖励给不出信号,GRPO 白推;给错信号,GRPO 把错的放大。所以力气主要花在奖励函数上,不是训练参数上。这也正好是 Code-R1 那篇文章的结论:reward quality matters more than reward quantity——可靠的奖励,比奖励的数量重要。

**奖励函数长什么样。** 三个设计,单个看都不起眼,合起来决定成败。

第一,**宽容的解析**。模型不是每次都乖乖吐一个干净的 JSON。它可能把调用包在 \`<think>\` 里、夹在散文里、用 \`action\` 当字段名、甚至输出 \`FUNC(ARG=v)\` 这种 Python 调用式。解析器只认一种格式的话,大量真实输出会被判 0 分——不是它不对,是没被听懂。所以解析器要容忍这几种格式,先把"模型到底想调哪个函数"从各种外壳里挖出来。

第二,**语义打分,不是字符串比对**。解析出来之后按规则打分:函数名对 +0.4,参数对 +0.6,满分 1.0;函数名错直接 0 分(参数再巧合也不算)。参数是宽容的深度比较:值为 None 的可选参数可以省略、字典键递归比较、列表**顺序无关**(调闹钟传 {hour, minute} 还是 {minute, hour} 都是同一次调用)。为什么用 0.4/0.6 而不是非对即错?因为二元奖励太陡:调用半对(函数对了参数错了)和完全乱写一样是 0 分,模型从"半对"里学不到任何东西。语义分把它拆成 0.4,模型才知道"函数名选对是有价值的",信号才学得动。

第三,**多调用链按序对齐、部分给分**。一条请求可能要求连续两个调用,只做对一个,给 0.5,而不是 0。这教会它链式和顺序。

**GRPO:真正的主力。** 先说"强化"到底在干什么,再走一遍流程。大模型本来只是"预测下一个词",RL 不是学预测,是学"什么样的输出能拿分"。对一条问题,GRPO 走四步:

第一步,**采样**。让模型**按当前的自己**,对同一条 prompt 生成 G=8 个候选回答(prompt 就是不含答案的那段对话:system+tools+user)。

第二步,**打分**。8 个回答每个用奖励函数打分,得到 r₁…r₈(0 到 1)。注意这里奖励就是**上面那个宽容语义评分器**,是规则函数,不是学出来的奖励模型——它可验证,不用再训一个打分模型。

第三步,**组内归一化**。8 个回答放在一起比,每个回答的优势 = (自己的分 − 组平均) ÷ 组标准差:

$$\\hat{A}_i = \\frac{r_i - \\mathrm{mean}(\\mathbf{r})}{\\mathrm{std}(\\mathbf{r})}$$

这是 GRPO 的灵魂。它把"绝对分"变成"相对分":比组内平均好的,优势为正;差的,为负。为什么不直接用绝对分?因为打分尺度会漂移——这批问题可能整体都难、下批都简单。组内相对化让"好/坏"永远在当下这批里比,信号稳定。这也正是 "Group Relative" 这个名字的来历:基准不是绝对满分,是**这一组之间的相对好坏**。

第四步,**更新**。优势为正的回答,里面的 token 概率被往高推;为负的,往低压(策略梯度)。然后回到第一步,反复 400 步,再续训到 1000 步。

**GRPO 和 PPO 到底差在哪?** 一句话:**PPO 多训一个 Critic 网络,GRPO 用组内平均替代它**。下面这张图是 verl(一个 RL 训练框架)官方文档里的 GRPO 训练流程——注意图里没有 Critic 那条路:

![GRPO 训练流程(来源:verl 官方文档)](/images/grpo-flow.png)

PPO 算优势用的是 GAE(广义优势估计),每一步都要估计"当前状态值多少钱"的 V(s):

$$\\delta_t = r_t + \\gamma V(s_{t+1}) - V(s_t)$$

而这个 V(s) 本身是一个要训练的模型(Critic),专门预测"从这个状态往后还能拿多少分"。所以 PPO 同时训两个模型:做决定的 Actor + 估分的 Critic。Critic 占差不多一倍的显存,多一份前向和反向。

GRPO 的聪明之处在于:**不训 Critic,直接把这一组 G 个回答的平均分当基准线**——平均分代表"当前策略在这道题上的正常水平",某个回答比正常水平好多少,优势就是多少。一个模型就够,显存省下来,论文原话是 "significantly reducing training resources"。单卡 5090 跑 3B 模型,靠的就是这个。

一个直觉:GRPO 的组内比较本身就是一种偏好——回答 A 比组内平均好,就相当于"A 比大多数候选强",模型往"比大多数强"的方向走,而不是往某个绝对标准走。(PPO 的 KL 惩罚加在奖励里,GRPO 的 KL 直接进损失,细节不同,思路一致:别让模型跑太远。)

**另外有条保险。** 更新时有个 KL 约束:新模型不能离 SFT 后的自己太远(beta=0.005)。为什么要这个?没有它,模型可能钻奖励函数的空子——比如输出"格式正确但函数乱选"的调用去骗分,或者生成越来越长的废话。这行话叫"奖励破解",是强化学习的经典翻车方式,KL 就是它的安全带。

**结果:从 21% 到 51%。** 评测在 DroidCall 留出的 200 条测试上做,评分器就是上面那个奖励函数:

| 配置 | exact acc | mean reward |
|---|---|---|
| 出厂 Qwen2.5-3B-Instruct | 42/200 = 21.0% | 0.358 |
| + SFT(DroidCall, 3 epochs) | 50/200 = 25.0% | 0.395 |
| + GRPO(400 步) | 95/200 = 47.5% | 0.649 |
| + GRPO(续训到 1000 步) | 102/200 = 51.0% | 0.684 |

两个指标。exact acc:这次调用完全正确才算对——函数名对、参数对(可选参数可以省略)。mean reward:宽容一点的语义分,上面奖励函数那节讲过,它给"只对了一半"也打分。**最值得看的不是 51% 本身,而是 SFT 只涨了 4 个点、GRPO 直接翻了一倍多。** 同样是训练,一个热身,一个才是主角。

**为什么是 3B?端侧主流就是 1B~4B。** 端侧(手机、平板、笔记本)能装的模型,主流尺寸基本落在这个区间:Meta 的 Llama 3.2 专门为端侧做了 1B 和 3B 两档,官方模型卡原话是 "running locally on edge",量化后 3B 约 2GB;Google 的 Gemma 3n 官方说 "designed for efficient execution on everyday devices such as laptops, tablets or phones",有效参数 2B/4B;Qwen2.5 最小的三档是 0.5B/1.5B/3B。为什么会聚在这里?手机内存摆着:3B 量化后约 2GB,7B 就得 4GB+,跑起来还热还掉电。再往上就不是"端侧"了。

那为什么选 3B,不选更小的 1.5B?因为函数调用不是"续写",它要求模型真理解意图、在 24 个函数里选对、还填对参数——这活太小了干不动。Qwen 官方博客原话:"even models with just 3 billion parameters are now delivering highly competitive results",把 3B 标成小模型里的性能甜点。更直接的证据是 DroidCall 论文自己:实验用的就是 Qwen2.5-3B 和 Gemma2-2B,微调后能接近甚至超过 GPT-4o。等于原论文已经验证了"这个尺寸能把这活干好"。选 3B 不是拍脑袋,是"装得下"和"干得了"的交点。

**一句诚实的提醒。** 评测用的评分器,和训练时的奖励函数,是同一个。所以 51% 是"**在这个评分器眼里**的 51%",不是系统执行之后用户满意度的 51%。评分器比死板的字符串比对强得多,但它终究是一个代理——它能验证"这次调用符不符合规范",验证不了"这是不是用户真正想要的"。这正是"可验证奖励"的边界,也是它为什么能被 GRPO 用:验证不了的东西,强化学习就没法拿它当信号。

**几件小事。** ① 续训还有增量:400 步 47.5% → 1000 步 51%,信号没耗尽,RL 的收益还没到平台期。② 全程没上 vLLM,GRPO 的采样用 TRL 的进程内生成(n=8)就够——模型才 3B,一张 32GB 卡同时跑得动,整个 pipeline 才没被显存卡住。③ 代码、权重、数据都开源了:github.com/xilon-my/ondevice-fc`,
}

export default article
