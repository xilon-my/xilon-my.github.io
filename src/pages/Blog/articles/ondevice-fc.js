const article = {
  slug: 'ondevice-fc',
  date: '2026-08-18',
  name: '面向手机端的函数调用模型：SFT、规则奖励与 GRPO',
  description: '以 DroidCall 的闹钟调用为例，说明如何用 SFT 建立输出格式，再用规则奖励和 GRPO 优化函数选择与参数填写。Qwen2.5-3B-Instruct 在 200 条测试样本上的满分率由 21.0% 提高到 51.0%。',
  tags: ['RL'],
  category: 'Project',
  author: 'shannon',
  takeaway: '这组实验得到的是评分器口径下的函数调用满分率，不是手机上的执行成功率。SFT 先把模型带入可解析的输出分布，规则奖励再区分函数名和参数是否正确；GRPO 利用同一请求下 8 个回答的相对奖励更新 LoRA。当前结果有效，但还需要严格评分器、独立奖励实现和真机测试。',
  detail: String.raw`
## 1. 任务：把一句话变成一次可执行调用

用户输入“帮我定早上 7 点半的闹钟”时，Android 执行层需要的不是一段解释，而是结构化调用：

\`\`\`json
[
  {
    "name": "ACTION_SET_ALARM",
    "arguments": {
      "EXTRA_HOUR": 7,
      "EXTRA_MINUTES": 30
    }
  }
]
\`\`\`

模型要完成三个判断：从候选函数中选择 \`ACTION_SET_ALARM\`，从自然语言中提取 7 和 30，再按照接口约定写出 JSON。任意一步错误，系统都可能无法执行请求。

本文记录一组单卡实验：基座模型是 Qwen2.5-3B-Instruct，数据来自 [DroidCall](https://github.com/UbiquitousLearning/DroidCall)，训练依次经过 SFT 和 GRPO，只更新 LoRA 参数。硬件是一张 RTX 5090（32 GB）。实验目标是得到一个适合继续量化和端侧验证的 3B 模型；本文没有给出真机延迟、内存或功耗结果，因此“面向手机端”描述的是模型规模和任务，不代表已经完成手机部署。

## 2. 先看实验全貌

整个过程可以压缩成四步：

| 步骤 | 输入 | 学习或评测信号 | 输出 |
|---|---|---|---|
| 数据转换 | 请求、候选函数、标准调用 | 无 | 对话格式数据 |
| SFT | 对话格式数据 | token 负对数似然 | SFT LoRA |
| GRPO | 每个请求采样 8 个回答 | 单次调用得 0、0.4 或 1，多次调用取均值 | GRPO LoRA |
| 测试 | 200 条留出样本 | 同一个规则评分器 | 满分率与平均奖励 |

最终结果如下：

| 配置 | 满分样本数 | 满分率 | 平均奖励 |
|---|---:|---:|---:|
| Qwen2.5-3B-Instruct | 42/200 | 21.0% | 0.358 |
| + SFT，3 epochs | 50/200 | 25.0% | 0.395 |
| + GRPO，400 步 | 95/200 | 47.5% | 0.649 |
| + GRPO，续训到 1000 步 | 102/200 | 51.0% | 0.684 |

这里把仓库中的 \`exact accuracy\` 译为“满分率”，因为它不是字符串完全一致率。生成结果会先被宽容解析，再按语义评分；奖励恰好等于 1 的样本才计入满分。这个口径将在第 5 节具体说明。

## 3. 数据：请求、工具定义和标准调用

DroidCall 把 Android intent 表示成函数调用，包含 24 个函数，覆盖闹钟、计时器、邮件、网页搜索和联系人等操作。每个函数都给出名称、说明、参数类型、必填项和默认值。闹钟函数可以简化为：

\`\`\`text
ACTION_SET_ALARM
  EXTRA_HOUR:    int， 必填，小时，24 小时制
  EXTRA_MINUTES: int， 必填，分钟
  EXTRA_MESSAGE: str， 选填，默认空字符串
  EXTRA_DAYS:    list，选填，默认 None
  EXTRA_VIBRATE: bool，选填，默认 false
\`\`\`

每条数据有三个核心字段：

- \`query\`：用户的自然语言请求；
- \`tools\`：当前请求可用的函数定义；
- \`answers\`：按顺序排列的标准调用。

simple 样本只有一次调用。complex 样本可能包含多次调用，后一次调用还可以引用前一次调用的返回值。训练集约有 1 万条数据；转换脚本固定留出 500 条作为验证集。官方的 200 条测试样本不参与训练，只用于表中的最终评测。GRPO 又从 SFT 训练部分固定抽取 2,000 条请求，而不是使用全部训练数据。

这种划分保证了测试样本与训练样本分离，但评测逻辑没有与训练奖励分离：两者调用同一个 \`fc_reward.py\`。因此，测试能够衡量模型是否适应这套规则，不能排除模型对评分器特性的适应。

## 4. SFT：先让输出进入可解析的分布

### 4.1 数据怎样进入模型

\`build_fc_sft.py\` 把每条数据转换成三段消息：

- system：函数定义的 JSON；
- user：用户请求；
- assistant：标准调用数组。

聊天模板再把三段消息串成一个 token 序列。记整个序列为 $z_1,z_2,\ldots,z_T$，其中 $T$ 是序列长度。自回归语言模型在位置 $t$ 根据此前的 token $z_{<t}$ 预测 $z_t$，负对数似然为：

$$
\mathcal L_{\mathrm{SFT}}(\theta)
=-\sum_{t=1}^{T}\log\pi_\theta(z_t\mid z_{<t})
$$

$\theta$ 表示模型参数，$\pi_\theta(z_t\mid z_{<t})$ 表示模型给真实下一个 token 分配的概率。概率越高，对应损失越小。

这里需要区分“通常怎样做”和“当前代码怎样做”。如果只想训练模型生成 assistant 回答，可以用掩码 $m_t$ 排除 system 和 user token：

$$
\mathcal L_{\mathrm{answer}}(\theta)
=-\sum_{t=1}^{T}m_t\log\pi_\theta(z_t\mid z_{<t})
$$

$m_t=1$ 表示位置 $t$ 属于 assistant 回答，$m_t=0$ 表示该位置只作为上下文。当前 \`train_fc_sft.py\` 没有设置 TRL 的 \`assistant_only_loss=True\`，数据格式又是 \`messages\` 而不是 \`prompt/completion\`，所以实际损失覆盖序列中的 system、user 和 assistant token。它仍然能学习函数调用，但“只在标准调用上计算损失”并不是当前实验的准确描述。若要比较两种训练口径，需要修改配置后重新训练，不能从现有 25.0% 结果推断差异。

### 4.2 LoRA 只训练低秩增量

实验不更新 3B 模型的全部参数，而是在注意力和前馈层的投影矩阵上加入 LoRA。对原权重矩阵 $W$，更新后的矩阵写成：

$$
W'=W+\frac{\alpha}{r}BA
$$

$W$ 保持冻结，训练只更新 $A$ 和 $B$。若 $W$ 的形状是 $d_{\mathrm{out}}\times d_{\mathrm{in}}$，则：

$$
A\in\mathbb R^{r\times d_{\mathrm{in}}},
\qquad
B\in\mathbb R^{d_{\mathrm{out}}\times r}
$$

$r$ 是低秩维度，$\alpha/r$ 是增量的缩放系数。本项目取 $r=16$、$\alpha=32$，缩放系数为 2。LoRA 同时作用于 \`q_proj\`、\`k_proj\`、\`v_proj\`、\`o_proj\`、\`gate_proj\`、\`up_proj\` 和 \`down_proj\`。

![LoRA：冻结原权重，只训练低秩增量](/images/lora-reparam.svg)

以一个 $2048\times2048$ 的矩阵为例，全量矩阵有 4,194,304 个参数；$r=16$ 时，$A$ 和 $B$ 一共有：

$$
16\times(2048+2048)=65,536
$$

这相当于原矩阵参数量的 1.56%。这个比例只适用于该矩阵；整个 adapter 的大小还取决于目标矩阵的数量和形状。

$r=16$ 是本次实验的配置，不是由结果证明的最优值。若要选择 rank，应固定数据、随机种子、目标矩阵、训练步数和 $\alpha/r$，分别比较 $r\in\{4,8,16,32\}$ 的验证集结果、显存和训练时间。当前实验没有这组消融。

### 4.3 SFT 的作用和结果

SFT 训练 3 个 epoch，学习率为 $1.41\times10^{-5}$，满分率从 21.0% 提高到 25.0%。它提供标准序列的逐 token 信号，适合建立对话模板和 JSON 结构；但它不会执行调用，也不直接区分“函数名正确、参数错误”和“函数名、参数都正确”。下一阶段用规则评分器提供这种区分。

## 5. 规则奖励：把完整回答映射到一个分数

GRPO 本身不规定奖励来自哪里。这个任务可以根据标准调用直接核对函数名和参数，因此 \`fc_reward.py\` 使用确定性规则，不再训练奖励模型。

### 5.1 先把不同文本归一成调用

解析器依次尝试：

- JSON 调用数组；
- 单个 JSON 对象；
- 以 \`name\` 或 \`action\` 表示函数名的对象；
- 以函数名为键的对象；
- \`FUNC(ARG=value)\` 形式。

它也会从说明文字或 \`<think>\`、\`<answer>\` 标签中提取嵌入的 JSON。所有成功解析的结果最终归一成：

\`\`\`json
{"name": "函数名", "arguments": {}}
\`\`\`

无法解析时奖励为 0。宽容解析可以减少“语义正确但外层格式不同”造成的误判，也扩大了评分器接受的输出空间。模型输出真正进入 Android 执行层之前，仍然需要唯一格式和严格 schema 校验。

### 5.2 单次调用为什么只有 0、0.4 和 1

定义 $I_{\mathrm{name}}$ 为函数名是否相同的指示变量，相同时为 1，否则为 0；$I_{\mathrm{args}}$ 对参数整体是否匹配作同样定义。单次调用奖励为：

$$
R_{\mathrm{call}}
=I_{\mathrm{name}}\left(0.4+0.6I_{\mathrm{args}}\right)
$$

因此只有三种结果：

| 生成结果 | 奖励 | 原因 |
|---|---:|---|
| \`ACTION_SET_ALARM(EXTRA_HOUR=7, EXTRA_MINUTES=30)\` | 1.0 | 函数名和全部必要参数正确 |
| \`ACTION_SET_ALARM(EXTRA_HOUR=7, EXTRA_MINUTES=0)\` | 0.4 | 函数名正确，参数整体不匹配 |
| \`ACTION_SET_TIMER(EXTRA_HOUR=7, EXTRA_MINUTES=30)\` | 0 | 函数名错误 |
| 无法解析的自然语言 | 0 | 没有得到调用结构 |

参数不是按字段比例计分。小时正确而分钟错误时，参数部分仍然是 0。比较器会递归检查字典；标准答案中值为 \`None\` 的字段允许省略；列表按顺序无关的方式比较。

当前实现还有一个重要边界：生成结果可以包含标准答案没有要求的额外字典字段，额外字段不会扣分。这里的“满分”表示必要字段都匹配，不表示生成参数与标准答案完全相等。

### 5.3 多次调用怎样对齐

设标准答案包含 $N$ 次调用，第 $j$ 次调用的得分是 $R_j$，最终奖励为：

$$
R=\min\left(1,\frac{1}{N}\sum_{j=1}^{N}R_j\right)
$$

如果标准答案依次要求 A 和 B，而模型只正确生成 A，奖励是 $(1+0)/2=0.5$。评分器按照标准答案顺序向后扫描生成调用，所以顺序会影响结果。

扫描过程允许跳过额外调用，额外调用本身没有单独惩罚。这意味着一个输出可以在包含多余调用的同时得到满分。对真实设备而言，多执行一次非预期 intent 可能产生实际副作用，因此部署评测必须另加严格规则：拒绝额外参数、额外调用、类型错误和未知字段。

## 6. GRPO：同一请求的 8 个回答相互比较

训练从 SFT adapter 开始。默认使用 2,000 条请求，每个请求采样 $G=8$ 个回答，最大回答长度为 512 token，学习率为 $5\times10^{-5}$，KL 系数 $\beta=0.005$。每卡 batch size 为 1，梯度累积 8 次；生成使用 TRL 的进程内引擎，没有使用 vLLM。

### 6.1 从 8 个奖励算相对优势

给定请求 $q$，策略生成 8 个回答 $o_1,\ldots,o_8$，评分器得到：

$$
\mathbf r=[1.0,1.0,0.4,0.4,0,0,0,0]
$$

组内平均奖励为 $\bar r=0.35$。项目固定的 TRL 1.10 使用带贝塞尔修正的样本标准差，因此这组数据的标准差约为 $s_r=0.438$。第 $i$ 个回答的优势是：

$$
A_i=\frac{r_i-\bar r}{s_r+\varepsilon}
$$

$\varepsilon$ 是防止分母为 0 的小常数。代入后：

| 奖励 | 结果 | 优势 |
|---:|---|---:|
| 1.0 | 函数名和参数都正确 | $+1.49$ |
| 0.4 | 只有函数名正确 | $+0.11$ |
| 0 | 函数名错误或无法解析 | $-0.80$ |

0.4 仍然得到正优势，因为它高于本组平均值 0.35。若 8 个回答全部得 0 或全部得 1，组内没有差异，优势都是 0，这个请求不会提供策略梯度。

### 6.2 一个回答中的 token 共用同一优势

奖励只在完整回答生成后计算。第 $i$ 个回答中的所有 token 都使用同一个 $A_i$：

$$
\hat A_{i,t}=A_i,
\qquad t=1,\ldots,|o_i|
$$

在正确回答中，函数名、小时、分钟和 JSON 标点都接收同一个正优势。算法知道整条回答高于组内平均，却不能从终局奖励中直接判断哪个 token 决定了正确性。这是回答级奖励的信用分配限制。

### 6.3 当前配置下，概率比在第一次更新时等于 1

一般的 GRPO 裁剪目标会比较当前策略 $\pi_\theta$ 和生成这批回答的旧策略 $\pi_{\theta_{\mathrm{old}}}$。第 $t$ 个 token 的概率比为：

$$
\rho_{i,t}(\theta)
=\frac{\pi_\theta(o_{i,t}\mid q,o_{i,<t})}
{\pi_{\theta_{\mathrm{old}}}(o_{i,t}\mid q,o_{i,<t})}
$$

原始目标中的策略项是：

$$
\min\left(
\rho_{i,t}A_i,
\operatorname{clip}(\rho_{i,t},1-\epsilon,1+\epsilon)A_i
\right)
$$

但这次脚本没有设置 \`num_iterations\`，TRL 1.10 的默认值是 1。进程内生成与优化对齐时，旧概率直接取当前概率的停止梯度副本：

$$
\rho_{i,t}
=\frac{\pi_\theta(o_{i,t}\mid q,o_{i,<t})}
{[\pi_\theta(o_{i,t}\mid q,o_{i,<t})]_{\mathrm{stop}}}
=1
$$

分母停止梯度，所以比值虽然数值为 1，分子仍然产生梯度；正优势提高采样 token 的概率，负优势降低它。由于每批回答只更新一次，概率比不会越过裁剪边界，clip 在当前默认配置下不发挥实际作用。只有复用同一批回答做多次更新，或生成与训练分布不一致时，旧策略概率和裁剪才会成为实际约束。

### 6.4 KL 参考分布是基座模型

训练还使用固定参考分布 $\pi_{\mathrm{ref}}$。逐 token 的 KL 估计为：

$$
D_{i,t}
=\frac{\pi_{\mathrm{ref}}(o_{i,t}\mid q,o_{i,<t})}
{\pi_\theta(o_{i,t}\mid q,o_{i,<t})}
-\log\frac{\pi_{\mathrm{ref}}(o_{i,t}\mid q,o_{i,<t})}
{\pi_\theta(o_{i,t}\mid q,o_{i,<t})}-1
$$

策略目标减去 $\beta D_{i,t}$，其中 $\beta=0.005$。$D_{i,t}$ 越大，当前策略偏离参考分布越多，惩罚也越大。

“从 SFT adapter 开始训练”和“以 SFT 模型为 KL 参考”是两件事。当前脚本先让 \`GRPOTrainer\` 在基座模型上创建 LoRA，再加载 SFT adapter；TRL 在计算参考概率时禁用 adapter。因此，这条代码路径中的参考分布是未加 LoRA 的 Qwen2.5-3B-Instruct，而不是冻结的 SFT adapter。若希望 KL 明确约束到 SFT 模型，需要在构建 trainer 前加载可训练 SFT adapter，并保留一份冻结参考 adapter，再重新验证配置。

更完整的 GRPO 推导见站内文章 [Group Relative Policy Optimization：GRPO](/blog/grpo)。

## 7. 结果能说明什么

SFT 使满分率提高 4 个百分点。GRPO 训练到 400 步后再提高 22.5 个百分点，继续到 1000 步后又提高 3.5 个百分点。后 600 步仍有增量，但只有 400 和 1000 两个检查点，不能据此判断收敛位置。

结果支持一个有限结论：对当前数据、模型、采样配置和评分器，规则奖励为函数调用提供了有效训练信号。它不能单独回答以下问题：

- 提升有多少来自 GRPO，有多少来自额外训练 token；
- 0.4 的函数名部分奖励是否优于二元奖励；
- 宽容解析是否提高了真实执行成功率；
- $r=16$、$\beta=0.005$ 和 1,000 步是否接近合适配置；
- INT4 量化后是否仍能准确生成数值参数。

原因是实验没有加入等预算长 SFT、二元奖励、严格 schema 奖励、多个随机种子或 rank 消融等对照组。训练和测试又共用评分器，所以 51.0% 不等于 Android 执行成功率，也不等于用户请求完成率。

一个更完整的下一轮实验至少应同时报告三种指标：当前宽容评分器的分数、拒绝额外字段与调用的严格匹配率、真实 Android intent 的执行结果。前两者可以离线计算，最后一项需要在隔离环境或真机上验证副作用。

## 8. 从 LoRA 到手机还缺哪些步骤

训练完成后，部署链路至少还有四步：

1. 将 LoRA 增量合并回 Qwen2.5-3B-Instruct；
2. 将 BF16 或 FP16 权重量化为 INT4，并重新评测函数名和数值参数；
3. 转换为端侧框架需要的格式，例如 llama.cpp 使用的 GGUF；
4. 在目标设备上测量首 token 延迟、生成速度、峰值内存、功耗和持续运行温度。

3B 模型的 4-bit 权重理论下限约为 1.5 GB，实际模型文件通常还包含量化尺度、元数据和未量化张量，因此接近 2 GB 是合理估计。运行时峰值内存还要加上 KV cache、临时张量和框架缓冲区。函数调用回答较短，有利于限制解码开销；包含候选函数定义的 system prompt 较长，仍会影响首 token 延迟。

部署前还应先修正两项训练口径：明确 SFT 是否只计算 assistant token 的损失，明确 KL 应锚定基座模型还是 SFT 模型。否则复现实验时，即使超参数表相同，训练目标也可能不同。

训练脚本、评分器、测试结果和 LoRA 权重见 [xilon-my/ondevice-fc](https://github.com/xilon-my/ondevice-fc)。
`,
}

export default article
