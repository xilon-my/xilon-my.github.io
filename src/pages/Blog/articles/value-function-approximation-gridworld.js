const article = {
  slug: 'value-function-approximation-gridworld',
  date: '2026-08-20 12:00',
  name: 'Value Function Methods in a 3×3 Grid World',
  description: '第 8 课:值函数方法。价值从"查表"变成"算出来":表里存 n 个数,函数近似只存一个参数向量 w。检索、更新都变了:更新 w 会连带改所有状态的价值(泛化),代价是函数类有限、逼近有残差。沿 最小二乘拟合 → TD-Linear → 目标函数 → 特征选择 → Sarsa/Q-learning 函数近似版 → 深度 Q 学习 走一遍。',
  tags: ['RL'],
  category: 'Course Review',
  folder: 'rl-math',
  author: 'shannon',
  takeaway: '值函数方法把"存 n 个价值"换成"存一个参数 w,价值由函数算出来"。检索要算、更新要改 w——一次更新会改变所有状态的价值(泛化),代价是逼近有残差:TD-Linear 收敛到 w*=A⁻¹b(投影 Bellman 误差的最小点),RMSE 停在函数类的地板,不归零。特征越多,地板越低(平面 0.22 → 二次 0.07 → one-hot 0);d_π 是目标函数的权重(均匀策略下恰好均匀)。动作值版就是把 v 换成 q,更新规则形状不变;DQN 是 Q-learning 加神经网络、目标网络、经验回放,一条 1000 步 episode 学出最优策略(表格版要 10 万步)。',
  detail: String.raw`

## 1. 价值从"查表"变成"算出来"

前几篇里,价值都存在一张表里:每个状态一个格子,想读就读、想改就改。这一篇换个表示:价值由一个函数给出

$$
\hat{v}(s,w)
$$

$s$ 是状态,$w$ 是参数向量。给定 $w$,输入状态 $s$ 就能算出价值——不再需要逐格存。这个函数通常写成线性组合:

$$
\hat{v}(s,w) = \phi^T(s)w
$$

$\phi(s)$ 是状态 $s$ 的特征向量,$w$ 是待定的参数。最直观的例子:3×3 grid world 里,状态是二维位置,用 $x$ 列、$y$ 行(都归一化到 $\{-1,0,1\}$)当特征:

$$
\phi(s)=[1,\ x,\ y]^T,\qquad \hat{v}(s,w)=w_1+w_2x+w_3y
$$

这是一个平面。给一组 $w$,它就给出 9 个格子各自的价值。

从表换成函数,两件事变了。

**检索**:查表直接读格子;函数要算。把状态输进去,算 $\phi(s)$,再做 $\phi^T(s)w$。神经网络做这件事就是一次前向传播。

**更新**:查表只改一个格子;函数必须改 $w$——而 $w$ 一变,所有状态的价值一起变。这带来两个相反的性质。

一是**存储省**。表要存 9 个数($s_1$ 到 $s_9$ 各一个),平面只需要 3 个数($w$ 的三个分量)。状态一多,省得更多:表要存 $n$ 个值,函数只需 $m$ 个参数,通常 $m \ll n$。

二是**有泛化**。表更新一个格子,其他格子不动;函数改一个 $w$,所有状态的价值都跟着变。因此"访问过 $s_3$ 的经验"能顺带修正"没访问过的邻居"的价值。这是函数近似比查表强的地方,也是这一篇后面所有内容的基础。

代价:函数类有限,逼近必然有残差——平面装不下一个弯曲的价值面。§3 会看到这个残差具体有多大。

## 2. 例子:用平面拟合 V_π0

沿用 3×3 grid world,策略还是均匀随机 $\pi_0$(每个动作 $1/5$)。上一篇已用模型解出真实价值

$$
V_{\pi_0}=[-3.083,\ -2.928,\ -3.579,\ -2.727,\ -2.639,\ -2.884,\ -2.864,\ -2.371,\ -2.926].
$$

现在用平面 $\hat{v}(s,w)=w_1+w_2x+w_3y$ 去拟合这 9 个数。拟合就是最小二乘:选 $w$,让平方误差之和最小

$$
\min_w \sum_{i=1}^{9}\big(v_{\pi_0}(s_i)-\phi^T(s_i)w\big)^2.
$$

解析解(正规方程):

$$
w = (\Phi^T\Phi)^{-1}\Phi^T v_{\pi_0},\qquad
\Phi=\begin{bmatrix}\phi^T(s_1)\\\vdots\\\phi^T(s_9)\end{bmatrix}.
$$

算出来:

$$
w=[-2.889,\ -0.119,\ 0.238],
$$

$$
\hat{v}_{\text{plane}}=[-3.008,\ -3.127,\ -3.246,\ -2.770,\ -2.889,\ -3.008,\ -2.532,\ -2.651,\ -2.770].
$$

逐格对比:

| 状态 | $s_1$ | $s_2$ | $s_3$ | $s_4$ | $s_5$ | $s_6$ | $s_7$ | $s_8$ | $s_9$ |
|---|---|---|---|---|---|---|---|---|---|
| $v_{\pi_0}$ | -3.083 | -2.928 | -3.579 | -2.727 | -2.639 | -2.884 | -2.864 | -2.371 | -2.926 |
| $\hat{v}_{\text{plane}}$ | -3.008 | -3.127 | -3.246 | -2.770 | -2.889 | -3.008 | -2.532 | -2.651 | -2.770 |

平面给的是"平滑斜过去"的 9 个数,真值却高低不平:禁区 $s_6$ 凹下去($-2.884$)、目标 $s_9$ 也低($-2.926$,均匀随机下站在目标格反而常踩禁区)、$s_8$ 凸出来($-2.371$)。平面的 RMSE = 0.223——这就是**函数类有限带来的地板**:再多的数据、再准的优化,平面也到不了 0。

换二次曲面 $\phi(s)=[1,x,y,x^2,y^2,xy]^T$(6 个参数):

$$
w=[-2.507,\ -0.119,\ 0.238,\ -0.365,\ -0.208,\ 0.109],
$$

$$
\hat{v}_{\text{quad}}=[-3.090,\ -2.953,\ -3.546,\ -2.752,\ -2.507,\ -2.991,\ -2.831,\ -2.477,\ -2.852].
$$

RMSE 降到 0.074——参数从 3 个加到 6 个,贴近了,但仍有残差。再往上,如果特征取 one-hot($\phi(s)=e_s$,9 个参数,等价于查表),RMSE 归零。

![价值面与拟合:真值、平面、二次](/images/fa-value-surface.png)

## 3. TD-Linear:更新 w,不再更新格子

表版 TD 每走一步更新一个格子。函数版把"格子"换成 $w$:每一步用同一个 TD error 去修正 $w$。

先走一步具体看看。$w_0=0$、步长 $\alpha=0.1$、用平面特征。第一次更新来自一个样本:$s_8\xrightarrow{\mathrm{right}}s_9$,$r=+1$。

TD target:$r+\gamma\,\hat{v}(s_9)=1+0.9\times0=1$。TD error:$1-\hat{v}(s_8)=1-0=1$。更新:

$$
w \leftarrow w + \alpha\cdot\text{error}\cdot\phi(s_8)
= 0 + 0.1\times1\times[1,0,1]^T
= [0.1,0,0.1].
$$

关键看这一步**改变了哪些格子**。把 9 个状态摆成 3×3(行从上到下是 $s_1,\dots,s_9$,和 grid world 的布局一致),更新前全是 0,更新后:

$$
\begin{array}{ccc}
0 & 0 & 0\\
0.1 & 0.1 & 0.1\\
\mathbf{0.2} & \mathbf{0.2} & \mathbf{0.2}
\end{array}
$$

$s_8$ 的值从 0 变 0.2——它被访问过,合理。但 $s_7$、$s_9$ **根本没被访问**,值也变成了 0.2;连 $s_4$、$s_5$、$s_6$ 也从 0 变成了 0.1。为什么?

回看更新式:$w \leftarrow w + 0.1\times\phi(s_8)$,而 $\phi(s_8)=[1,0,1]$——所以 $w_1$ 和 $w_3$ 各加 0.1,得到 $w=[0.1,0,0.1]$。代入 $\hat{v}(s,w)=w_1+w_2x+w_3y=0.1+0.1y$:价值只由 $y$ 决定。底部一行 $y=1$ 都是 0.2,中间一行 $y=0$ 都是 0.1,顶部一行 $y=-1$ 是 $0.1-0.1=0$。**一次更新,$w$ 的所有分量进了所有格子的公式,整片价值面一起变。**这就是 §1 说的泛化,现在有一个具体数字了。

再来一次:$s_9\xrightarrow{\mathrm{stay}}s_9$,$r=+1$。$\hat{v}(s_9)$ 现在是 0.2,target $=1+0.9\times0.2=1.18$,error $=1.18-0.2=0.98$。这次 $\phi(s_9)=[1,1,1]$,三个分量都加 $0.1\times0.98$:

$$
w \leftarrow [0.198,\ 0.098,\ 0.198].
$$

更新后:

$$
\begin{array}{ccc}
-0.098 & 0 & 0.098\\
0.1 & 0.198 & 0.296\\
0.298 & 0.396 & 0.494
\end{array}
$$

$s_9$ 从 0.2 涨到 0.494(stay 有 +1,价值往上爬);$s_8$ 从 0.2 涨到 0.396。而 $s_1$ 从 0 跌到 -0.098、$s_3$ 从 0 涨到 0.098——这两个格子**从没走过**。原因:$w_2$ 变大了,而它的特征 $x$ 在左列是负的、在右列是正的,于是左列被压下去、右列被抬上来。一次 $s_9$ 的更新,整片价值面重新倾斜,九个格子全变了。这就是泛化的另一面:不只补正,也会带偏。

通用更新式。把上面的过程写成公式,表版 TD(第 7 课 §3)把 $\hat{v}$ 换成 $\phi^T w$:

$$
w \leftarrow w + \alpha_t\big[\underbrace{r+\gamma\,\hat{v}(s',w)-\hat{v}(s,w)}_{\text{TD error}}\big]\nabla_w\hat{v}(s,w).
$$

线性情形 $\hat{v}(s,w)=\phi^T(s)w$,梯度就是 $\phi(s)$:

$$
w \leftarrow w + \alpha_t\big[r+\gamma\,\phi^T(s')w-\phi^T(s)w\big]\phi(s).
$$

这就是 TD-Linear。它和表版 TD 长得几乎一样,差别只有一个:更新对象从"状态 $s$ 的那一格"换成了"整个 $w$"——而 $w$ 出现在所有格子的公式里,所以一次更新动全身。

## 4. 例子:TD-Linear 在 3×3 上跑起来

在 3×3 上按 $\pi_0$ 采样,用 TD-Linear 分别配平面、二次两种特征训练(衰减步长,20 次平均),看 RMSE 随回合数的变化:

![TD-Linear 收敛:RMSE 停在函数类的地板](/images/fa-td-linear-converge.png)

| 回合数 | 平面 RMSE | 二次 RMSE |
|---|---|---|
| 10 | 0.274 | 0.215 |
| 100 | 0.230 | 0.097 |
| 500 | 0.226 | 0.083 |
| 1,000 | 0.224 | 0.077 |
| 5,000 | 0.224 | 0.074 |

两件事值得注意。第一,**RMSE 不归零**,而是停在 §2 算出的地板:平面 0.224,二次 0.074——恰好等于"在这个函数类里最小二乘拟合真值"的残差。第二,两个特征的地板不同:特征越多,地板越低。**TD-Linear 学到的是"当前函数类里能学到的最好",不是真值**。

## 5. TD-Linear 到底在最小化什么

§2 的最小二乘拟合要求知道真值 $v_{\pi_0}$——那是旁观者的特权。agent 没有真值,只有样本。那 TD-Linear 在无真值的情况下,收敛到了哪里?

教材的答案(§8.2.5):TD-Linear 收敛到

$$
w^* = A^{-1}b,\qquad
A=\Phi^T D(I-\gamma P_\pi)\Phi,\qquad
b=\Phi^T D r_\pi.
$$

这里 $P_\pi$、$r_\pi$ 是 Bellman 方程 $v_\pi=r_\pi+\gamma P_\pi v_\pi$ 里的转移矩阵和奖励向量,$D$ 是对角矩阵,对角元是**平稳分布** $d_\pi(s)$——agent 在策略 $\pi$ 下跑足够久,访问每个状态的比例。$w^*$ 最小化的不是"对真值的平方误差",而是**投影 Bellman 误差**。

**平稳分布先解释一下。** 目标函数要给每个状态一个权重。一种朴素做法是均匀权重(每个状态 $1/n$),但它不看真实动态——某些状态可能很少被访问。正确做法是用平稳分布 $d_\pi$ 做权重:

$$
J(w)=\sum_s d_\pi(s)\big(v_\pi(s)-\hat{v}(s,w)\big)^2.
$$

$d_\pi$ 是"长期访问比例":访问多的状态,误差权重就大。它不需要我们手动算——TD-Linear 用样本更新,样本多的状态自然影响大。

**我们这个世界的特例**:均匀随机策略下,3×3 的转移矩阵恰好双随机(每个状态进、出的概率都是 1,因为 5 个动作均匀、撞墙留在原地、边界对称),所以

$$
d_\pi(s)=\frac{1}{9}\quad\text{对每个 }s.
$$

权重均匀,目标函数退化成 §2 的"全状态平均平方误差"。

而且在这个世界里还有个更强的巧合:$w^*=A^{-1}b$ 和 §2 的最小二乘解**精确相等**(数值上差 < 1e-12,平面、二次都一样)。也就是说:这里的 TD-Linear 收敛到的,恰好是"知道真值时最小二乘拟合"的结果。§4 的地板数字之所以和 §2 吻合,正是这个原因。

**这是特例,不是一般结论。** 换一个非均匀的策略,两者就分开了。比如给"向下"的动作加偏置(概率 0.6),平稳分布立刻不匀:$s_9$ 占 0.70,$s_1$ 只有 0.0003。此时 $w_{\text{LS}}$ 和 $w^*$ 差出 0.24——TD 学的不是"对真值的最小二乘",而是投影 Bellman 误差的最小点。教材 §8.2.5 用一整节数学证明的就是这个一般情形。

## 6. 特征怎么选:维度越高,地板越低

特征 $\phi(s)$ 怎么选,是线性函数近似的核心问题。§2 已经看到两条路:

- **平面** $\phi=[1,x,y]$:3 个参数,地板 0.223。
- **二次** $\phi=[1,x,y,x^2,y^2,xy]$:6 个参数,地板 0.074。
- **one-hot** $\phi=e_s$:9 个参数,地板 0(就是查表)。

维度越高,逼近能力越强,地板越低。教材还介绍了 **Fourier 特征**:$\phi(s)$ 的每个分量取 $\cos(\pi(c_1x+c_2y))$,其中 $c_1,c_2\in\{0,1,\ldots,q\}$,一共 $(q+1)^2$ 个分量。$q=1,2,3$ 时维度分别是 4、9、16,拟合精度随维度上升——和多项式一样的规律。

为什么特征选择"非平凡"?因为好特征需要**任务先验**。你知道价值面大致像什么,才能选对特征。比如知道真值在一个平面上,用平面就够了;不知道,就只能往高维度堆(多项式、Fourier),或者干脆上神经网络——那是 §8 的事。

**一个值得记住的端点**:one-hot 特征 $e_s$ 让线性近似退化成查表,表版 TD 是 TD-Linear 的特例。这也是"表→函数"两个世界统一的接口。

## 7. 动作值:Sarsa / Q-learning 的函数近似版

状态值能近似,动作值也能:把 $\hat{v}(s,w)$ 换成 $\hat{q}(s,a,w)$,$\phi(s)$ 换成 $\phi(s,a)$。Sarsa 的函数近似版:

$$
w \leftarrow w + \alpha_t\big[r+\gamma\,\hat{q}(s',a',w)-\hat{q}(s,a,w)\big]\nabla_w\hat{q}(s,a,w).
$$

Q-learning 的函数近似版,只把 $a'$ 换成 $\max_a$:

$$
w \leftarrow w + \alpha_t\big[r+\gamma\max_a\hat{q}(s',a,w)-\hat{q}(s,a,w)\big]\nabla_w\hat{q}(s,a,w).
$$

形状和第 7 课的表版一模一样,区别仍是"更新 w、连带所有 $(s,a)$ 的价值"。策略依然用表存($\arg\max_a\hat{q}$),教材明确说这一点要到第 9 章才换成函数。

在 3×3 上跑 Sarsa 函数近似版(二次特征 × one-hot 动作,30 维参数,配 $\varepsilon$-greedy 改进),任务是找从 $s_1$ 到 $s_9$ 的最优路径:

| 回合 | 总奖励 | 长度 |
|---|---|---|
| 1 | -14 | 200(没到目标) |
| 10 | 0 | 45 |
| 50 | 0 | 5 |
| 200 | +1 | 4 |
| 1,000 | +1 | 4 |

学到后来,每回合都是 4 步最短路径($s_1\to s_4\to s_7\to s_8\to s_9$),奖励 +1。路径上的状态都学对了:它找的是"从 $s_1$ 到目标的路径",而**不是所有状态的最优策略**——$s_9$ 自己的最优动作(stay)没学到,因为回合在 $s_9$ 终止,stay 的 +1 永远不会被采样成"后续的收益"。教材在 Algorithm 8.2 的说明里原话就是这个限制:路径任务不给每个状态的最优保证。这和表版 Sarsa(第 7 课 §7)遇到的"路径外状态可能局部次优"是同一件事。

## 8. 深度 Q 学习(DQN)

Q-learning 配神经网络,就是深度 Q 学习(DQN)。数学上它还是 §7 的更新,但要处理一个新麻烦:**目标里有网络自己的输出**。

目标 $y=r+\gamma\max_a\hat{q}(s',a,w)$ 里的 $w$ 和 $\hat{q}(s,a,w)$ 里的 $w$ 是同一个。对 $w$ 求梯度时,两处都要算——直接把网络拖住,训练不稳定。教材的解法是把目标里的 $w$ 冻结住,于是:

- **目标网络**。维护两个网络:主网络(每步更新)和目标网络(参数固定,每隔 C 步从主网络复制)。目标 $y$ 用目标网络算,主网络只对 $\hat{q}(s,a,w)$ 求梯度,梯度就干净了。
- **经验回放**。目标函数 $\mathbb{E}[(y-\hat{q})^2]$ 需要给定 $(S,A)$ 的分布,最简单的假设是均匀分布;但样本是按行为策略采出来的**序列**,相邻样本强相关。把样本存进回放缓冲区,每次从里面均匀抽一个小批量——打破相关,同时一个样本能被重复用,数据效率更高。

教材的演示(Figure 8.11,5×5):行为策略均匀随机,采**一条 1000 步的 episode** 塞进缓冲区,训练后 loss 和值误差都收敛到 0,贪心策略即最优。对比第 7 课表版 Q-learning 在 3×3 上,uniform 行为策略要约 10 万步 RMSE 才归零——**两个数量级**。原因正是 §1 的两个性质:函数近似让一条样本泛化到一片状态,回放让同一条样本被反复用。

教材还做了个反向实验(Figure 8.12):样本缩到 100 步。loss 照样收敛到 0(网络把现有样本拟合得很好),但值误差不归零——样本太少,不足以把最优动作值估准。这说明深度方法"拟合样本"和"学对价值"是两回事:样本覆盖率不够时,前者好不代表后者好。

## 9. 小结

- **表 → 函数**:存 $n$ 个价值变成存 $m$ 个参数,检索从读格子变成算函数,更新从改格子变成改 $w$。
- **泛化与残差是一体两面**:改 $w$ 连带所有状态的价值(§3 里一次更新动了七个没访问过的格子),但函数类有限,逼近停在地板不归零。
- **TD-Linear**:把表版 TD 的 $\hat{v}$ 换成 $\phi^T w$,更新 $\phi(s)$ 方向上的 $w$。收敛到 $w^*=A^{-1}b$,最小化投影 Bellman 误差;RMSE 停在函数类地板(平面 0.22、二次 0.07)。
- **目标函数与平稳分布**:$J(w)=\sum_s d_\pi(s)(v_\pi-\hat{v})^2$。我们的均匀策略下 $d_\pi$ 恰好均匀,且 $w^*$ 与最小二乘解精确相等——特例;一般策略下两者不同。
- **特征**:维度越高地板越低(多项式 3→6→one-hot 9);好特征靠任务先验,或上神经网络。
- **动作值**:Sarsa / Q-learning 换成 $\hat{q}(s,a,w)$,更新形状不变;路径任务只保证路径上的最优(§7 里 $s_9$ 的 stay 没学到,教材原话的限制)。
- **DQN**:神经网络 + 目标网络(冻结目标里的 $w$)+ 经验回放(打破相关、复用样本)。一条 1000 步 episode 学出最优策略,表格版要 10 万步。`,
}
export default article
