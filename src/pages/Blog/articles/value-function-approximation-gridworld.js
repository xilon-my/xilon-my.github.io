const article = {
  slug: 'value-function-approximation-gridworld',
  date: '2026-08-20 15:06',
  name: 'Value Function Methods in a 3×3 Grid World',
  description: '第 8 课:值函数方法。任务和 MC/TD 一样——估给定策略 π0 的状态价值;差别只在表示方式:价值从"查表"换成"函数 v̂(s,w)"。把表版 TD 更新式里的"格子"换成"参数 w",就是 TD-Linear,它用 π0 的样本一步步逼近 v_π0。收敛后 w 停在函数类的地板(RMSE 不归零),在这个世界里恰好等于"已知真值时最小二乘的最优拟合"。动作值版套上同样的替换,完成"评估→改进"循环;深度 Q 学习是它加神经网络。',
  tags: ['RL'],
  category: 'Course Review',
  folder: 'rl-math',
  author: 'shannon',
  takeaway: '整篇只有一个问题:给定 π0,不逐格存价值,用一个函数 v̂(s,w)=φ(s)ᵀw 估 v_π0。表版 TD 每步更新一个格子,函数版把"格子"换成"参数 w"——更新式一模一样,这就是 TD-Linear:一步样本 (s,r,s′) 更新一次 w,连带所有状态的价值(泛化);代价是函数类有限,RMSE 停在函数类地板不归零(平面 0.223 → 二次 0.074 → one-hot 0)。TD-Linear 收敛到 w*=A⁻¹b,它不需要真值;在这个世界里 w* 恰好等于"已知真值时最小二乘的最优拟合",这是一般情况(投影 Bellman 误差)下的特例。动作值版把 v̂ 换成 q̂,配合 ε-greedy 改进就完成"评估→改进"循环;DQN 是它加神经网络、目标网络、经验回放。',
  detail: String.raw`
## 1. 这一篇要解决什么问题

前三篇(策略迭代、MC、TD)做的是同一件事:**给定一个策略 $\pi$,估出每个状态的价值 $v_\pi(s)$**。区别只在一个地方——期望怎么算:

- 有模型:代入 $P$、$R$ 直接算(策略迭代)。
- 无模型:用采样出的 episode 数据估(MC、TD)。

这一篇把问题再往前推一步:**价值用什么来表示**。

之前价值都存在一张表里:每个状态一个格子,想读就读、想改就改。这一篇换成**函数**:

$$
\hat{v}(s,w)
$$

$s$ 是状态,$w$ 是参数。给定 $w$,把状态输进去就算出价值,不再逐格存。最直观的线性形式:

$$
\hat{v}(s,w) = \phi^T(s)w
$$

$\phi(s)$ 是状态 $s$ 的特征向量,$w$ 是待定的参数。在 3×3 grid world 里,状态是二维位置,取 $x$ 列、$y$ 行(都归一化到 $\{-1,0,1\}$)当特征:

$$
\phi(s)=[1,\ x,\ y]^T,\qquad \hat{v}(s,w)=w_1+w_2x+w_3y
$$

这是一个平面:给一组 $w$,它就给出 9 个格子各自的价值。

**注意任务没变**:本篇全程估的还是 $\pi_0$(均匀随机策略)的价值 $v_{\pi_0}$,MC 和 TD 文章里都是这个 $\pi_0$。变的只是"价值存成什么样子"。

**这一章叫"值函数方法"(Value Function Methods)**,名字指的就是这件事:**值函数(v_value)不再用表,而是用一个函数来表示**。$v_\pi(s)$ 本身仍是一个"函数"(从状态到价值),之前只是用查表去存它;这一章直接用一个参数化的函数 $\hat{v}(s,w)$ 去逼近它。后面 §3 的 TD-Linear、§7 的动作值、§8 的深度 Q 学习,都是在"值函数用函数表示"这个框架下的具体算法——所以它们都叫值函数方法。

## 2. 为什么从表换成函数

**检索**:查表直接读格子;函数要算——输入状态,算 $\phi(s)$,做 $\phi^T(s)w$。这更麻烦。

**更新**:查表只改一个格子;函数必须改 $w$——而 $w$ 一变,所有状态的价值一起变。这带来两个相反的性质。

一是**存储省**。表要存 9 个数($s_1$ 到 $s_9$ 各一个),平面只需要 3 个数($w$ 的三个分量)。状态一多,省得更多:表存 $n$ 个值,函数只需 $m$ 个参数,通常 $m \ll n$。

二是**有泛化**。表更新一个格子,其他格子不动;函数改一个 $w$,所有状态的价值都跟着变。因此"访问过 $s_3$ 的经验"能顺带修正"没访问过的邻居"的价值。这是函数近似比查表强的地方。

代价:**函数类有限,逼近必然有残差**——平面装不下一个弯曲的价值面。

## 3. TD-Linear:把"格子"换成"参数"

表格形式 TD 每走一步,更新一个格子的价值(第 7 课 §3):

$$
v(s) \leftarrow v(s) + \alpha\big[r+\gamma\,v(s')-v(s)\big].
$$

函数版只改一处:**把"格子 $v(s)$"换成"参数 $w$"**。误差一样是 $r+\gamma\hat{v}(s',w)-\hat{v}(s,w)$,但更新对象从状态 $s$ 的那一格,变成整个 $w$:

$$
w \leftarrow w + \alpha\big[r+\gamma\,\hat{v}(s',w)-\hat{v}(s,w)\big]\nabla_w\hat{v}(s,w).
$$

$\nabla_w\hat{v}(s,w)$ 告诉 $w$ 该往哪个方向改,才能让 $\hat{v}(s,w)$ 更接近 target $r+\gamma\hat{v}(s',w)$。线性情形 $\hat{v}(s,w)=\phi^T(s)w$,梯度就是 $\phi(s)$,更新式变成:

$$
w \leftarrow w + \alpha\big[r+\gamma\,\phi^T(s')w-\phi^T(s)w\big]\phi(s).
$$

这就是 **TD-Linear**。它和表版 TD 长得几乎一样——差别只有一个:**更新对象从"状态 $s$ 的那一格"换成了"整个 $w$"**。这个 $w$ 出现在所有格子的公式里,所以一次更新动全身。

agent 按 $\pi_0$ 走一步,拿到样本 $(s, r, s')$,更新一次 $w$。收敛后,$\hat{v}(s,w)$ 逼近 $v_{\pi_0}(s)$——和 MC、表版 TD 估的是同一个东西,只是这次价值由函数给出。

### 3.1 例子

$w_0=0$、步长 $\alpha=0.1$、用平面特征。第一次更新来自一个样本:$s_8\xrightarrow{\mathrm{right}}s_9$,$r=+1$。

TD target $=r+\gamma\hat{v}(s_9)=1+0.9\times0=1$,误差 $=1-\hat{v}(s_8)=1-0=1$。$\phi(s_8)=[1,0,1]$,所以:

$$
w \leftarrow 0 + 0.1\times1\times[1,0,1] = [0.1,\ 0,\ 0.1].
$$

更新后 $\hat{v}(s)=0.1+0.1y$,把 9 个格子按 3×3 摆出来(和 grid world 布局一致):

$$
\begin{array}{ccc}
0 & 0 & 0\\
0.1 & 0.1 & 0.1\\
\mathbf{0.2} & \mathbf{0.2} & \mathbf{0.2}
\end{array}
$$

$s_8$ 被访问过,从 0 变 0.2,合理。但 $s_7$、$s_9$ **根本没被访问**,值也变成了 0.2;连 $s_4$、$s_5$、$s_6$ 也从 0 变成 0.1。原因就是 $w$ 进了所有格子的公式——一次更新,整片价值面一起动。这就是 §2 说的泛化。

## 4. 和 MC、表版 TD 差在哪

同一个问题(估 $\pi_0$ 的价值),三种方法的差别只在"一个样本是什么":

| | MC | 表版 TD | TD-Linear |
|---|---|---|---|
| 一个样本 | 整条回报 $G$ | 一步备份 $r+\gamma v(s')$ | 一步备份 $r+\gamma\hat{v}(s',w)$ |
| 攒一个样本要等多久 | 整条 episode(400 步) | 一步 | 一步 |
| 样本里有没有自己的估计 | 没有 | 有 | 有 |
| 更新对象 | 一个 $(s,a)$ 的格子 | 一个 $s$ 的格子 | 整个 $w$ |
| 没被访问过的状态 | 不动 | 不动 | 跟着动(泛化) |

TD-Linear 和表版 TD 唯一实质区别是**最后一行**:表版一次只改一个格子,函数版一次改一整片。这就是函数近似带来的新东西。代价(§5)也是表版没有的:**函数类有限,逼近有残差**——即使样本无限多,平面也装不下弯曲的价值面,RMSE 到不了 0。

## 5. TD-Linear 收敛到哪

在 3×3 上按 $\pi_0$ 采样训练 TD-Linear,看它收敛到什么。第一,**RMSE 不归零**,停在各自的地板:平面 0.223、二次 0.074。第二,特征不同,地板不同——特征越多,地板越低。

**TD-Linear 到底学到了什么?** 它没有真值可对,所以"学得准不准"要看它收敛到的 $\hat{v}(s,w)$ 离 $v_{\pi_0}$ 多远。答案:收敛后 $\hat{v}(s,w)$ 是"**$v_{\pi_0}$ 在函数类里的最优逼近**"。

怎么验证这个说法?需要一个参照物:**如果我们真的知道真值 $v_{\pi_0}$,那"最优逼近"是什么**。这是个最小二乘问题——选 $w$ 让平方误差之和最小:

$$
\min_w \sum_{i=1}^{9}\big(v_{\pi_0}(s_i)-\phi^T(s_i)w\big)^2,
$$

解析解 $w=(\Phi^T\Phi)^{-1}\Phi^T v_{\pi_0}$。算出平面 $w=[-2.889,-0.119,0.238]$、RMSE 0.223;二次 $w=[-2.507,-0.119,0.238,-0.365,-0.208,0.109]$、RMSE 0.074。

**关键对比**:TD-Linear 全程没用真值,只靠样本;而它收敛到的 $w$,和上面这个"知道真值时的最小二乘解"**数值上精确相等**(平面、二次都是,差 < 1e-12)。也就是说:

> TD-Linear 不需要知道 $v_{\pi_0}$,样本够多时,它自己走到了"$v_{\pi_0}$ 在函数类里的最优逼近"那里。

这就是函数近似的核心逻辑:**agent 没有真值,但有样本;TD 把"每步的备份"当目标,一步步把 $w$ 推向那个最优逼近**。

## 6. 特征决定地板

§5 的地板来自函数类有限。特征越长,函数类越大,地板越低:

- **平面** $\phi=[1,x,y]$:3 个参数,地板 0.223。
- **二次** $\phi=[1,x,y,x^2,y^2,xy]$:6 个参数,地板 0.074。
- **one-hot** $\phi=e_s$:9 个参数,地板 0(等价于查表)。

维度越高,逼近能力越强,地板越低。教材还介绍了 **Fourier 特征**($\phi(s)$ 每个分量取 $\cos(\pi(c_1x+c_2y))$,维度 $(q+1)^2$),同样的规律:维度越高越准。

为什么特征选择"非平凡"?因为好特征需要**任务先验**。你知道价值面大致像什么,才能选对特征;不知道,就只能往高维度堆(多项式、Fourier),或干脆上神经网络——那是 §8 的事。

**一个端点**:one-hot 特征 $e_s$ 让线性近似退化成查表。此时 TD-Linear 就是表版 TD——这也是"表→函数"两个世界统一的接口。

## 7. 动作值:Sarsa / Q-learning 的函数近似版

状态值能近似,动作值也能:把 $\hat{v}(s,w)$ 换成 $\hat{q}(s,a,w)$,$\phi(s)$ 换成 $\phi(s,a)$。Sarsa 的函数近似版:

$$
w \leftarrow w + \alpha\big[r+\gamma\,\hat{q}(s',a',w)-\hat{q}(s,a,w)\big]\nabla_w\hat{q}(s,a,w).
$$

Q-learning 的函数近似版,只把 $a'$ 换成 $\max_a$:

$$
w \leftarrow w + \alpha\big[r+\gamma\max_a\hat{q}(s',a,w)-\hat{q}(s,a,w)\big]\nabla_w\hat{q}(s,a,w).
$$

形状和表版一模一样,区别仍是"更新 $w$、连带所有 $(s,a)$ 的价值"。策略依然用表存($\arg\max_a\hat{q}$),教材明确说这一点要到第 9 章才换成函数。

**为什么要估动作值?** 前面几篇强调过:**决策靠 $Q$**。改进策略是 $\pi(s)=\arg\max_a q(s,a)$,而 $q$ 不能从 $v$ 白拿($V\to q$ 需要转移概率 $P$,无模型时没有)。所以要改进策略,必须直接估 $q$。

**这就闭环了**:估 $\pi_0$ 的动作价值 $\rightarrow$ $\arg\max$ 改进成 $\pi_1$ $\rightarrow$ 再估 $\pi_1$ 的价值 $\rightarrow$ 再改进……这就是"评估→改进"的循环(策略迭代的思想,MC 文章 §7 讲过)。

在 3×3 上跑 Sarsa 函数近似版(二次特征 × one-hot 动作,30 维参数,配 $\varepsilon$-greedy 改进),任务是找从 $s_1$ 到 $s_9$ 的最优路径:

| 回合 | 总奖励 | 长度 |
|---|---|---|
| 1 | -14 | 200(没到目标) |
| 10 | 0 | 45 |
| 50 | 0 | 5 |
| 200 | +1 | 4 |
| 1,000 | +1 | 4 |

学到后来,每回合都是 4 步最短路径($s_1\to s_4\to s_7\to s_8\to s_9$),奖励 +1。路径上的状态都学对了:它找的是"从 $s_1$ 到目标的路径",而**不是所有状态的最优策略**——$s_9$ 自己的最优动作(stay)没学到,因为回合在 $s_9$ 终止,stay 的 +1 永远不会被采样成"后续的收益"。教材在 Algorithm 8.2 的说明里原话就是这个限制。这和表版 Sarsa(第 7 课 §7)遇到的"路径外状态可能局部次优"是同一件事。

## 8. 深度 Q 学习(DQN)

到这一步,函数近似从"手动选特征"换成了"神经网络"。Q-learning 配神经网络就是深度 Q 学习(DQN):**神经网络本身就是那个 $\hat{q}$ 函数**——输入 (s,a),输出 q̂(s,a)。这是这一章"值函数方法"的最终形态:值函数不再查表、不再手选特征,而是由一个可学习的函数逼近器直接表示。

教材里 DQN 的网络结构:输入是归一化的行、列、动作索引(都缩到 [0,1]),经过一个隐藏层(100 个神经元、ReLU),输出 q̂(s,a):

输入(3) ──→ 隐藏层(100, ReLU) ──→ 输出(1)

行、列、动作 → q̂(s,a)

输出 q̂(s,a) 是该 (s,a) 的价值估计。贪心策略就是遍历 5 个动作,取输出最大的那个。

**数学上还是 §7 的更新**,但有一个新麻烦:**目标里有网络自己的输出**。目标 $y=r+\gamma\max_a\hat{q}(s',a,w)$ 里的 $w$ 和 $\hat{q}(s,a,w)$ 里的 $w$ 是同一个。对 $w$ 求梯度时,两处都要算——直接把网络拖住,训练不稳定。教材的解法是把目标里的 $w$ 冻结住,于是:

- **目标网络**。维护两个网络:主网络(每步更新)和目标网络(参数固定,每隔 C 步从主网络复制)。目标 $y$ 用目标网络算,主网络只对 $\hat{q}(s,a,w)$ 求梯度,梯度就干净了。
- **经验回放**。目标函数 $\mathbb{E}[(y-\hat{q})^2]$ 需要给定 $(S,A)$ 的分布,最简单的假设是均匀分布;但样本是按行为策略采出来的**序列**,相邻样本强相关。把样本存进回放缓冲区,每次从里面均匀抽一个小批量——打破相关,同时一个样本能被重复用,数据效率更高。

教材的演示(Figure 8.11,5×5):行为策略均匀随机,采**一条 1000 步的 episode** 塞进缓冲区,训练后 loss 和值误差都收敛到 0,贪心策略即最优。对比第 7 课表版 Q-learning 在 3×3 上,uniform 行为策略要约 10 万步 RMSE 才归零——**两个数量级**。原因正是 §2 的两个性质:函数近似让一条样本泛化到一片状态,回放让同一条样本被反复用。

教材还做了个反向实验(Figure 8.12):样本缩到 100 步。loss 照样收敛到 0(网络把现有样本拟合得很好),但值误差不归零——样本太少,不足以把最优动作值估准。这说明深度方法"拟合样本"和"学对价值"是两回事。

## 9. 小结

- **问题没变**:估 $\pi_0$ 的价值 $v_{\pi_0}$。变的是表示:表 $\rightarrow$ 函数 $\hat{v}(s,w)=\phi^T(s)w$。
- **为什么换函数**:存得少(表存 $n$ 个值,函数存 $m$ 个参数),能泛化(改一个 $w$ 带动所有格子);代价是函数类有限,逼近有残差。
- **TD-Linear**:把表版 TD 更新式里的"格子 $v(s)$"换成"参数 $w$",估的还是 $\pi_0$。一步样本更新一次 $w$,连带所有状态。
- **收敛**:TD-Linear 收敛到"$v_{\pi_0}$ 在函数类里的最优逼近"——和"知道真值时最小二乘的解"精确相等;RMSE 停在函数类地板(平面 0.223、二次 0.074),特征越多地板越低。
- **目标函数**:$J(w)=\sum_s d_\pi(s)(v_{\pi_0}-\hat{v})^2$,权重是平稳分布。我们这里 $d_\pi$ 恰好均匀,所以 TD 解 = 最小二乘解;一般策略下两者不同(TD 最小化的是投影 Bellman 误差)。
- **动作值**:把 $\hat{v}$ 换成 $\hat{q}$,配合 $\varepsilon$-greedy 改进,完成"评估→改进"循环。路径任务只保证路径上的最优。
- **DQN**:神经网络 + 目标网络(冻结目标里的 $w$)+ 经验回放(打破相关、复用样本)。一条 1000 步 episode 学出最优策略,表格版要 10 万步。
`,
}
export default article
