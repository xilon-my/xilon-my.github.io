const article = {
  slug: 'rag',
  date: '2026-08-05 10:00',
  name: 'RAG from Scratch: Bolt-On Memory for LLMs',
  description: 'RAG 的完整工作逻辑、分块这个最容易被低估的细节、怎么客观评估一个 RAG 好不好(RAGAS / RAGChecker / 中文 benchmark),以及从 Naive 到 Agentic RAG 的进化。',
  tags: ['RAG'],
  category: 'Project',
  author: 'Shannon',
  detail:
`这个系列前几篇在聊 Agent:怎么约束 Agent 的行为(Superpowers)、怎么用图把流程定死(LangGraph)、怎么让 Agent 用工具(MCP)。这一篇换一个层——知识层:怎么让模型知道它没学过的东西。

核心问题简言之:**LLM 很强,但它的知识被冻结在训练截止那天。** 模型不会告诉你它不知道,它会一本正经地编。RAG(Retrieval-Augmented Generation,检索增强生成)的思路很朴素:别让模型猜,让它去查。答问题之前,先到你的资料库里把相关的几段捞出来,拼进 prompt,再让它基于这些资料回答。

这篇文章从工作逻辑讲起,拆到分块这种细节,讲怎么客观评估它好不好,最后看 RAG 从 2020 到现在的进化。重点放在分块——这个最容易被跳过、却几乎决定检索质量的环节。

## 工作逻辑:先检索,再生成

RAG 的原理一句话就能说完,但拆开是两条流水线,一条离线、一条在线。

**离线那条叫入库(indexing)**,把资料变成可检索的形式,只做一次:

1. **加载**——把 PDF、Markdown、网页、数据库记录读成纯文本。
2. **分块(chunking)**——把长文本切成一段段。全流程里最容易被低估的一步,后面单独讲。
3. **嵌入(embedding)**——每段文本用一个嵌入模型算出一个向量(几百到几千维的浮点数数组)。语义相近的文本,向量在空间里就离得近。
4. **存向量库**——向量和原文一起存进向量数据库。

**在线那条叫问答(inference)**,每次提问跑一遍:

1. 把问题用**同一个**嵌入模型算成向量。
2. 在向量库里做 **top-k 检索**——找跟问题向量最像的 k 段(通常 3～10)。
3. 把这几段拼进 prompt,让 LLM"基于以下资料回答"。
4. LLM 生成答案——答案的每个事实,理论上都能回溯到检索到的段落。

RAG 原论文(Lewis et al., 2020)的架构图把这条流水线画得很清楚——上半是查询编码和索引检索,下半是生成器在检索到的文档上生成:

![RAG 原论文架构图:查询编码 → 索引检索 → 生成器在检索文档上生成答案](/images/rag-arch-8bit.png)

这个循环的关键在最后两步之间:LLM 自己不用"记住"任何资料,它只需要"读"检索结果。资料更新了,重新入库就行,模型权重不用动——这就是 RAG 对"知识过期"问题的天然解。

## 为什么是 RAG,不是微调

既然模型缺知识,为什么不直接微调?两者解决的其实是不同的问题:

| 维度 | RAG | 微调 (Fine-tuning) |
|---|---|---|
| 更新知识 | 重新入库,分钟级 | 重新训练,小时到天级 |
| 来源可查 | 是,答案能回溯到原文 | 否,知识焊进权重 |
| 幻觉 | 大幅降低(检索错了仍会) | 可能不降反升 |
| 需要的数据 | 文档本身 | 上千条高质量标注样本 |
| 训练成本 | 零,按查询计费 | GPU 小时 |
| 适合 | 知识频繁变、要引用来源、私有数据 | 输出风格/格式/领域推理 |

两者不是对手,是互补的。业界的共识是 **RAG 管知识层,微调管行为层**:微调让模型按你的格式输出,再叠一层 RAG 让事实保持新鲜、可溯源。一个容易被忽略的点:微调反而可能加剧幻觉——知识焊进了权重,没有来源可查,错了也不知道。

### 那直接联网搜索呢?现在 agent 都能 curl 了

近几年最常被问的问题:agent 都能直接上网搜了,RAG 还有必要吗?先分清一个事实——**curl 拿到的是公开世界,RAG 拿到的是你的数据**。

- **curl / 联网搜索** 搜的是公开网页:实时、面广,但内容不可控——网页上有什么就拿到什么,不相关、过时甚至错的都混进来,而且没公开的东西永远搜不到。
- **RAG** 检索的是你自己控制的语料:内部文档、代码库、合同、客户数据。**curl 一个字都搜不到私有数据,这是 RAG 无法被替代的根基。** 你决定索引什么,模型就只能读到什么;答案能回溯到你的原文,可审计。对稳定、大量的私有文档,本地毫秒级检索也比每次实时抓网页快得多、便宜得多。

所以判断标准简言之:**你的知识在哪儿**。在私有文档里且量大 → RAG;在公开网络上且要最新 → 联网搜索;两个都用 → agentic RAG 让模型按问题类型路由——向量库、web 搜索、SQL 并列着选,库里不行就转联网,这正是后面"从 Naive 到 Agentic"要讲的形态。开头那句"别让它记,让它查"还成立,只是"查"从"查自己的库"扩展成了"该查库就查库,该上网就上网"。

## 三个概念打底:嵌入、相似度、向量库

**嵌入(Embedding)** 是把文本变成向量的神经网络。它学到的是"语义坐标":说"退款"和"退货政策"的文本,即便没有共同词汇,向量也靠得近。向量的每一维都不可解释,意义活在相对位置里。中文语料里,本地开源常用 **[bge-m3](https://github.com/FlagOpen/FlagEmbedding)**(1024 维,MIT 协议),轻量演示用 [nomic-embed](https://github.com/nomic-ai/nomic-embed-text),走 API 用 OpenAI 的 text-embedding-3。嵌入模型的质量通常比分块策略影响更大——先修分块,再谈换模型。

**相似度** 默认用余弦相似度(cosine)——两个归一化向量夹角的余弦,越接近 1 越像。它只看方向、不看长度,对文本向量最合适。注意一个铁律:**入库和查询必须用同一个嵌入模型、同一个度量**。混用会让索引在数学上失效,结果全部作废。

**向量库** 干两件事:存向量,和按相似度把"最像的 k 个"快速捞出来。一条记录通常是(向量, 原文片段, 元数据)三件套——想象你有一万条 FAQ,用户来问一句,你不想把一万条都读一遍,向量库帮你把最像的那几条挑出来。度量就用上一节说的余弦:入库前先把向量做 L2 归一化,归一化之后内积就等于余弦,所以 [FAISS](https://github.com/facebookresearch/faiss) 里用 IndexFlatIP(内积索引)就行,欧氏距离看长度,对文本没意义。

但无论用什么度量,精确找最近邻都只有一个笨办法:跟库里每个向量都比一遍——每比一次就是一次距离计算,最后还要按距离排一遍序挑出最小的 k 个。n 是向量总数,十万、百万、千万,一次查询扫全库,谁都扛不住。向量库的应对是近似最近邻(ANN)索引——注意,是"近似",不保证绝对最像,只保证"够像"。拿一点点召回损失,把复杂度从 O(n) 压到近对数级。"近似"不是偷懒,是工程必然:库里上百万向量,精确 top-k 的代价不划算,宁可偶尔漏掉一个理论上最优、排序也差不多的候选,也要让每次查询都在几毫秒内回来。主流三种:

- **HNSW**:一张多层"图"。上层节点稀疏,像全国地图只标省会;下层节点稠密,像街道图。查询从顶层开始,往"更像"的节点跳,跳到差不多就下到下一层继续,逐层细化到最底层。因为每层只走很少几步就到目标附近,复杂度接近对数级,是生产里最常见的默认选择。
- **IVF(倒排文件)**:先离线把全库向量做 k-means 聚类——把相似的向量归成一堆,每堆一个代表点(质心)。查询时只找最近的几个堆,在堆内扫。堆数少就快但可能漏,是"分桶"的思路,适合超大库。
- **PQ(乘积量化)**:把每个向量压缩成一串很短的码——向量拆成几段,每段在"码本"里挑最近的码——可压到原来的 1/4～1/32。省内存省带宽,代价是精度下降。HNSW、IVF、PQ 可以叠着用,实战里 HNSW + PQ 的组合很常见。

接下来是几乎人人都踩过的坑:**[FAISS](https://github.com/facebookresearch/faiss) 是库,不是数据库**。这两个词必须分清:

- **库(library)**:你代码里调用的算法库,只管"算最近邻"这一件事。FAISS 是 Meta 开源的。向量存在**你进程的内存**里——查得快,但进程一关索引就没了;要留到下次,你得自己把索引 write_index 存成一个磁盘文件、启动时再 read_index 加载。更要命的坑是它**只存向量、不存原文**:入库时向量和"第几块原文"按位置对应,检索返回的是下标,你得拿下标回查自己存的原文(原文存在文件还是数据库里都行,FAISS 一概不管)。过滤、并发、多机分片也一样,每条都是你自己实打实的工作量。
- **数据库(database)**:一个跑着的服务,把向量 + 原文 + 元数据一起存下来,自带查询接口,还管持久化(重启不丢)、按元数据过滤(比如只搜某个租户、某个日期、有权限的)、副本和权限。[Chroma](https://github.com/chroma-core/chroma)、[Qdrant](https://github.com/qdrant/qdrant)、Pinecone 属于这类。

记住:库 = 向量在内存、原文你另存、持久化你自己管;数据库 = 向量+原文+元数据全在服务里,持久化、过滤、并发都内置。

**为什么 FAISS 只是库,数据库们却有全套功能?** 这其实不是技术差距,是**分工**。FAISS 是 Meta 2017 年开源的内部工具,它的使命是把 ANN 算法做到最快最省——HNSW 建图、乘积量化、GPU 加速,这些研究密集的东西才是它的全部价值;开源它,是为了把这些算法分享出来,不是想做一个数据库产品。而"持久化、过滤、并发、网络、复制"这些数据库功能,是另一层常规工程——不是做不到,是 FAISS 故意没做:要么你自己包(write_index 存盘、加过滤、多机分片,自由但费劲),要么用产品([Milvus](https://github.com/milvus-io/milvus) 就是拿 FAISS 当引擎、外面包上存储和分布式的典型)。简言之:FAISS 是"发动机零件",向量数据库是"整车"——选"自己搭"要连发动机带底盘都自己造,选数据库就是开箱即用。

所以直接回答那个常见疑问:**向量数据库不需要 FAISS。** Chroma、Qdrant、pgvector、Pinecone 各自内置了自己的 ANN 引擎(多数就是各自实现的 HNSW),查最近邻是它们自己的活;FAISS 只出现在"自己搭检索层"那条路里,例外只有 Milvus 这种允许你把 FAISS 当可选后端的数据库。

这个区别不是名词洁癖,有实际后果:生产里几乎都要"过滤"——先按租户/权限/日期把候选集缩到子集,再在子集里做 ANN。检索的正确性和性能都靠这一下,而库不管这个,数据库内置。所以选型大致是:

| 场景 | 选什么 |
|---|---|
| 原型快跑 | [Chroma](https://github.com/chroma-core/chroma) |
| 规模 + 过滤 | [Qdrant](https://github.com/qdrant/qdrant) |
| 已有 Postgres | [pgvector](https://github.com/pgvector/pgvector) |
| 托管省事 | Pinecone |
| 自己搭检索层 | [FAISS](https://github.com/facebookresearch/faiss) |

每一行拆开看:

- **[Chroma](https://github.com/chroma-core/chroma)**:嵌入式 Python 库,在你自己的进程里跑,向量存到本地文件,零部署,写几行代码就能跑通,原型/学习够用;但没到生产级,无权限、性能有限。
- **[Qdrant](https://github.com/qdrant/qdrant)**:Rust 写的独立向量数据库服务,自托管,过滤功能强、性能好,适合真上了规模、要按租户/标签过滤的生产。
- **[pgvector](https://github.com/pgvector/pgvector)**:你本来就有 Postgres,装个扩展、加一列向量和一个索引就行,不用新引入一套系统,适合"已经跑着 Postgres、向量量级不大"的场景。
- **Pinecone**:托管的云服务,上传就完事、不用运维,但要付费、数据在别人那里。
- **[FAISS](https://github.com/facebookresearch/faiss)**:算法库,自己搭检索层,自由最大、工作量也最大,适合团队有人力自己写服务。

关于上限:2025 年 DeepMind 的分析指出,单向量稠密嵌入有一个可证明的召回上限——512 维在约 50 万篇文档、1024 维在约 400 万篇时开始漏召回,再大就要靠混合检索(BM25 + 稠密)打破,这个后面单独讲,这里先提一句。再给一个量级参考:HNSW 在 1000 万向量上能到亚 100ms;存储上维度越高越贵,float32 下 384 维约 1.5GB/百万向量、1024 维约 4GB/百万向量——所以选型本质是在"够用"和"成本"之间权衡。

有一个被反复低估的事实:**检索质量决定 RAG 的上限。** 生成器只能回答检索器给它的东西。AWS 的人说得很直接:"A RAG is only as good as its retriever"——检索没捞到,后面再好的模型也没用。所以调 RAG 的顺序永远是:先调检索,再谈生成。

## 检索的两种方式:关键词 vs 语义

检索匹配的问题,本质就简言之:怎么判断一段资料和问题相关。这没有唯一答案,分两条路,各自解决一类问题——关键词抓"字面",语义抓"意思"。下面把两套招数、一个融合套路、一个收尾精挑,一次讲透。

**关键词(稀疏)检索**是最古老的思路:数一数问题里的词在资料里出现多少次。**BM25** 是这一路的经典算法,公式可以拆成三股力量:词在文档里出现越多越相关(词频)——比如"退货"在 A 篇出现 5 次、B 篇只出现 1 次,BM25 先认为 A 更相关;但烂大街的词("的""是""了")出现再多也没信息量,要按它在整个语料里有多常见来降权(逆文档频率);另外文档越长,同一个词出现的含金量越低,所以再按文档长度矫正一下。它只认"字面相同"——问题里出现"退货政策",只有文档里真的写着"退货政策"才命中,写"退款"它不认。这类向量天生"稀疏":维度是整个词表(可能几十万维),绝大多数位置是 0,只有命中的词位是 1 或权重。它的长处是快、可解释,对精确词(产品号、人名、专有名词)稳得一批。

**语义(稠密)检索**是另一条路:用嵌入模型把问题、文档各变成一段几百维的稠密向量,按余弦相似度找最近邻。它不认字面,认意思——问"退款",能捞到讲"退货政策"的段落,因为这两句话在语义空间里离得近。这跟前面"三个概念打底"里讲的嵌入是同一件事,只不过那次在讲"怎么表示",这里在讲"拿它来检索"。它也有盲区:如果答案依赖一个文档里出现、问题里却没出现的精确术语,稠密向量可能漏。简言之:它擅长"抓大意",不擅长"对暗号"。

| 维度 | 关键词(BM25) | 语义(稠密) |
|---|---|---|
| 依据 | 字面词频 | 语义相近 |
| 强项 | 产品号、人名、专有名词 | 同义改写、口语说法 |
| 弱项 | 换个词就漏 | 精确术语对不上 |

两条路正好互补:关键词抓"精确词",语义抓"意思"。**混合检索(hybrid)** 就是两条都跑,再把两个排名合起来。问题来了:一个词频分、一个余弦分,量纲都不同,没法直接比,怎么办?答案是 **RRF(Reciprocal Rank Fusion,倒数排名融合)**:根本不看分数,只看"排名第几",给每个候选算一个融合分 = 它在两个榜单里倒数排名的和(具体是 1/(60+名次)),再按融合分重排。名字里的"倒数排名"就是全部核心:排名越靠前,融合分越高,两个榜单都靠前的候选自然登顶。注意它赢的不是"某个榜单的第一名",而是"两个榜单都排得靠前的平均选手"——这正是比简单拼分数稳的地方。

最后是 **rerank(重排)**。检索的第一阶段讲究"宁多勿漏",召回优先,先捞 top-20～50;但捞得广,top-k 里必然混着不少不太相关的。所以第二阶段用一个更精细的打分器,把问题(query)和每个候选"拼在一起"重新打分,只留下最相关的 top-3～5。打个比方:第一阶段是人力粗筛简历,看学历关键词就放进池子;第二阶段是面试官逐个细聊,才定最终名单。为什么非要拆两段?精确打分贵,不可能对全库做,只能对第一阶段缩出来的少量候选做——这是用"两阶段"换"预算可控"。

## 分块:最容易被低估的一步

如果把 RAG 的质量问题归因,一半以上出在检索;而检索质量的一半,出在分块。分块错了,后面没有东西能补救——embedding 模型、reranker、LLM 都只能放大这个错误,不能修复。

核心是一个"金发姑娘"权衡:

- **太小**(比如不到 100 token;token 是分词后的最小单位,中文大致一字一个):每一段都是一句话的碎片。检索精确,但捞出来没有上下文——代词指代谁?实体是谁?答案散落在好几段里。
- **太大**(超过 1000 token):每一段都是一个"话题的平均值"。相似度信号被稀释,不相关的内容塞满上下文窗口。

这个权衡是可测量的,不是玄学。一篇 2025 年的多数据集研究:SQuAD 的 recall@1 在 **64 token** 时最高(64.1%),涨到 512 token 掉了 10～15 个百分点——事实型答案就藏在一句话里,块越大噪声越多。而 TechQA 反过来,从 128 token 的 16.5% 涨到 512 token 的 61.3%——它的答案是描述性的、分散在多句里。同一个设置,两个数据集,最优块大小完全相反。所以不存在通用最优解,**分块是一个针对语料调的超参数**。

### 七种分块策略

| 策略 | 怎么做 | 适合 | 坑 |
|---|---|---|---|
| 固定长度 + 重叠 | 按 N token 滑窗切,留 10～15% 重叠 | 结构均匀的文本、快速原型 | 句子从中间切断、表行切碎 |
| 递归 / 结构化 | 按段落→句子→字符逐级切;Markdown 按标题切 | 技术文档、Markdown、代码 | 超大段落仍会被硬切 |
| 句子窗口 | 只嵌入单句,检索命中后扩成前后 N 句再喂 LLM | 新闻、报告、书籍 | 单句太短,丢代词和限定 |
| 语义分块 | 先算每句的 embedding,相似度骤降处断开 | 话题频繁切换的语料 | 入库多一次推理,未必比固定长度好 |
| Late Chunking | 先对整个文档做一次长上下文嵌入,过完 transformer 再分块 | 长文档、跨段引用多 | 需要支持长上下文的嵌入模型 |
| 父子分块 | 小的子块用于检索,命中了回取大的父块喂给 LLM | 长结构化文档(合同/手册) | 上下文膨胀 30～60% |
| 上下文增强 | 入库时让 LLM 给每段写一句"这段在讲什么"再嵌入 | 元数据密集的语料(日期/版本/产品号) | 入库贵,每段一次 LLM 调用 |

### 逐个看这七种策略

**固定长度 + 重叠**:按 N token 滑窗硬切,是所有人的基线——论文报告新策略,都是拿它当"打不过的对手"。赢在结构均匀的文本和快速原型。坑也直白:句子从中间切断,表行会被切碎。

**递归/结构化**:按"段落→句子→字符"的优先级逐级切,能保段落就保段落。[LangChain](https://github.com/langchain-ai/langchain) 的 RecursiveCharacterTextSplitter 就是这个思路;Markdown 变体按标题切,把标题路径带上(比如 "## 架构 > ### 数据库选择"),正文就继承了标题里的关键词;代码变体按函数/类切,或用 tree-sitter 这种 AST。适合技术文档、Markdown、代码。坑:超大段落仍会被硬切——但它结构最多、默认最稳。

**句子窗口**:索引只嵌单个句子(检索精确),命中后把前后 N 句一起取出来喂给 LLM(上下文完整)。[LlamaIndex](https://github.com/run-llama/llama_index) 的 SentenceWindowNodeParser 默认 window_size=3,即命中句前后各 3 句、共 7 句。注意代价:窗口句不嵌入,只有命中句的向量参与检索——这是它"精确 + 完整"两头都要的代价。2025 年一个对比里它语义相似度 0.700,胜过 auto-merging(0.505)、multi-query(0.620)、HyDE(0.693)。适合新闻、报告、书籍。

**语义分块**:先把每句嵌入,算相邻句的相似度,相似度骤降的地方就是"话题换了",在此断块。[LangChain](https://github.com/langchain-ai/langchain) 的 SemanticChunker 用百分位/标准差等阈值。优点:块内部话题一致、几乎不用重叠。坑:入库多一次推理(每句都要嵌入),而且 2026 年一些评测发现它对真实数据未必赢固定长度——嵌入模型的质量,往往比分块策略更重要。

**Late Chunking**([Jina](https://github.com/jina-ai/late-chunking),2024 年 9 月):反着来——先用支持长上下文的嵌入模型把整个文档过一遍,在 transformer 输出之后、平均池化之前再分块。这样每个块的 embedding 都被整个文档"看过":"城市"这个词在块 A 里出现、但真正指代的"柏林"在前面的块里,它的向量也能带上这个关联。Jina 的例子里,查询"柏林"的相似度从 0.753 涨到 0.850。坑:要求模型支持长上下文 + 平均池化,大多数常用嵌入模型不支持——先查你的模型支不支持,再决定要不要学它。

**父子分块**:索引小块(256～384 token)做精确检索,每块在元数据里记着父块的 ID;命中子块就回取父块(一个章节或几千字)喂给 LLM——等于把"检索精度"和"生成上下文"解耦。长结构化文档(合同、手册、法规)常用。坑:喂给 LLM 的上下文膨胀 30～60%,而且改分块要整个重新入库。

**上下文增强**(Anthropic Contextual Retrieval,2024 年 9 月):入库时让 LLM 给每段写一句 50～100 token 的"上下文定位"(这段出自哪、前文发生了什么),嵌入的是"定位 + 原文",同时把定位后的文本也喂进 BM25。Anthropic 的基准:top-20 检索失败率 5.7% → 3.7%(上下文嵌入)→ 2.9%(再加上下文 BM25)→ 1.9%(再加 rerank)。坑:每段一次 LLM 调用,入库最贵(用 prompt 缓存降到约 1 美元/百万 token);而且段落本身越自包含(比如 FAQ 问答对),收益越小。

### 参数怎么定

- **块大小**:最常被引用的起点是 **512 token**(约 50 token 重叠)。按问题类型调:事实型短问答 256～512,分析型/多跳 512～1024,整段理解 1024～2048。2026 年的生产基线是 400～600 token + reranker。
- **重叠**:传统惯例 10～20%(15% 常见),但 2026 年一篇系统分析发现重叠几乎没带来可测量的收益(BERTScore 变化 ≤0.004),却让块数膨胀约 1.25 倍。建议从 0 开始,用你自己的数据验证过再加大。
- **按 token 切,别按字符切**:切分要对齐 embedding 模型的 tokenizer,保证块不超出模型的上下文窗口被静默截断。
- **代码永远别从函数中间切**:按函数/类切,或直接用 tree-sitter 之类的 AST 分块。

### 按文档类型选

长文叙事 → 句子窗口,或递归段落切分(150～400 token);代码 → 按 AST/函数切,绝不从中间切;Markdown/HTML → 按标题切,把标题路径带上;PDF → 先 OCR 再句子窗口;长结构化文档 → 父子分块 + rerank。

没有把握就从"递归分块 + 512 token"起步,在约 50 条问题的评测集上量一下 Recall@5(前 5 个结果里,该捞到的相关段落捞到多少),量出检索缺口,才值得为语义/late/上下文这些贵策略付 2～200 倍的入库成本。

## 怎么客观评价一个 RAG

搭好了,怎么知道它好不好?每个环节(分块、嵌入、检索、生成)都能调,但调完到底变好没有,靠肉眼感觉不算数——它不可复现,还会骗你:答案更流畅了,事实可能反而更远了。这个系列一贯的态度:先量出来,再说好坏。

目前最主流的评估框架是 **[RAGAS](https://github.com/explodinggradients/ragas)**(GitHub 约 15.1k stars,Apache-2.0,来自 explodinggradients 团队,EACL 2024)。它定义了 4 个指标,分两组:

| 组 | 指标 | 测什么 | 要不要标注 |
|---|---|---|---|
| 生成侧 | Faithfulness 忠实度 | 答案论断能否从上下文推出来 | 不用 |
| 生成侧 | Answer Relevancy 答案相关性 | 答的是不是被问的那个问题 | 不用 |
| 检索侧 | Context Precision 上下文精确度 | 相关片段排没排前面 | 要 |
| 检索侧 | Context Recall 上下文召回 | 上下文够不够全 | 要 |

faithfulness 的玩法:把答案拆成原子论断,逐个问"这条能否从检索到的上下文推出来",得分 = 被支持的论断占比——直接抓幻觉。answer relevancy 更绕:让 LLM 只根据答案反推 3 个"可能被问的问题",再和原问题算余弦相似度,答非所问会暴露。这两者的机制都是 LLM 当裁判(LLM-as-judge)。RAGAS 最大的卖点就是前两个不需要人工标注 ground truth(ground truth 就是人工标注的正确答案):免标注、自动化、可规模化。搭完 RAG,拿一组固定问题跑一遍记下分数;改成配置 B,同组问题再跑一遍——faithfulness 升了没,一目了然。(注:较新版本的 RAGAS 把检索侧两个指标改名成了 Context Relevance 系列,思路一致,查文档时别对不上。)

但 LLM 裁判有个绕不开的问题:**它会继承模型自己的偏差**——冗长偏差(越长的答案分越高)、对 prompt 措辞敏感、自己不确定的地方反而心虚。最尖锐的质疑来自 Amazon 的 **[RAGChecker](https://github.com/amazon-science/RAGChecker)**(NeurIPS 2024)。它做了论断级(claim-level)的元评估,发现 RAGAS 的 faithfulness 与人类整体判断的相关性很弱——Pearson 相关系数(衡量两个变量线性相关程度的统计量,越接近 1 越强正相关)只有约 7.8,而 RAGChecker 与人类判断的整体相关性能到 61.9。通俗说:一个"看起来忠实"的 RAGAS 高分,可能和真人认为的好坏对不上。

RAGChecker 的方法论不同:不按整段答案打分,而是把答案、ground truth、检索上下文全部拆成原子论断,逐条判断"这条论断能否被上下文蕴含"。由此得到一组细粒度诊断指标:overall precision/recall/F1、claim_recall(检索捞全了没)、faithfulness、context_utilization(上下文里的有用信息用了多少)、hallucination(幻觉比例)、noise sensitivity(对噪声多敏感)。用它最大的好处是能**定位问题出在检索还是生成**——这正是它和"一个笼统总分"的本质区别。RAGChecker 还在自己的 benchmark(8 套 RAG 系统、2 个检索器 × 4 个生成器、4162 个问题、10 个领域)里验证了一个和本系列前面完全一致的观点:**检索质量是最主要的因素**——dense 检索器 E5-Mistral 一致地赢过 BM25,换什么生成器都一样。

RAGChecker 的评估框架原图——模型回答 vs 标准答案的论断集合,以及每个检索块被判为相关/不相关,喂出整体、检索、生成三组指标:

![RAGChecker 评估框架:模型回答 vs 标准答案的论断集合,和检索块的相关性判定](/images/ragchecker-framework-8bit.png)

中文场景有现成的 benchmark:评估中文 RAG 常用 **[CRUD-RAG](https://github.com/IAAR-Shanghai/CRUD_RAG)**(3.6 万个中文测试样本,按 Create/Read/Update/Delete 四类任务组织)和 **[RGB](https://github.com/chen700564/RGB)**(中英双语,涵盖噪声鲁棒性、反事实鲁棒性等能力)。

最后是务实建议。没有完美的评估框架:RAGAS 便宜、免标注,适合做快速回归对比——"配置 B 比 A 好在 faithfulness 上吗",一组问题跑两遍就有数;RAGChecker 更重(每个答案要跑一次"论断抽取 + 判断"的 LLM),但能告诉你问题在检索还是生成;医疗、法律这类高风险领域,任何自动分数都要拿一小撮人工标注去校准。2026 年的共识:没有通用赢家,组合着用。最差的做法是什么都不量——"我觉得它变好了"。前面提过,检索质量决定上限。量化评估,就是先把这个上限的位置量出来,再把精力用在关键处——量完调,调完再量,直到分数不再动。这也是"别让它记,让它查"能成立的前提:查得好不好,靠分数验证,不靠感觉。

## 从 Naive 到 Agentic:RAG 的五年

RAG 这个概念 2020 年由 Lewis 等人提出(《Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks》,NeurIPS 2020)。那时候模型是 BART,检索器是 DPR(dual encoder,用嵌入做检索的早期代表),索引是 Wikipedia,分块是 100 词的定长切块。它证明了"参数化记忆(BART)+ 非参数化记忆(索引)"能配合:换掉索引、模型不用重训,知识就更新了。

之后的进化按五代看:

### Naive RAG(2020～2022)

固定的"检索-然后-读"链条。问题:分块噪声、检索不精确、"lost in the middle"(上下文中间的信息被模型忽视)。

### Advanced RAG(2023)

在固定链条前后加优化:

- **检索前**:查询改写、查询分解、HyDE(让 LLM 先写一个"假设答案",拿它去检索,弥合问句和文档的措辞差距)。
- **检索中**:混合检索——BM25 稀疏向量 + dense 向量,RRF 融合。dense 抓语义,BM25 抓精确词、产品号、专有名词。
- **检索后**:cross-encoder rerank、上下文压缩。先捞 top-20～50,重排后留 top-3～5。

### Modular RAG(2023～2024)

把 RAG 拆成可插拔模块(索引、预检索、检索、后处理、记忆、生成、编排),可以线性、条件、并行、循环地组合。查询可以路由:走 RAG、走联网搜索、还是走 SQL。

### Graph RAG(2024)

微软的 **[GraphRAG](https://github.com/microsoft/graphrag)** 用 LLM 把语料抽成知识图谱,社区检测后给每个社区写摘要,能回答"整个语料的全局性问题"——普通 RAG 做不到。代价是入库成本极高(大型语料早期要几万美元)。**[LightRAG](https://github.com/HKUDS/LightRAG)** 用廉价的实体/关系图把它拉回实用,**[LazyGraphRAG](https://github.com/microsoft/graphrag)** 把索引成本压到与普通 RAG 一个量级。

同一时期还有 **RAPTOR**——把块递归地聚成树,每一层用 LLM 把一组相似块摘要成父节点,检索时按问题需要的粒度选层——和 GraphRAG 同属"超越平铺 top-k"的路线,适合整体性、跨文档的问题。

### Agentic RAG(2024～2026,当下)

检索从"流水线的一步"变成"agent 循环里的一系列决策":

- **什么时候检索**——Self-RAG 让模型自己判断要不要查;Adaptive-RAG 按问题难度路由(简单问题直接答,不查)。
- **检索什么**——改写问题、拆成子问题做多跳检索。
- **信不信检索结果**——Corrective RAG 给检索结果打分,不行就转联网搜索或重查。
- **用哪个工具**——向量库、web 搜索、SQL、代码解释器并列,模型自己选。

工程上,这个模式收敛成"快速路由器 + 小规划器 + 有界循环"。LangGraph 这类图框架把"要不要检索""检索后要不要再检索"变成图里的条件边——和本系列 LangGraph 那篇是同一套思路。

### 绕不开的争论:长上下文是不是让 RAG 过时了

2024 年的论点是"上下文窗口都 100 万 token 了,直接全塞进去"。但实证不支持:

- **长窗口不平均**。中间位置的信息掉 30%+ 准确率(lost-in-the-middle),模型声称的窗口长度实际不可用。
- **成本是压倒性的**。RAG 每次查询通常几百 token,全量塞约 100 万 token,成本差约 **1250 倍**(未计 prompt 缓存;缓存会显著缩小这个倍数)。
- **更新跟不上**。RAG 换一批文档是分钟级,长上下文要重新喂。

到 2026 年,"长上下文取代 RAG"的论点基本被否掉,共识是混合:**用 RAG 检索,用长上下文推理**——检索捞 top 候选,长窗口负责把候选读透。

### 2026 年的现状

- **Agentic RAG 是默认形态**:检索是 agent 的一次工具调用,不是 prompt 模板。
- **混合检索是共识架构**:2026 年 Q1 混合检索采用率从 10.3% 涨到 33.3%;rerank 被视为必须——"top-20 + rerank 到 3"打败任何单一检索器的"top-3"。
- **多模态检索落地**:[ColPali](https://github.com/illuin-tech/colpali) 把 PDF 页面当图像检索,不用 OCR。
- **MCP 成为 agent 与检索后端之间的标准接口**——呼应本系列那篇 MCP 文章。
- **"RAG" 这个标签本身在被"context architecture"取代**:agent 在运行时通过工具拉数据,向量库变成"事实的单一来源"。

简言之:RAG 没死,变体在死。固定"检索-拼 prompt"的原始架构正在退场,取而代之的是"agent 决定何时、如何检索"。

## 结论

回到开头的问题:怎么让模型知道它没学过的东西?答案是**别让它记,让它查**。

RAG 的全部骨架是两条流水线:入库把资料变成可检索的向量,问答时把最相关的几段捞出来拼给 LLM。骨架很简单,难在细节——分块怎么切、检索用什么策略、要不要 rerank、要不要用 agent 决定检索时机。这些细节里,分块最容易被跳过,却几乎决定了检索质量。

对学习者来说,自己搭一遍是最快的路径:一段代码、一个本地嵌入模型、一个 FAISS 索引,数据就用你自己的文档。跑通了,你就同时理解了嵌入、相似度、向量库、分块这些词——它们不神秘,只是一条让模型"学会查资料"的流水线。

而 RAG 的进化方向也很清晰:从"不加判断的检索"到"判断该不该检索"。这跟这个系列在 agent 上看到的趋势是同一条——把确定性流程交给代码,把判断交给模型。`,
  takeaway: 'RAG 的原理是"先检索、再生成":入库把文档切成块、嵌入成向量、存进向量库;问答时把问题嵌入、捞 top-k、拼进 prompt 让 LLM 基于资料回答。骨架只有两条流水线,难在细节:分块策略直接决定检索质量,是 RAG 里最容易被低估的杠杆;检索质量决定整条链路的上限;评价分两段——检索用召回类指标,生成用 LLM 裁判(RAGAS),但裁判本身要校准,Amazon 的 RAGChecker 发现它和人类判断相关很弱;2026 年的生产基线是混合检索 + 强制 rerank + agent 决定何时检索。分块、嵌入、向量库、rerank 这些词,自己搭一遍就全懂了——一段代码加你自己的文档,就是一次完整的 RAG。',
}

export default article
