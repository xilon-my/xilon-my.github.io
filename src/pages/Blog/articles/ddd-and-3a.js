const article = {
  slug: 'ddd-and-3a',
  date: '2026-08-14 10:00',
  name: 'Domain-Driven Design: 域、子域、限界上下文,再到 3A 架构',
  description: '从 DDD 的域、子域和限界上下文,说明模块、服务与微服务之间的关系;再把这些概念映射到业务、系统、技术三层架构,并说明 Agent 如何借助 OKF 知识仓定位待修改代码。',
  tags: ['RAG'],
  category: 'Project',
  author: 'Shannon',
  takeaway: '这些概念都用于描述大型程序或组织的边界。限界上下文统一局部术语的含义,模块组织上下文内的代码,服务承担一项业务职责,微服务把一个限界上下文部署为独立程序。企业架构可以分为业务、系统、技术三层:业务架构描述业务目标,系统架构将目标映射为程序,技术架构记录部署与源码位置。代码相关知识可以用 OKF 写入独立知识仓;Agent 修改代码前,沿业务→系统→技术逐层检索并定位源码。3A 是企业架构的俗称,不是 DDD 的术语。',
  detail: `限界上下文、模块和微服务都与 **DDD(领域驱动设计,Domain-Driven Design)** 有关。DDD 认为软件复杂度主要来自业务规则,因此在编写代码前需要先定义业务边界。下面从程序的基本组成开始。

## 1. 程序是什么

程序接收输入并执行预先定义的操作。例如,手机 App 可以显示页面、接收表单、计算金额和保存用户信息。

一个 App 通常由团队长期协作完成,内部包含界面、业务计算和数据存储等不同组成部分。

规模较小的程序可能不需要明确分层;随着代码和参与者增加,同一术语在不同部分中可能表示不同含义。**大型程序需要划分职责并明确边界。**

后续概念都用于回答两个问题:**边界位于何处,边界内如何组织。** 本文先讨论单个程序的划分,再扩展到企业架构、知识存放方式以及 Agent 的代码定位过程。

## 2. 两个背景词:域、子域

先看一个具体的例子——一家公司的购物 App(后面就叫它**商城程序**)。

**域**表示软件服务的业务范围。商城程序的域包括商品展示、订单、收款、发货、退换货和客服。

**子域**把完整业务划分为职责和重要程度不同的部分。例如:

    推荐商品    ← 核心子域(形成业务差异,投入主要研发资源)
    订单       ← 核心子域(交易就发生在这)
    物流配送   ← 支撑子域(支撑核心业务,也可以采购外部能力)
    支付       ← 通用子域(可接入微信、支付宝等通用服务)

注意,子域管的是"生意怎么切",还没到程序。到了程序里,才轮到下一个词——限界上下文。

## 3. 限界上下文和模块:先划分语义边界,再组织代码

商城程序,从头到尾长这样:

    商城程序
    ├── 下单(一个限界上下文)
    │   ├── 订单/       ← 一个模块
    │   ├── 计价/       ← 一个模块
    │   └── 支付/       ← 一个模块
    └── 查物流(另一个限界上下文)
        ├── 配送/       ← 一个模块
        └── 路线/       ← 一个模块

这张图里有两层。

**第一层,限界上下文——按词义划分边界。** "订单"在下单上下文中表示商品、金额和优惠,在物流上下文中表示地址、进度和状态。含义不同的模型应当放在不同边界内,每个边界使用自己的术语与规则。这一层称为**限界上下文**。

**第二层,模块——在限界上下文内组织代码。** 一个上下文中仍可能包含大量代码,可以按订单、计价和支付等职责划分模块。模块通常对应目录或包,用于组织相关实现。

两者的顺序是:**先用限界上下文划分语义边界,再用模块组织边界内的代码。**

上图中的 计价/ 是一个模块;模块内还可以包含承担具体业务职责的服务。

## 4. 模块、服务、微服务

**服务**是在模块中承担一项业务职责的单元。例如,计价模块可以包含计算价格的服务;下单、计价和发送通知也可以分别由不同服务承担。服务不等同于单个文件,而是由职责定义的代码单元。

**微服务**描述的是部署边界。模块用于组织一个程序内部的代码,微服务则把部分能力拆为独立运行和部署的程序。

在单体程序中,多个模块由同一个进程运行和部署。拆分微服务后,部分模块进入独立程序,可以单独运行和部署。**将一个限界上下文及其内部模块部署为独立程序,是常见的微服务划分方式。** 模块边界可以保持不变,变化的是运行与部署单元。

拆分后,下单程序可以单独发布和扩容。代价是程序之间需要通过网络通信,增加超时、一致性和故障处理的复杂度。因此通常在团队需要独立发布、变更频率差异明显或流量需要单独扩展时再拆分。

简言之:**模块组织代码,服务承担职责,微服务定义独立运行和部署的边界。**

## 5. 架构:整个公司怎么组织 —— 业务、系统、技术三层

单个程序之外,企业还需要组织数十个或更多程序,这属于企业架构的范围。

这层规划叫**企业架构**。它有好几种切法:有的切成四层(业务/数据/应用/技术),有的把"数据+应用"合起来叫**系统**,于是成了**业务架构、系统架构、技术架构**——这就是常说的 **3A**。3A 是企业架构的俗称,不是 DDD 的术语。两种切法是同一套东西:四层里的"数据+应用",合起来就是"系统"。下面按业务/系统/技术三层,一层一层看,每层具体有什么。

### 业务架构:有什么

业务架构回答一个问题:**公司(或一个产品)做的是什么生意。** 它不管程序,只管生意本身。一份业务架构,具体就是回答下面这些:

- **能力地图**:公司具备哪些能力(卖货、发货、收款、促销、售后)
- **业务流程**:一件事按什么顺序办(下单 → 算价 → 付款 → 发货 → 收货;后面的例子只用到前三步)
- **业务对象**:做生意要记哪些东西(订单、客户、商品、库存)
- **业务规则**:能做什么、不能做什么(没付款不发货;折扣不能超过原价;优惠券不能和折扣叠加)
- **业务角色**:哪些角色参与流程(买家、客服、仓库)
- **统一术语**:大家说的词必须一个意思(客户 = 下单的人 + 收货地址 + 联系信息)

简言之:**业务架构是"生意的说明书",只回答"做什么",不回答"怎么做"。**

### 系统架构:有什么

明确业务架构后,下一步是把业务能力映射为程序。这层叫**系统架构**,回答如何用程序实现业务。映射关系包括:

- 一项业务能力 → 一个**限界上下文**(它可以是程序里的一块,也可以单独部署成一个程序)
- 一个业务对象 → 一个**数据实体**(程序里记的信息)
- 一条业务规则 → 一条**约束**(程序必须满足的条件)
- 一个业务流程 → 一组**接口 API**(其他系统按什么顺序调用)

因此,系统架构需要说明系统的职责范围和内部层次、限界上下文的划分与协作方式,以及每个程序提供的接口、管理的数据和必须满足的约束。

简言之:**系统架构把业务映射为程序,说明每个程序的职责、调用关系和数据。**

### 技术架构:有什么

程序设计完了,还差最后一层:**这些程序跑在哪、代码放在哪。** 这层叫**技术架构**,具体是:

- 每个程序的**代码在哪**(源码路径,比如 src/services/orders/)
- 数据存在**哪张表**(数据库表,比如订单表)
- 跑在**哪台机器**(服务器、云、容器)
- 用什么**技术栈**(什么语言、什么数据库)

简言之:**技术架构是"设备清单",回答用什么实现、跑在哪。** 一般最后写——生意没定、程序没定,写技术没有意义。

### 三层怎么映射

三层不是三份互不相干的文档,是同一件事从粗到细的三遍描述,一层对一层:

| 业务架构里 | 系统架构里 | 技术架构里 |
|---|---|---|
| 业务能力:卖货 | 限界上下文:商城程序 | 部署:商城程序怎么部署 |
| 业务对象:订单 | 数据实体:orders | 数据库表:orders 表 |
| 业务规则:先付款后发货 | 约束:程序里的校验 | 代码里的实现 |
| 业务流程:下单 → 算价 → 付款 | 接口:下单、算价、付款 | 端点:具体 URL |

方向是固定的:**战略 → 业务架构(定方向)→ 系统架构(翻译成程序)→ 技术架构(落到设备)。** 业务架构描述业务目标,系统架构把目标转换为程序结构,技术架构再将程序落实到具体设备和基础设施。

三层都在说明各自范围内的边界和组织方式:业务架构划分业务,系统架构划分程序,技术架构划分部署与基础设施。

## 6. 知识层:OKF

三层架构最终形成一组供人和 Agent 阅读的文档。Agent 修改代码前需要先读取相关架构信息,因此还需要确定这些代码知识的存放位置。

本文采用独立知识仓存放这些文档,与模块、服务和微服务的源码仓分开。原因是这些内容描述代码及其业务背景,但不属于程序实现。

这些文档可以使用 **OKF(Open Knowledge Format,开放知识格式)**:每个 Markdown 文件描述一个概念,文件头的元数据标注类型(type:流程 / 模块 / 部署 / 数据),正文通过链接表示概念关系。本文重点说明它在整体架构中的位置。

拿一家公司举例,这个知识仓的目录大概长这样:

    arch/
    ├── agent.md                     # 给 agent 的总说明,启动时注入为系统提示
    │
    ├── docs/                        # 叙事文档:给人读的背景
    │   ├── architecture/            # 顶层架构设计、决策记录
    │   └── knowledge_base/          # 术语表、行业知识
    │
    ├── spec/                        # 结构化实体:OKF 格式
    │   ├── 商城/                    # 按领域分
    │   │   ├── biz_arch/            # 业务架构:流程、规则
    │   │   ├── sys_arch/            # 系统架构:服务拓扑、接口契约、领域模型
    │   │   └── tech_arch/           # 技术架构:物理组件、DB 表、部署拓扑
    │   ├── 仓库/
    │   └── 财务/
    │
    ├── rules/                       # 结构化约束:OKF 格式
    │   ├── coding_standards/        # 编码规范
    │   ├── arch_constraints/        # 架构底线
    │   ├── transactional/           # 交易类约束
    │   ├── analytical/              # 分析类约束
    │   └── frontend/                # 前端约束
    │
    └── codespec/                    # 一次改动留下的产物
        └── changes/{改动名}/
            ├── proposal.md          # 提案:做什么、影响哪些系统
            ├── us-design.md         # 设计:改哪些文件、接口怎么变
            ├── tasks.md             # 任务列表
            └── 配置.yaml            # 这次改动的配置

目录包含三类内容:**spec/ 和 rules/ 使用 OKF 格式记录供 Agent 检索的结构化知识;docs/ 记录供人阅读的背景;codespec/ 保存每次代码修改的过程文档。** agent.md 是 Agent 启动时读取的总说明。

spec/ 按领域划分(商城、仓库、财务),每个领域再分为 biz_arch(业务架构)、sys_arch(系统架构)和 tech_arch(技术架构),分别记录流程、接口和部署。rules/ 则记录编码规范、架构约束和交易约束。

### 一个 OKF 文件的结构

spec/ 里每个概念(一个流程、一个模块、一个部署),是一个这样的文件——这样一个文件,叫一个**实体**。拿商城"算价"这条链举例子,三个文件,各属一层:

    spec/商城/biz_arch/下单流程.md
    ---
    type: process
    name: 下单流程
    domain: 商城
    steps: [下单, 算价, 付款]
    related: [计价模块]
    ---

    spec/商城/sys_arch/计价模块.md
    ---
    type: module
    name: 计价模块
    domain: 商城
    API: /api/price/calc
    data_model: [订单, 优惠券, 折扣]
    related: [下单流程, 支付模块, 计价部署]
    ---
    商城"算价"这个环节的实现,优惠券和折扣能不能叠加,就在这定。

    spec/商城/tech_arch/计价部署.md
    ---
    type: deployable
    name: 计价部署
    domain: 商城
    repo: 下单服务仓
    source_path: src/services/pricing/
    db_tables: [price_rules, orders]
    runtime: cluster-prod-1
    ---

其中,**domain: 商城** 对应第 2 节中的子域。文件先归入商城域,再按流程、模块或部署类型组织。

文件结构相同:元数据中的 type 标注类型,related 列出关联实体,其后可以补充正文。**type 用于筛选实体类型,related 用于沿关系继续检索。** 关联写在 related 字段中,程序无需解析正文即可遍历。

### 为什么用 OKF,方便定位

如果只使用普通文档,Agent 搜索"算价"时可能命中 50 个文件,需要逐个读取并推断文件之间的关系。OKF 通过结构化字段减少这部分工作。

OKF 使用 type 和 related 完成两级定位:

- **type:每个文件标"这是什么"。** 要找模块就按 type 搜 module,要找部署就搜 deployable,不用靠猜。
- **related:每个文件标"它连着谁"。** 命中一个,顺着 related 跳到邻居,再跳再跳。

顺着 related 走,正好穿过前面讲的三层:

    下单流程(业务架构)→ 计价模块(系统架构)→ 计价部署(技术架构)

计价部署文件记录源码路径 src/services/pricing/、数据库表 price_rules 和运行环境 cluster-prod-1。这三个字段构成后续代码定位所需的坐标。

为什么这么放?

- **地图要能独立更新。** 业务改了,改地图;代码没动,不用碰代码仓。
- **地图要能全局检索。** 公司有几千个微服务,agent 不可能一个个代码仓去翻。先查地图——"这个需求是哪个服务管的"——再去那个服务的代码仓找代码。
- **地图只读。** agent 改的是代码,永远不改地图;地图由专人维护,跑完自动更新。

那"模块 / 服务 / 微服务"里到底放什么?照旧只放代码,OKF 文件一个都不进代码仓。**模块管代码,知识仓管"关于代码的知识",两条线。**

简言之:**OKF 文件存放在独立知识仓中,不属于模块、服务或微服务的源码。**

## 7. 从"改哪行代码"出发:agentic RAG 怎么穿过三层

前面把架构知识放入独立知识仓,并用 type / related 标注类型和关系。下面说明 Agent 如何利用这些字段逐步定位待修改代码。

这个动作,正好是前面那篇讲的 agentic RAG:**检索是 agent 的一次工具调用,不是 prompt 模板;什么时候检索、检索什么、信不信结果、用哪个工具——四个决策,在循环里做,收敛成"快速路由器 + 小规划器 + 有界循环"。**

这里的检索分为两类:**关键词/结构化检索**按名称或字段查询,例如 grep 和数据库查询;**语义检索**把文本转换为向量并比较相似度。

- **知道要找什么之后**——OKF 文件头顶标着 type、列着 related,按 type 命中、沿 related 跳是精确查找,grep 就能做,这一小段**多少规模都不用向量**。
- **把自然语言需求映射到实体**——规模较小时,需求中的词可以直接在 OKF 中通过 grep 查找;规模增大后,"订单"等词可能出现在多个域中,需求措辞也可能与实体名称不同(例如需求写"算账",文件名为"计价"),此时可以使用向量、BM25 或混合检索处理同义表达。

当查询与代码没有字面重叠时,向量检索用于查找语义相关的实现。何时检索、检索什么、是否采用结果和选择哪个工具,与底层是否使用向量是两个不同层次的问题。

**当 agent 要改一段具体代码,它就是靠这一连串检索决策,穿过业务/系统/技术三层,最后定位到坐标的。**

先看场景。一个 RR(需求单)进来:"把'优惠券不能和折扣叠加'改成'可以叠加'。" 这句话到"改哪行代码",中间要穿过三层。

Agent 使用一组检索工具,并在循环中按需调用:

- **粗检工具**:检索 docs/ 和 spec/,获取系统背景
- **实体检索工具**:按 type 匹配 OKF 实体,再沿 related 遍历关联实体
- **读代码工具**:获得源码位置后,读取对应文件和行区间

### 先拼上下文:agent 怎么知道该搜什么

每次检索前,Agent 还需要确定查询范围。这由**上下文组装**完成:系统根据当前阶段选择部分知识,而不是把整个知识仓加入上下文。

流程按阶段配置上下文,每份上下文只包含两类信息:当前阶段需要读取的目录,以及此前产生的记录。澄清和绑定阶段的配置如下:

    澄清阶段,这一包里装:
      该看哪些目录   →  spec/商城/biz_arch/、docs/knowledge_base/
      之前发生了什么 →  RR 原文、澄清记录

    绑定阶段,这一包里装:
      该看哪些目录   →  spec/商城/sys_arch/、spec/商城/tech_arch/
      之前发生了什么 →  澄清好的需求、定下的域

同一份配方,换个阶段就换内容:目录从"生意和术语"换到"程序和设备",历史从"需求原文"换成"澄清结果"。

拼的时候做三件事:**选**(只挑这一步要的目录)、**压**(长文档留摘要)、**排**(业务先 → 系统 → 技术后)。拼完的包,agent 启动时先读一遍,它就知道:**我处理的是商城域"算价"的改动;业务在 biz_arch、系统在 sys_arch、技术在 tech_arch;前面已经澄清到这一步了。**

上下文中提供的是检索目录,不是最终答案。Agent 从 spec/商城/ 开始,按 type 匹配实体,再沿 related 查询关联项。**上下文负责限定检索范围,检索工具负责返回相关内容。** 两者分开可以减少无关上下文,并明确下一步查询对象。

**第一步,澄清:第一次检索。** 需求太含糊,"优惠券""折扣"可能指很多东西。澄清前的包里装着背景和 RR 原文——**没有背景,问不出有意义的澄清问题**。然后回问:"是商城'算价'那条规则吗?改完之后订单金额怎么算?"直到需求明确。

**第二步,绑定:第二次检索,沿三层穿透。** 需求清楚了,agent 调实体检索工具,按 type 命中、沿 related 一层一层跳:

- 查 biz_arch:促销业务中的"算价"环节 → 命中流程"下单 → 算价 → 付款"
- 沿 related 查 sys_arch:"下单程序"(前面那个拆出去的微服务)的"计价"模块 → 接口 POST /api/price/calc → 数据:订单、优惠券、折扣
- 再沿 related 查 tech_arch:源码 src/services/pricing/ → 数据库表 price_rules → 跑在 cluster-prod-1

**第三步,读取代码。** Agent 获得运行环境、服务、源码路径和数据库表等坐标后,打开文件并读取待修改行。前两次检索用于定位范围,这一步根据实际代码确定修改方式。

把循环里那四个决策套上来,认出来就是前面那篇讲的东西:

- **什么时候检索**:澄清阶段先粗检,绑定阶段精检,获得源码位置后停止。
- **检索什么**:一次只检一层,查完才知道下一层该查谁——这就是"多跳"。
- **信不信结果**:命中置信度太低,不硬猜,退回澄清再问。
- **用哪个工具**:粗检、实体检索、读代码,按阶段换。

简言之:**Agent 通过多次有明确目标的工具调用定位待修改代码。每次先组装当前阶段的上下文,再检索一层、检查结果并决定下一层,获得源码位置后停止检索并读取代码。**

## 8. 结尾

整体顺序是:**先划分业务,再映射到程序和部署;相关知识写入独立知识仓,Agent 根据结构化关系完成定位。**

- 域/子域:业务边界——区分核心能力、支撑能力和可采购的通用能力。
- OKF:关于代码的知识,放独立知识仓,不跟代码混在一起。
- agent 消费 3A:从需求出发,沿业务 → 系统 → 技术逐级下钻,定位到要改的那段代码。

另外记住:**"3A"是企业架构的俗称,不是 DDD 的术语。**`,
}

export default article
