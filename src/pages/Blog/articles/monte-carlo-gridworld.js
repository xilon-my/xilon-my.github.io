const article = {
  slug: 'monte-carlo-gridworld',
  date: '2026-08-19 15:00',
  name: 'Model-Free Monte Carlo in a Stochastic 3×3 Grid World',
  description: '第 5 课:无模型的蒙特卡洛。上一篇都要用 P、R 算期望;这篇没有模型,价值靠 episode 数据的样本平均来估。即使环境确定、策略随机,同一条 (s,a) 每次回报也不同。沿 MC Basic → 回合长度/稀疏奖励 → Exploring Starts → ε-Greedy 走一遍。',
  tags: ['RL'],
  category: 'Course Review',
  folder: 'rl-math',
  author: 'shannon',
  takeaway: '没有模型,就必须有数据:价值是回报的期望,期望用样本平均估计(大数定律)。MC Basic = 把策略迭代的评估从"解 Bellman 方程"换成"采样平均",且直接估 q(v→q 需要 P)。环境确定、策略随机(π0 均匀 1/5)时,同一条 (s,a) 每次采样回报仍不同——这正是必须采样平均的原因。MC Basic 有两个效率问题(一条 episode 只用开头一个样本 → every-visit;要收齐才更新 → 收一条就更新),代价是访问覆盖 → Exploring Starts。软策略 ε-Greedy 去掉访问覆盖条件,代价是牺牲最优性:ε 越大探索越全、价值越差(目标格价值从 10 塌到 -2.93)。',
  detail: String.raw`

## 1. 均值估计:没有模型,就从数据学期望

上一篇的策略迭代、价值迭代都是模型版:每一步的期望都是"代入 $P$、$R$ 算"出来的。这一篇没有 $P$、$R$,手里只有智能体与环境交互产生的 episode 数据——一条条 $(s,a,r,s')$ 组成的轨迹。

**没有模型,就必须有数据。** 有模型,期望可以算;没模型,期望只能从数据估。而状态值、动作值本质上都是回报的期望——所以"估计一个价值"就是"估计一个期望",先看这个最小的问题。

随机变量 $X$,要算它的均值 $\mathrm{E}[X]$,两条路:

- **模型法**:知道 $X$ 的分布 $p(x)$,直接算 $\mathrm{E}[X]=\sum_x p(x)x$。
- **无模型法**:分布未知,但手里有 $n$ 个样本 $\{x_1,\dots,x_n\}$,用样本平均逼近
  $$
  \mathrm{E}[X]\approx\bar{x}=\frac{1}{n}\sum_{j=1}^{n}x_j
  $$

样本平均为什么能逼近期望?大数定律:$n$ 越大,平均越接近真值。硬币是现成的例子:正面 $+1$、反面 $-1$,分布未知,抛 $n$ 次取平均,平均就逼近 $\mathrm{E}[X]=0$。条件:样本须**独立同分布**(i.i.d.)——否则平均可能永远偏在第一个样本附近,再多数据也没用。

## 2. 环境:无模型,策略是均匀随机的

沿用上一篇的 3×3 grid world:目标 $s_9$、禁区 $s_6$、$\gamma=0.9$、5 个动作 up/right/down/left/stay(规则详见上篇)。环境是**确定性的**:每个状态、每个动作的结果都固定,没有随机分支。

但初始策略 $\pi_0$ 是**均匀随机**:每个状态、每个动作的概率都是 $1/5$(图上每个格子标 1/5)。

![3×3 确定性 grid world,π0 均匀随机](/images/mc-3x3-map.png)

**agent 知道的只有这 5 个动作可选,不知道每个动作会通向哪个状态。** 图上画的转移(哪条边到哪个格子)是读者的上帝视角,不是 agent 手里的地图。agent 只能真的按下动作、观察结果,才知道环境怎么反应。

**即使环境确定,同一条 $(s,a)$ 每次采样得到的回报也不同**——因为策略是随机的:从 $(s_1,\text{right})$ 出发后,后面每一步的动作由 $\pi_0$ 随机选,轨迹每次都变。没有模型只能用数据,回报波动只能靠多条样本平均来抵消——这正是上一节的均值估计问题,只是"样本"变成了"回报"。

## 3. MC Basic:把策略迭代的评估换成数据

策略迭代有两步,两步的核心都是动作值 $q$:

1. **策略评估**:解 Bellman 方程得 $V_\pi$,再由 $q_\pi(s,a)=R(s,a)+\gamma\sum_{s'}P(s'|s,a)V_\pi(s')$ 转成 $q$。
2. **策略改进**:$\pi_{k+1}(s)=\arg\max_a q(s,a)$。

要把它变成无模型,只需处理第一步里 $q$ 的来历。$q$ 是期望:

$$
q_\pi(s,a)=\mathrm{E}[\,G_t \mid s_t{=}s,\,a_t{=}a\,]
$$

**期望可以用数据估**(§1):从 $(s,a)$ 出发、按 $\pi$ 采样 $n$ 条 episode,记第 $i$ 条的回报为 $g^{(i)}$,

$$
q_\pi(s,a)\approx\frac{1}{n}\sum_{i=1}^{n} g^{(i)}
$$

$n$ 越大越接近真实值。这就是 MC Basic:策略迭代的**评估**换成"从 $(s,a)$ 采样、回报平均",**改进不变**(仍是 $\arg\max_a q$)。

**为什么直接估 $q$,而不是先估 $V$ 再转 $q$?** 因为 $V\to q$ 的公式里有 $P(s'|s,a)$——没有模型就转不动。直接估 $q$ 绕开了模型:整个流程里不出现 $P$、$R$。这是 MC 与模型版的分界:模型版评估的输入是 $P$、$R$;MC 评估的输入只有 episode 数据。

这里要分清两个概率,图上格子里的 $1/5$ 和公式里的 $P(s'|s,a)$ 不是一回事:

- $1/5$ 是**策略** $\pi(a|s)$:agent 自己选动作的概率。agent 知道,因为动作是它自己选的。
- $P(s'|s,a)$ 是**环境模型**:agent 在 $s$ 做了 $a$ 之后,环境把它推到哪个 $s'$ 的概率。agent **不知道**——它只知道有哪些动作可选,不知道每个动作会通向哪个状态、给多少奖励,只能真的做一次、观察结果。

$V\to q$ 的公式里用的是后者——是环境的 $P(s'|s,a)$,不是策略的 $1/5$。$q_\pi(s,a)$ 的定义里动作 $a$ 已经给定,不需要再乘"选 $a$ 的概率"。图上画出的转移是**读者的上帝视角**(所以旁观者能解出真实值),但 agent 自己没有这张转移表。

这里要澄清奖励。MC 的 episode 里确实有奖励——每一步的 $r_t$ 都在。但那是**采样时观察到的即时奖励**,不是奖励函数 $R(s,a)$。$R$ 是"对任意 $(s,a)$ 都知道该给多少奖励"的完整表格,模型版拿它来算**还没走过**的状态;MC 没有这个表,它只在真的走过某个 $(s,a)$ 时,才收到那一步的奖励。没走过的 $(s,a)$ 会得到什么,MC 无法预测——这正是"没有模型"的含义。

## 4. 例子:策略随机,回报波动,必须采样平均

以评估 $(s_1,\text{right})$ 为例:第一步做 right 到 $s_2$;之后每一步由均匀随机的 $\pi_0$ 选动作。环境虽然是确定性的,但动作是随机选的,所以每次采样,轨迹和回报都不同。

**每条 episode 固定走 $L$ 步**(截断成定长),$\gamma=0.9$ 时 $L=400$ 足够长($\gamma^{400}\approx 3\times10^{-19}$,后面的项已经压到零,等于算到回报收敛)。**不一定走到 $s_9$,走到 $s_9$ 也不会提前结束**——agent 继续按 $\pi_0$ 随机走完剩下的步数,可能又走丢。事实上均匀随机下多数 episode 从头到尾都没到过 $s_9$,回报全是从撞墙、踩禁区攒来的负值——这正是 §5 讲的稀疏奖励问题。**平均的是"回报"这个数字,不是把步数逐帧对齐**——每条 episode 无论长到多少步,都只贡献一个回报值 $G=\sum_k\gamma^k r_{k+1}$。

实际采样 5 条回报:

$$
-2.85,\quad -2.06,\quad -2.66,\quad -1.51,\quad -2.12
$$

多数是负的——均匀随机下 agent 乱走,经常撞墙、踩禁区。单条回报不能代表 $q_{\pi_0}$,累积平均则逐渐逼近一个值:

![MC 采样平均收敛](/images/mc-sample-mean.png)

**这个值是多少,MC 自己不知道**——它只看到样本平均在收敛。我们作为旁观者,如果额外假设**模型已知**(转移概率——环境确定,每个动作的结果固定——以及奖励规则),可以解出真实值 $q_{\pi_0}(s_1,\text{right})=-2.635$。MC 全程没有用到这些:样本够多时,平均自己就逼近它了。

$s_1$ 五个动作,对比 MC 用 2000 条 episode 采样平均的估计,和旁观者用模型解出的真实值:

| 动作 | MC 采样平均(只用数据) | 真实值(模型解,需 $P$、$R$) |
|---|---|---|
| up | $-3.758$ | $-3.774$ |
| right | $-2.581$ | $-2.635$ |
| down | $-2.435$ | $-2.454$ |
| left | $-3.734$ | $-3.774$ |
| stay | $-2.817$ | $-2.774$ |

![q_π0(s1,·)](/images/mc-q-s1.png)

两列几乎一致——**MC 没有用任何 $P$、$R$ 的信息,只靠采样平均,就得到了和模型解差不多的 $q$**。$\arg\max$ 是 down($-2.454$),所以 $\pi_1(s_1)=\text{down}$。$s_3$ 同理:五个动作 $-4.221,-4.221,-3.596,-2.635,-3.221$,$\pi_1(s_3)=\text{left}$。改进和策略迭代一模一样——MC 只换了"$q$ 从哪来"(数据 vs 模型)。

## 5. 回合长度与稀疏奖励

MC 的数据是完整回合,回合长度影响回报里能不能看到目标奖励。正奖励只在到达目标 $s_9$ 之后才出现,而从各状态到 $s_9$ 需要不同的最少步数:

| 到 $s_9$ 的最少步数 | 状态 |
|---|---|
| 1 | $s_6$、$s_8$(一步进目标) |
| 2 | $s_3$($s_3$ 要穿过禁区)、$s_5$、$s_7$ |
| 3 | $s_2$、$s_4$ |
| 4 | $s_1$(最远) |

所以回合长度 $L$ 决定了"从各状态出发、$L$ 步内能否看到 $+1$":

| 回合长度 L | 谁能在 L 步内看到目标 |
|---|---|
| 1 | 只有 $s_6$、$s_8$、$s_9$ |
| 2 | 加上 $s_3$、$s_5$、$s_7$ |
| 3 | 加上 $s_2$、$s_4$ |
| ≥4 | 全部,包括最远的 $s_1$ |

回合不足 4 步时,从 $s_1$ 出发的 episode 里没有 $+1$——数据收了,但看不到目标奖励。更糟的是均匀随机策略下 agent 乱走,多数时候到不了目标,反而撞墙、踩禁区,回报为负;模拟显示从 $s_1$ 出发的期望回报随回合长度下滑:$L{=}4$ 约 $-1.16$、$L{=}6$ 约 $-1.54$——走得越长,积累的负奖励越多,回合长并不自动是好事。

"看得到目标"也不等于"估得准":即便 $L$ 足够大,均匀随机策略下 $s_1$ 的价值估计仍是负的,远低于最优策略下的 $7.29$。回合越长,估计越接近**当前策略**的真实值;策略越好,这个值才越高。

真实 RL 任务里,状态空间大、随机性强时,一条回合碰到目标的概率很低,数据几乎全是 0。常用的解法是稠密奖励:靠近目标就给小奖励,而不是只有踩中目标才 $+1$。

## 6. MC Basic 的两个效率问题 → Exploring Starts

MC Basic 的评估要为每个 $(s,a)$ 各采样多条 episode。这有两个效率问题。

**问题 1:一条 episode 只用了一个样本。** 采样到的一条 episode(示意,恰好一路走对到目标):

$$
s_1 \xrightarrow{\text{right}} s_2 \xrightarrow{\text{down}} s_5 \xrightarrow{\text{down}} s_8 \xrightarrow{\text{right}} s_9 \xrightarrow{\text{stay}} s_9 \to \cdots
$$

(均匀随机策略下,多数 episode 是在附近乱走、回报为负的那类,§4 已经看到;这条是少数恰好走到目标的。为看清结构,示意它到 $s_9$ 之后继续 stay 吃 $+1$——不是走到 $s_9$ 就结束,而是按 §4 的约定继续走。)$s_1$ 这条轨迹经过了五个状态-动作对:$(s_1,\text{right})$、$(s_2,\text{down})$、$(s_5,\text{down})$、$(s_8,\text{right})$、$(s_9,\text{stay})$。**其中每一个都产生了一次回报采样**——从 $(s_2,\text{down})$ 之后的部分就是 $(s_2,\text{down})$ 的一个样本,从 $(s_5,\text{down})$ 之后的部分就是 $(s_5,\text{down})$ 的一个样本,依此类推。但 MC Basic 只用开头那个 $(s_1,\text{right})$ 的回报,后面四个全扔了。要为全部 45 个 $(s,a)$ 各估一次,就得为每个 $(s,a)$ 各开一条 episode。

**改进 1:每次访问(every visit)。** 一条 episode 里每个被访问的 $(s,a)$ 都用上。上一条 episode 拆成五条子-episode,每条子-episode 的回报就是对应 $(s,a)$ 的一个样本:

| 子-episode(从被访问的 $(s,a)$ 出发) | 给哪个 $(s,a)$ 的样本 | 回报 |
|---|---|---|
| $s_9 \to s_9 \to \cdots$ | $(s_9,\text{stay})$ | 10 |
| $s_8 \to s_9 \to s_9 \to \cdots$ | $(s_8,\text{right})$ | 10 |
| $s_5 \to s_8 \to s_9 \to \cdots$ | $(s_5,\text{down})$ | 9 |
| $s_2 \to s_5 \to s_8 \to s_9 \to \cdots$ | $(s_2,\text{down})$ | 8.1 |
| $s_1 \to s_2 \to s_5 \to s_8 \to s_9 \to \cdots$ | $(s_1,\text{right})$ | 7.29 |

![一条 episode 拆成多条子-episode(示意:恰好走对到目标)](/images/mc-episode-decomp.png)

**一条 episode 从"一个样本"变成"五个样本"**。计算上有技巧:从后往前"回溯"算回报——最后一个 $(s,a)$ 的回报最简单,前面的等于"这一步的奖励 $+ \gamma\times$ 后面那个 $(s,a)$ 的回报",一次扫描就得到所有被访问的 $(s,a)$ 的回报。注意这些子样本是相关的(后面的轨迹是前面轨迹的后缀),好在相关性弱,平均起来问题不大。

**问题 2:必须等齐 N 条 episode 才更新。** MC Basic 评估某个 $(s,a)$,流程是:先收齐 N 条从它出发的 episode,全部平均完,得到 $q$ 的一个估计,然后才更新策略。**收齐之前,策略一次都没改过**——采 2000 条期间,策略停在原地 2000 步。

**改进 2:收一条就更新。** 每收到一条 episode 就立即用它更新估计。维护两个计数器:每访问一个 $(s,a)$,$\text{Returns}(s,a)\ += g$、$\text{Num}(s,a)\ +=\ 1$,$q(s,a)=\text{Returns}(s,a)/\text{Num}(s,a)$——随时能读出当前估计。于是第 1 条回报 $-2.85$ 就先把估计更新成 $-2.85$,第 2 条 $-2.06$ 再把它拉向 $-2.45$,每来一条,估计变一点,策略也能跟着改。单条回报不准(一条 $-2.85$,另一条 $-0.08$),但这属于广义策略迭代:价值估计粗糙也能改进策略,边采边改,估计随样本增加越来越准,策略跟着越改越好。

**代价:Exploring Starts 条件。** 前两个改进解决的是"有了数据怎么用"(拆开用、到一条就用),但没解决"**没数据怎么办**"。某个 $(s,a)$ 从来没被访问过,它就没有样本——every-visit 再高效,也只是利用这条 episode 里"访问过的" $(s,a)$;没被访问到的 $(s,a)$,$q$ 停留在初始值,策略改进时 $\arg\max$ 可能跳过它,哪怕它其实是最优动作。要估全 45 个 $(s,a)$,就得保证每个都被访问到足够多次。最直接的办法:让每个 $(s,a)$ 都能成为 episode 的起点——想估 $(s_3,\text{stay})$,就专门从 $(s_3,\text{stay})$ 开一条 episode。这就是 Exploring Starts 条件。但真实环境里,agent 通常只能从固定初始状态出发,不能凭空站到任意 $(s,a)$,这个条件难以满足。于是引入 ε-Greedy 绕开它。

## 7. ε-Greedy:软策略去掉访问覆盖条件

访问覆盖要求"每个 $(s,a)$ 都被访问"。如果策略本身是**软策略**——每个动作都有非零概率被选到——那么一条足够长的 episode 自然会把所有 $(s,a)$ 都访问一遍,条件自动满足,也就不需要刻意让每个 $(s,a)$ 当起点。

最常见的软策略是 $\varepsilon$-Greedy。设 $a^*=\arg\max_a q(s,a)$、$|\mathcal{A}(s)|$ 为该状态动作数:

$$
\pi(a|s) = \begin{cases} 1-\varepsilon+\dfrac{\varepsilon}{|\mathcal{A}(s)|}, & a = a^* \\[6pt] \dfrac{\varepsilon}{|\mathcal{A}(s)|}, & a \neq a^* \end{cases}
$$

采样时,以 $1-\varepsilon$ 概率选贪心动作,否则均匀随机选一个。以 $s_1$、$\varepsilon=0.2$、5 个动作为例,贪心动作概率 $1-0.2+0.04=0.84$,其余四个各 $0.04$。

把 MC Exploring Starts 的改进步从"选贪心"换成"选 $\varepsilon$-Greedy",得到 MC $\varepsilon$-Greedy 算法,不再需要 Exploring Starts 条件。代价是:策略被限制在 $\varepsilon$-Greedy 集合 $\Pi_\varepsilon$ 内,只能保证是该集合中的最优,不一定是全体策略中的最优;$\varepsilon$ 越大,越偏离最优,但探索越充分——这就是下一节的探索与利用权衡。

## 8. 例子:ε-Greedy 输出的是概率分布

MC $\varepsilon$-Greedy 每轮只需要一条回合,因为策略是软的。先看"软"的表现:

![ε-Greedy 是软策略](/images/mc-eps-soft.png)

左图:目标格 $s_9$,最优动作是 stay(价值 10)。$\varepsilon$-Greedy 学到的不是单个动作,而是一组概率:$\varepsilon=0$ 时 stay 概率 1;$\varepsilon=0.5$ 时 stay 概率 $0.6$,其余四个动作各 $0.1$——每步有 $40\%$ 的概率不 stay。右图:最优动作被选中的概率 $1-\varepsilon+\varepsilon/|\mathcal{A}|$ 随 $\varepsilon$ 从 1 降到均匀分布的 $0.2$。

由于每个动作概率非零,一条足够长的回合能访问所有 $(s,a)$。模拟:初始策略为均匀随机,每轮采样一条 60000 步回合,改进用 $\varepsilon$-Greedy($\varepsilon=0.5$):

| 轮次 | 一条回合访问的 $(s,a)$ | argmax 与最优策略一致 |
|---|---|---|
| 1 | 45/45 | 7/9 |
| 2 | 45/45 | 9/9 |

MC Basic 要为 45 个 $(s,a)$ 各采样;$\varepsilon$-Greedy 一条长回合就覆盖全部,两轮 argmax 学到最优。

## 9. 探索与利用的权衡

$\varepsilon$ 是探索与利用的旋钮。**探索**:策略能多访问些动作,把每个 $(s,a)$ 都估准;**利用**:策略应选当前价值最高的贪心动作。$\varepsilon$ 大 → 探索强;$\varepsilon$ 小 → 利用强。问题是:提高探索(大 $\varepsilon$)会牺牲最优性。

**最优性随 $\varepsilon$ 增大而下降。** 保持贪心最优策略的动作选择不变、只把策略换成 $\varepsilon$-Greedy,目标格 $s_9$ 的价值随 $\varepsilon$ 一路下滑:

![目标格价值随 ε 塌陷](/images/mc-eps-value-trend.png)

$$
\varepsilon=0{:}\ 10 \quad \varepsilon=0.1{:}\ 8.59 \quad \varepsilon=0.2{:}\ 7.18 \quad \varepsilon=0.5{:}\ 2.96 \quad \varepsilon=1.0{:}\ -2.93
$$

为什么塌得这么狠?$s_9$ 正上方就是禁区 $s_6$。$\varepsilon$ 大时,在 $s_9$ 有相当概率乱动:up 撞进禁区、撞墙,把 $+1$ 的收益吃掉。到 $\varepsilon=1$(完全均匀),$s_9$ 的价值 $-2.93$ **低于邻居 $s_8$ 的 $-2.37$**——待在目标格反而不如在旁边,因为随机乱走随时可能栽进禁区。

**探索能力随 $\varepsilon$ 增大而增强。** 用最优策略配不同 $\varepsilon$ 采样十万步,统计访问覆盖和踩禁区比例:

| $\varepsilon$ | 访问覆盖 | 踩禁区比例 |
|---|---|---|
| 0.05 | 24/45 | 1.0% |
| 0.1 | 30/45 | 2.0% |
| 0.2 | 36/45 | 4.0% |
| 0.5 | 45/45 | 10.2% |
| 1.0 | 45/45 | 11.1% |

$\varepsilon$ 小则覆盖不全(24/45),可能错过最优动作;$\varepsilon$ 大则覆盖全(45/45),但策略变差(踩禁区比例从 1.0% 升到 11.1%)。两张图是同一枚硬币:左图看探索能力,右图看最优性。

**实用技巧:$\varepsilon$ 衰减。** 开始用大的 $\varepsilon$ 充分探索、把全部 $(s,a)$ 都访问到,再逐步减小 $\varepsilon$ 逼近最优策略。先靠大 $\varepsilon$ 把每个 $(s,a)$ 估准,再靠小 $\varepsilon$ 把策略收紧到最优。

## 10. 小结

MC 与模型版的区别只有一个:期望从"代入 $P$、$R$ 算"变成"用数据平均"。因此:

- 策略评估从"解方程"变成"采样平均",且**直接估计 $q$**(从 $V$ 转 $q$ 需要 $P$);
- 策略改进不变:$\arg\max_a q$。

初始策略设为均匀随机(每个动作 $1/5$)。环境虽然是确定性的,但**策略随机**就让同一条 $(s,a)$ 每次回报都不同——这正是 MC 必须采样平均的原因。

三条算法是同一思路的逐步改进:

- **MC Basic** = 策略迭代把评估换成样本均值。每个 $(s,a)$ 都要采样。
- **MC Exploring Starts** = Basic + 一条 episode 估一串(每次访问)+ 收一条就更新。代价:Exploring Starts 条件。
- **MC $\varepsilon$-Greedy** = Exploring Starts + 贪心换 $\varepsilon$-Greedy。软策略自动覆盖所有 $(s,a)$,去掉条件。代价:$\varepsilon$ 牺牲最优性。

两个实践约束:样本要覆盖所有 $(s,a)$(靠软策略或 exploring starts);回合要够长,数据里才能看到目标奖励。

这个确定性世界里还有一点值得注意:**禁区 $s_6$ 的最优价值是 10**——它一步 down 就能逃进目标,而且它是最短路径的必经格(从 $s_3$ 到 $s_9$ 走 $s_3\to s_6\to s_9$ 只需 2 步)。价值顺着能到达目标的路径流过去,禁区格自己也在路径上;只有策略随机、乱走时,禁区才是负收益的来源。`,
}

export default article
