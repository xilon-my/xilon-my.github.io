const article = {
  slug: 'rag-self-train',
  date: '2026-08-06',
  name: '全参数微调检索模型的多模态 RAG',
  description: '一直用的是现成的检索模型:embedding 拿来就用,reranker 拿来就用。这次试着自己训了两个,一个检索器(bi-encoder),一个重排器(reranker),用在金融文档问答上。',
  tags: ['RAG'],
  category: 'Project',
  author: 'shannon',
  detail: `**数据**:使用 FinRAGBench-V,一个包含 855 条人工标注问题的开源金融 RAG 基准,每条问题都标有对应答案页。评测留出 99 条查询,并按文档切分,保证训练集和评测集不包含同一份文档。

这个项目以**一页**作为检索单位,每份 PDF 的一页对应一个可检索块,编号为"文档名_页码"。这样切分有两个原因。

一是避免额外调整分块参数。块太小会丢失上下文,块太大会稀释检索信号,通常需要按语料调整。金融问答的答案往往集中在某一页,例如一张表或一段财报附注,页面本身可以作为相对完整的单位。这个基准的标注(qrels)也是**页级**的,因此按页切分能让检索目标与评测标注直接对应。

二是需要说明这种切分的限制:如果答案跨页(结论在第 116 页、解释在第 117 页),页级检索可能遗漏部分内容。这个基准的答案通常集中在单页,因此这里接受这一限制。

模型方面

**bi-encoder(检索器)** 把一段文本压成一个向量(几百维数字),"相似"靠向量距离衡量:语义相近的文本,向量靠得近。它出厂时(预训练)已经会这个,但那是通用语义——"这句话和那句话像"。它没学会你的领域。微调就是拿 (问题, 答案页) 对子,用**对比损失**把它往领域里推。

具体拿一次训练的一个 batch 说:每批 32 个样本,每个样本是一条 (问题, 答案页, 一个硬负样本)。对其中任何一条问题,正样本是**它自己的答案页**;负样本来自两个地方——

1. **显式给的硬负样本**(那 1 个);
2. **同 batch 里其他 31 个问题的答案页**(自动的,叫批内负样本,不用额外准备)。

损失函数只要求一件事:**问题和自己答案页的相似度,要高过它和这 31+1 个负样本里任何一个的**。梯度沿着"答案页更近、负样本更远"的方向调权重。这个损失叫 **InfoNCE**(对比损失),目标是在多个候选中提高正确答案的相对分数。

注意一个细节:因为负样本大部分来自 batch 里的其他答案页,bi-encoder 实际见到的负样本种类比显式准备的硬负样本更多,其中偶尔会包含同一份文档的页面。这与后面 reranker 遇到的问题不同。训练后,模型学习的不是通用"语义相近",而是"金融文档中什么样的页面对应答案"。这一阶段只需几十秒,说明预训练模型已经具备通用检索能力,微调主要用于适配领域数据。

**reranker(重排器)** 不直接比较向量。它把 (问题, 页) 拼成一个序列,输出相关性分数。训练数据是标记为相关(1)或不相关(0)的 (问题, 页) 对,损失使用 **BCE(二元交叉熵)**:模型输出 0 到 1 的相关概率,训练让相关对的输出接近 1,不相关对接近 0。

检索分两段,因为"便宜"和"贵"要分开用。

第一段**粗筛**,要快、要全,目的是别漏。它跑两个东西:

- **BM25**:一个给"页面和问题多相关"打分的公式,逻辑分三步。

  第一步,把问题拆成词。中文没有空格,一般按"相邻两个字符"拆——"退款政策"拆成"退款""款政""政策"。

  第二步,问题里的每个词,对页面单独算一个贡献分,受三个因素影响:
  1. **出现次数**:这个词在这页里出现得越多,贡献越高。同样问"留抵进项税",在 5 个地方出现的页,比只出现 1 次的页更像答案。
  2. **稀有度**:但"的""是""公司"等高频词几乎每页都有,缺少区分度。因此需要按词在整个语料中的稀有程度加权,越少见的词匹配后贡献越大。
  3. **长度**:一份 100 页的年报,每个词天然比一页纸出现得多,直接比次数对长文档不公平。所以按文档长度归一化,同样的匹配在短文档里分更高。

  第三步,把问题所有词对页面的贡献加起来,就是这个页面的 BM25 分,按分排序取前 k 个。

  它只认字面:问题和页面必须出现同一个词才命中。问"退货政策"不会命中只写"退款"的页——除非靠向量检索把这两句话的语义拉近。
- **向量检索**:用 bi-encoder 把每页编码成向量并存入向量数据库,查询时再把问题编码到同一向量空间,计算问题与页面的相似度并排序。它可以让"退款"匹配到语义相近的"退货政策",但可能遗漏精确的产品编号。

精确检索需要逐页计算相似度,复杂度为 O(N),语料达到百万页时成本会明显增加。此时可以使用**近似最近邻索引**,例如 HNSW(把向量组织成多层图)或 IVF(先聚类,再搜索最近的若干簇),以少量精度损失换取速度。本项目只有约八千页,精确计算只需几毫秒,因此没有使用 FAISS 等近似索引库。这样也便于逐步实现 BM25、向量检索、融合和重排,观察完整流程。

两个排名通过 RRF 按名次融合,取 top-50 页。命中正确文档后,同一文档中主题相近的其他页面也可能进入候选集。因此这一阶段优先保证召回,不负责精确过滤。

第二段**精排**:reranker 对 top-50 逐页打分,取 top-10 给下游。这个模型贵,没法对全库跑,只能对粗筛缩出来的少数候选跑——这就是分两段的原因。

实际过程中遇到两个问题。

**第一个问题:一半页面是图片。** 语料包含几百份金融 PDF,其中约一半页面是扫描件或渲染图表,文字层为空。这使 24 条依赖图像的问题无法检索到答案页。这里使用 MinerU 对答案集合中的 130 个图片页做 OCR。

**第二个问题:第一次训练的 reranker 降低了指标。** nDCG 为 0.656,低于不使用重排时的 0.753。原因出在负样本构造上。

训练时的负样本是其他文档中的近邻页面。reranker 没有 bi-encoder 的批内负样本,只学习显式提供的 (问题, 页) 对,因此主要学会区分不同文档。推理阶段的 top-50 候选却大多来自同一份文档:第 116 页是答案,第 90 页来自同一份年报、主题相近,但内容不对应问题。由于训练数据缺少"同文档但不相关"的样本,这些错误页面可能排到答案页之前,使答案离开 top-5。

修正方法是在负样本中加入两类页面:①同文档的其他页;②由训练后的 bi-encoder 检索出的高相似度错误页。调整后,nDCG 从 0.656 提高到 0.797,召回恢复到 0.909。

最终结果,99 条真实查询。先解释三个指标:

- **召回@10**:检查正确答案页是否进入 top-10,不考虑具体名次。0.909 表示 99 次查询中有 90 次答案页进入前 10。
- **MRR@10**:看正确答案在 top-10 里排第几,取倒数(第 1 名得 1 分、第 2 名 0.5、第 3 名 0.33),再对所有问题平均。1.0 意味着每个问题的答案都排第一。
- **nDCG@10**:同样衡量答案名次,但折扣方式不同。MRR 使用 1/排名,第 2 名为 0.5;nDCG 使用 1/log₂(排名+1),第 1 名为 1、第 2 名为 0.63、第 3 名为 0.5、第 4 名为 0.43,下降更缓。这表示前几名都具有一定价值,满分为 1.0。

| 配置 | 召回@10 | nDCG@10 | MRR@10 |
|---|---|---|---|
| 现成全栈(冻结 278M reranker) | 0.909 | 0.754 | 0.702 |
| 微调 110M 检索器,不带重排 | 0.909 | 0.753 | 0.702 |
| 微调检索器 + 微调重排 | 0.909 | 0.797 | 0.761 |

三种配置的召回均为 0.909,说明差异主要来自排序指标,而不是答案页能否进入前 10。

值得注意的是前两行:微调出来的 110M 检索器,**不配 reranker**,就达到了"现成检索器 + 冻结 278M reranker"的水平。相当于微调把重排的一部分工作也做了。

同时微调两个模型后,MRR 比现成配置提高 0.059。为了判断这一差异在 99 条查询上是否稳定,我增加了配对 bootstrap 和 Wilcoxon 检验:MRR 差异显著(p=0.026),nDCG 则处于临界位置(p=0.052)。因此只能得出"MRR 显著提升",不能概括为所有指标都显著提高。

另外,这里的测评指标来源于我们的数据自带标注(qrels),可以直接计算,RAGAS/RAGChecker 那类框架评适合没有标准答案的场景。

本来还想尝试一下蒸馏:"老师学生":重排器判断准但贵,检索器便宜但判断弱。蒸馏想做的事是,让贵的重排器当老师,给每条 (问题, 答案页, 负样本) 打分,算出"答案页比负样本页好多少"(分数差);再训练便宜的检索器去模仿这个分数差——这样检索器单独就拥有了重排器的判断力,推理时连重排器都不用跑。

做法是用 Margin-MSE 损失,让检索器的"问题-答案页相似度 减去 问题-负样本相似度"逼近教师模型的分数差。训练后,指标**没有提高,反而低于纯对比微调**(0.879 vs 0.909)。可能有两个原因:一是数据只有 618 条,Margin-MSE 使检索器偏离了对比微调学到的表示,而它优化的是"复现分数差",不是直接优化检索排序;二是对比微调后的检索器已经接近重排器的水平,留给蒸馏的提升空间较小。

代码、权重、数据都开源了:github.com/xilon-my/rag-self-train`,
}

export default article
