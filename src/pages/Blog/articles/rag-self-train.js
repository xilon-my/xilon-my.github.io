const tick = String.fromCharCode(96)

const article = {
  slug: 'rag-self-train',
  date: '2026-08-06',
  name: '在 FinRAGBench-V 上微调检索器与重排器',
  description: '以一条金融文档查询为例，拆解页级语料、BM25 与稠密检索、RRF 融合、cross-encoder 重排，以及 bi-encoder 和 reranker 的全参数微调。99 条留出查询上，微调后的 110M 检索器在不使用重排器时达到冻结 278M 重排栈的排序水平；两个模型同时微调后，MRR@10 从 0.702 提高到 0.761。',
  tags: ['RAG'],
  category: 'Project',
  author: 'shannon',
  takeaway: '这组实验的主要变化发生在排序，而不是召回。文档级切分避免了同一份财报同时进入训练和评测；InfoNCE 使 110M bi-encoder 单独达到冻结重排栈的水平；reranker 能否继续改善结果，主要取决于训练负样本是否覆盖推理时的同文档近邻。当前系统仍是 OCR 后的文本检索，不是端到端视觉检索。',
  detail: String.raw`
## 1. 任务：从 8,008 页里找到一张表

先看一条实际查询：

> 根据苏州科达 2020 年度报告，计算留抵进项税在期末和期初的变化率。

答案位于年报中的某一页，页面上可能同时有表格、正文和页眉。系统要先从 8,008 个页面中找出相关页，才轮到下游模型读取数字并计算。本文只评测前半段，也就是**检索与排序**，不把生成答案的正确率混进来。

一条查询依次经过以下步骤：

1. BM25 按字符匹配取前 50 页；
2. bi-encoder 按向量相似度取前 50 页；
3. RRF 合并两份排名，再保留前 50 页；
4. cross-encoder 逐一读取“问题 + 候选页”，重新排序并输出前 10 页。

这里的 bi-encoder 是检索器，负责在整个语料中快速粗筛；cross-encoder 是 reranker，中文译作重排器，只处理粗筛后的少量候选。项目分别对这两个模型做了全参数微调。

## 2. 数据：多模态来源，文本检索实现

数据来自 [FinRAGBench-V](https://aclanthology.org/2025.emnlp-main.211/)。原基准包含 60,780 个中文页面和 855 条中文问答，问题涉及正文、表格、图表和跨页信息。项目从中选出能够匹配本地 PDF 的 193 份文档，共得到 8,008 个带文本页面和 720 条可用查询。

![FinRAGBench-V 中纯文本 RAG 与多模态 RAG 的对比示例](/images/finragbench-v-example.png)

图中是 FinRAGBench-V 论文的 Figure 1，来源为 [Zhao et al., EMNLP 2025](https://aclanthology.org/2025.emnlp-main.211.pdf)。它说明了只保留正文时可能丢失图表信息。本文实现与图中的多模态检索器并不相同：页面仍然由文本模型检索；数字生成的 PDF 用 PyMuPDF 提取文字，查询所引用但没有文字层的约 130 个页面先用 MinerU 做 OCR。页面原图只在检索后展示给下游。因此更准确的描述是“面向多模态文档的 OCR + 文本检索”，不是端到端视觉检索。

### 2.1 为什么按页切分

FinRAGBench-V 的 qrels 直接标注“哪一页与问题相关”，所以项目也把一页作为一个检索单元，编号形如 ${tick}文档名_页码.png${tick}。这样有两个好处：检索单元与标注粒度一致；表格和财报附注通常能在同一页内保留较完整的上下文。

代价也很直接。答案如果跨越两页，单页文本就不完整；一页如果同时包含多个主题，向量会混合这些内容。本文没有比较固定 token、父子分块和页级分块，因此不能把页级切分概括为普遍更优，它只是与这组标注最直接的选择。

### 2.2 文档级切分

查询不是随机逐条切分，而是先按来源文档分组，再把文档分到训练侧和评测侧。最终训练数据有 618 条查询，评测集有 99 条查询。这样同一份年报不会一部分出现在训练集、另一部分出现在评测集，减少模型记住公司名、年份和页面措辞后得到偏高结果的可能。

这 99 条是从 855 条中文问答逐步筛出来的子集，语料也只是完整中文语料的子集。文中的结果只对应这 193 份文档，不能直接当作 FinRAGBench-V 全量榜单成绩。

## 3. 粗排：BM25、稠密检索与 RRF

### 3.1 BM25 保留字面信号

中文句子没有天然空格，项目把文本切成相邻的两个字符。例如“留抵进项税”会产生“留抵”“抵进”“进项”“项税”。对问题中的一个二元字符 \(t\) 和页面 \(d\)，BM25 的贡献可以写成：

$$
\operatorname{BM25}(t,d)
=\operatorname{IDF}(t)
\frac{f(t,d)(k_1+1)}
{f(t,d)+k_1\left(1-b+b\frac{|d|}{\operatorname{avgdl}}\right)}
$$

\(f(t,d)\) 是字符 \(t\) 在页面 \(d\) 中出现的次数；\(|d|\) 是该页长度；\(\operatorname{avgdl}\) 是全部页面的平均长度；\(\operatorname{IDF}(t)\) 表示这个字符组合在整个语料中有多稀有。项目取 \(k_1=1.5\)、\(b=0.75\)。

具体到前面的查询，“留抵”“进项”“期末”这类较少见的组合会提供较强信号。BM25 也能稳定识别产品编号、年份和公司名，但无法处理没有共同字面的同义表达。

### 3.2 bi-encoder 补充语义信号

bi-encoder 分别编码问题和页面：

$$
q=E_\theta(x),\qquad p_d=E_\theta(d)
$$

\(x\) 是问题文本，\(d\) 是页面文本，\(E_\theta\) 是参数为 \(\theta\) 的 BGE 编码器。它们都被映射为 768 维向量，再用余弦相似度排序：

$$
s(x,d)=\frac{q^\top p_d}{\lVert q\rVert\lVert p_d\rVert}
$$

页面向量可以提前计算并保存，查询时只需编码一次问题。训练和推理都只在问题侧加 BGE 的检索指令“为这个句子生成表示以用于检索相关文章：”，页面侧不加，避免训练与推理格式不一致。

本项目只有 8,008 页，直接计算全部页面的相似度即可，没有使用 FAISS、HNSW 或 IVF。语料扩大到百万页后，才需要近似最近邻索引在速度和召回之间取舍。

### 3.3 RRF 只使用名次

BM25 分数和余弦相似度不在同一个尺度上，直接相加需要额外归一化。RRF（Reciprocal Rank Fusion）改为只看名次：

$$
\operatorname{RRF}(d)
=\sum_{i=1}^{m}\frac{1}{k+r_i(d)}
$$

\(m=2\) 表示两份排名，\(r_i(d)\) 是页面 \(d\) 在第 \(i\) 份排名中的位置，\(k=60\) 用于减小第一名与后续名次之间的差距。某一页同时在 BM25 和向量检索中靠前，它的融合分数就会更高。融合后取前 50 页交给重排器。

## 4. 微调 bi-encoder：让正确页在一批候选中靠前

初始模型是 110M 参数的 ${tick}BAAI/bge-base-zh-v1.5${tick}。每条训练样本包含一个问题 \(x_i\)、一个答案页 \(p_i^+\) 和一个显式负页 \(p_i^-\)。负页与问题相似，但不是标注答案。

一个 batch 有 32 条样本。对第 \(i\) 个问题，它自己的答案页是正样本；其余 31 个答案页、batch 中 32 个显式负页都作为负样本。忽略重复页面时，一个问题要在 64 个候选中选对自己的答案页，也就是面对 63 个负样本。这个数量比“31 个批内负样本加 1 个显式负样本”更多，因为其他样本携带的显式负页也会进入候选集合。

令 \(\mathcal C_i\) 表示第 \(i\) 个问题在 batch 中看到的全部候选页，温度 \(\tau\) 控制 softmax 分布的集中程度，InfoNCE 损失为：

$$
\mathcal L_{\mathrm{NCE}}
=-\frac{1}{B}\sum_{i=1}^{B}
\log\frac{\exp(s(x_i,p_i^+)/\tau)}
{\sum_{p\in\mathcal C_i}\exp(s(x_i,p)/\tau)}
$$

\(B=32\) 是 batch 大小。减小这个损失，就会提高问题与答案页的相对相似度，并压低问题与负页的相似度。训练运行 3 个 epoch，学习率为 \(10^{-5}\)，更新编码器全部参数，而不是只训练 LoRA adapter。在一张 RTX 5090 上记录的训练时间约为 29 秒；这个时间依赖硬件、软件版本和缓存状态。

批内负样本也有风险：其他问题的答案页可能实际与当前问题相关，只是 qrels 没有标出来，这会形成 false negative。当前实验没有做去重后的相关性复核，因此 InfoNCE 的监督并不等于完整事实，只等于这组标注给出的相关关系。

## 5. 微调 reranker：直接判断一个问题—页面对

重排器从 ${tick}BAAI/bge-reranker-base${tick} 初始化，约 278M 参数。它不预先保存页面向量，而是把问题和页面拼成一个序列：

$$
[\mathrm{CLS}]\;x\;[\mathrm{SEP}]\;d\;[\mathrm{SEP}]
$$

Transformer 让问题 token 与页面 token 进行完整注意力交互，最后输出一个 logit \(z\)。通过 sigmoid 得到相关概率 \(\hat y=\sigma(z)\)。相关页标为 \(y=1\)，负页标为 \(y=0\)，二元交叉熵为：

$$
\mathcal L_{\mathrm{BCE}}
=-y\log\hat y-(1-y)\log(1-\hat y)
$$

这种联合编码通常比独立向量更适合判断细粒度对应关系，但每个“问题—页面”对都要重新运行模型。8,008 页全部重排需要 8,008 次前向计算，所以这里只重排 RRF 给出的 50 个候选。

训练脚本为每个问题加入最多 8 个负页：

- 3 个同一文档中的其他页面；
- 3 个 BM25 找到的跨文档近邻；
- 2 个由微调后 bi-encoder 找到的跨文档错误页。

去重后平均每个问题有 6.7 个负页。同文档页面与答案页共享公司、年份和主题，跨文档 BM25 近邻共享字面词，bi-encoder 自挖负样本则代表当前检索器最容易混淆的页面。三类样本对应三种不同的错误来源。

脚本训练 2 个 epoch，学习率为 \(5\times10^{-6}\)，最大长度为 512。代码虽然构造了 10% 的 validation pairs，但当前 ${tick}model.fit${tick} 调用设置的是 ${tick}evaluator=None${tick}，实际没有按验证集早停或选最优 checkpoint；表中的结果来自第 2 个 epoch 结束后的模型。这个实现细节需要保留，否则会把“计划中的验证集”误写成“已经用于模型选择的验证集”。

## 6. 负样本为什么会决定重排结果

早期版本只从其他文档中选择负页。同一份年报的其他页面被排除在训练数据之外，但推理时的 top-50 往往包含同一份年报的多个页面。于是训练分布和推理分布出现差异：模型会区分“苏州科达年报”和“另一家公司年报”，却没有学会区分“苏州科达年报中的答案页”和“同一份年报中的相邻页”。

这个问题不能靠换一个 BCE 公式解决。损失函数只告诉模型如何处理已经提供的样本，不会自动补出训练数据中缺失的错误类型。第二版加入同文档页、BM25 近邻和自挖近邻后，训练样本才覆盖了推理阶段的主要混淆项。

同文档负页在当前代码中按语料顺序取最多 3 页，并没有再按相似度筛选。因此“同文档”表示它们来自更接近的分布，不表示每一页都是经过检索挖出的最难负样本。若继续改进，可以先排除跨页答案，再在同文档内按 BM25 或 bi-encoder 分数选择负页。

## 7. 评测：先定义三个数字

每条查询有一个标注答案页。设它在前 10 个结果中的名次为 \(r\)；如果没有进入前 10，记为未命中。

**Recall@10** 在这组单答案标注下等同于 Hit@10：

$$
\operatorname{Recall@10}
=\frac{1}{N}\sum_{i=1}^{N}\mathbf 1[r_i\le 10]
$$

\(N=99\) 是查询数，\(\mathbf 1[\cdot]\) 是条件成立时取 1、否则取 0 的指示函数。0.909 表示 99 条查询中有 90 条的答案页进入前 10。

**MRR@10** 更重视第一名：

$$
\operatorname{MRR@10}
=\frac{1}{N}\sum_{i=1}^{N}
\begin{cases}
1/r_i,&r_i\le10\\
0,&r_i>10
\end{cases}
$$

答案排第 1 得 1 分，第 2 得 0.5 分，第 5 得 0.2 分。

**nDCG@10** 在单答案情形下使用对数折扣：

$$
\operatorname{nDCG@10}_i
=\begin{cases}
1/\log_2(r_i+1),&r_i\le10\\
0,&r_i>10
\end{cases}
$$

答案排第 1、2、3 时分别得到 1、0.631、0.5 分。与 MRR 相比，nDCG 对第 2、3 名的扣分较缓。

## 8. 五组消融结果

五组实验使用同一批 99 条查询、同一份语料和同一段评测代码，只替换模型权重与是否启用重排：

| 配置 | Recall@10 | nDCG@10 | MRR@10 |
|---|---:|---:|---:|
| BM25 | 0.899 | 0.740 | 0.688 |
| 冻结 bi-encoder + RRF | 0.879 | 0.722 | 0.673 |
| 冻结 bi-encoder + 冻结 reranker | 0.909 | 0.754 | 0.702 |
| 微调 bi-encoder + RRF | 0.909 | 0.753 | 0.702 |
| 微调 bi-encoder + 微调 reranker | 0.909 | 0.797 | 0.761 |

可以从表中分三步读结果。

第一，冻结向量检索与 BM25 融合后，结果没有自动提高：Recall@10 从 0.899 降到 0.879。融合两个检索器只有在两者提供互补的正确信号时才有帮助，“多一路检索”本身不构成保证。

第二，微调后的 110M bi-encoder 不使用 reranker，nDCG@10 为 0.753、MRR@10 为 0.702，与“冻结 bi-encoder + 冻结 278M reranker”的 0.754 和 0.702 基本相同。配对检验中，nDCG 差异的 \(p=0.990\)，没有证据说明两者不同。这里可以得出的结论是：在这组数据上，领域微调让轻量检索器达到冻结重排栈的排序水平。

第三，两个模型同时微调后，Recall@10 仍为 0.909，但 nDCG@10 提高到 0.797，MRR@10 提高到 0.761。答案页进入前 10 的数量没有变化，变化发生在前 10 内部的名次。

## 9. 99 条查询上的统计边界

平均指标有提高，不代表换一批查询后仍会得到同样差值。项目对每条查询保留成对结果，再做 10,000 次配对 bootstrap，并用双侧 Wilcoxon signed-rank test 检查“微调双模型”与“冻结全栈”的差异。

| 指标 | 平均差值 | 95% bootstrap CI | Wilcoxon \(p\) | 解释 |
|---|---:|---:|---:|---|
| MRR@10 | +0.059 | [0.005, 0.112] | 0.026 | 区间不含 0，达到 0.05 阈值 |
| nDCG@10 | +0.044 | [-0.004, 0.089] | 0.052 | 区间包含 0，未达到 0.05 阈值 |

因此可以说 MRR@10 在这 99 条查询上显著提高；不能把同一句话扩展到 nDCG@10。\(p=0.052\) 与 0.05 很接近，但预设阈值之后仍应按未达到阈值报告。样本规模较小，置信区间也提示真实差值仍有较大不确定性。

## 10. 一次没有带来改善的蒸馏

项目还测试了 cross-encoder 到 bi-encoder 的蒸馏。教师 reranker 分别给答案页和负页打分，形成教师间隔：

$$
\Delta_t=z_t(x,p^+)-z_t(x,p^-)
$$

学生 bi-encoder 用两个余弦相似度形成学生间隔：

$$
\Delta_s=s_s(x,p^+)-s_s(x,p^-)
$$

再最小化 Margin-MSE：

$$
\mathcal L_{\mathrm{distill}}=(\Delta_s-\Delta_t)^2
$$

学生从已经做过 InfoNCE 微调的 checkpoint 继续训练。结果 Recall@10 从 0.909 降到 0.879，没有进入最终五行主表。这说明在 618 条查询的规模下，复现教师分数间隔没有比直接对比学习更适合当前检索目标。可能原因包括教师分数尺度与余弦间隔不匹配、样本量不足，以及蒸馏更新使原有向量空间偏离 InfoNCE 得到的表示。现有实验没有逐项消融这些原因，所以它们只能作为后续假设。

## 11. 结论与复现范围

这组实验支持三个具体结论：

1. 页级 qrels 配合文档级切分，可以构造泄漏较少的检索训练与评测；
2. 618 条领域查询已经足以改变 bi-encoder 的排序表现，使它在这组数据上达到冻结重排栈的水平；
3. reranker 的改进依赖负样本是否覆盖推理候选，特别是同文档但不相关的页面。

同时保留四个边界：语料只是 FinRAGBench-V 中文部分的子集；图片页依赖 OCR，检索器没有直接编码视觉内容；90/99 的 Recall@10 在三组主要配置中没有继续提高；reranker 的 validation split 尚未真正用于早停或模型选择。

代码、训练脚本、结果表和配对统计保存在 [xilon-my/rag-self-train](https://github.com/xilon-my/rag-self-train)。复现顺序是：

${tick}${tick}${tick}bash
# 构造第二版负样本
python scripts/05b_build_train_data_v2.py

# 训练 bi-encoder 与 reranker
python scripts/06_train_biencoder.py
python scripts/07b_train_reranker_v2.py

# 五组消融与配对统计
python scripts/08_full_ablation.py
python scripts/09_stats.py
${tick}${tick}${tick}

发布包中的数据和 checkpoint 需要先按仓库 README 放到 ${tick}data/${tick} 与 ${tick}checkpoints/${tick}。实验记录对应一张 RTX 5090；换用其他 GPU 时，结果指标应由相同 seed 和评测脚本重新计算，训练时间不能直接沿用。
`,
}

export default article
