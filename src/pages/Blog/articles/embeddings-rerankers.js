const article = {
  slug: 'embeddings-rerankers',
  date: '2026-08-06 10:00',
  name: 'Embeddings and Rerankers from Scratch: When and How to Judge Relevance',
  description: '嵌入模型(embedding)和重排模型(reranker)的深度拆解:为什么一个把判断提前到入库、一个留到查询时,它们分别怎么被训出来,近三年又怎么被蒸馏成同一份判断力。',
  tags: ['RAG'],
  category: 'Project',
  author: 'Shannon',
  detail:
`先看一次真实的翻车。你问检索器"这笔订单的退款政策是什么",embedding 检索器从库里捞回 top-3:一条讲退货流程的,一条讲运费险的,一条讲优惠券的,没一条直接回答。答案其实就在库里,只是它的每个词都和问题对不上——embedding 把整段压成一颗向量,精确词被稀释成了泛语义。同样的候选集换给 reranker,它把问题和每段文档逐字对看一遍,答案跳到了第一名。

前一篇《RAG from Scratch》说过,检索质量是 RAG 的天花板。而这块天花板由两个模型钉死:embedding(双塔 bi-encoder)在入库时把全库文档压成一颗向量,reranker(单塔 cross-encoder)在查询时让查询和文档逐 token 互看再打分。它们回答同一个问题——"这段文档跟你的问题相不相关"——判断方式却南辕北辙:一个在入库时就把判断做完了,一个把判断留到查询时。

这篇文章不讲怎么搭 RAG 流水线(那在前一篇),钻到流水线的两个引擎内部:它们为什么长成"双塔"和"单塔"、怎么被训出来、为什么这两年越变越大。最后你会发现,这两个模型不是对手——它们是同一份判断力的老师和学生。

## 同一个问题,两种判断方式:双塔与单塔

判断"相不相关"这件事,可以发生在两个时刻:入库时(先把所有文档变成可比较的形式),或者查询时(拿到问题再逐个细看)。embedding 选了前者,reranker 选了后者。

要把这两个模型讲明白,得先分清两对概念:一对是"入库/查询"——什么时候判断;另一对是"训练时/推理时"——同一个模型,训练时是一种结构、推理时是另一种用法。下面每个模型都按训练、推理两面拆开讲。

### 从 transformer 出发:两个模型都是它加一小层

假设你只知道 transformer,我们就从它出发。一个 transformer 模型做一件事:输入一串 token(词的编号),经过多层注意力,输出一串向量——每个 token 对应一个"上下文向量",编码的是"这个 token 在这个句子里、结合上下文是什么意思"。下文的一切都从这两句长出来。

先排掉一个词义坑:transformer 输入层那张把词变成向量的表叫 token embedding,和本文说的"嵌入模型"不是一回事——本文的 embedding 模型,是把**整句话**变成一个向量。

把 transformer 的输出(一串 token 向量)变成"一个向量"或"一个分数",各需要多加一小层,这正是两个模型的分界:

- **embedding 模型 = transformer + 池化层**。池化把"一串 token 向量"并成"一个向量"。一句话进来:先过 transformer,得到每个 token 的上下文向量;再池化成一个向量——这就是"这句话的嵌入"。
- **reranker = transformer + 打分头**。把 query 和 passage 拼进**同一个序列**,过 transformer,再拿整串的某种表示过一个打分头,输出一个相关性分数。

两者真正的区别一句话能说完:**embedding 把两个文本分开编码、最后比向量;reranker 把两个文本拼在一起编码、直接打分。** 这个区别决定了两者在训练和推理上各自长什么样。先看双塔。

### 双塔:训练时成对喂,推理时一次一个

"双塔"这个名字,只在训练时是字面成立的。**训练时**要喂的是成对数据:(query, 正文档)该靠近,(query, 负文档)该推远。这一对文本同时过共享编码器,各出一个向量,再用一个对比损失调整权重——损失的目标就是上面那句话:正对近、负对远,把"什么算相似"写进权重。这是"两句话同时输入"唯一真实的时刻,也是"双塔"名字的来源——两座塔同时在工作,中间共用一套权重。

**推理时**完全是另一回事,这才是你实际用它的样子:**永远一次只喂一个文本**。

1. 入库:每个文档**单独**过一遍编码器,得到一个向量,存进向量库。文档之间互不相干,一个接一个来。
2. 查询:查询**单独**过一遍编码器,得到一个向量。
3. 比较:不在模型里。FAISS 拿查询向量对着一批**早就存好的**文档向量做内积,挑出最像的 k 个。

那为什么还叫"双塔"?因为检索天然涉及两个文本(查询、文档),它们必须被**同一个**编码器编码,才落在同一个向量空间、才比得了。图上的两座塔其实是同一个 transformer 画了两遍——左边编码查询、右边编码文档,权重是同一套。推理时你每次只跑这一个模型:入库时跑一遍每个文档,查询时跑一遍查询。不是"从两个 BERT 里挑一个",而是**本来就只有一个模型**。这也划清了两者的分工:模型的职责是决定"什么算相似"(训练时把语义写进权重),FAISS 的职责是在画好的空间里机械地找最近的 k 个——它只做算术,不懂语义。

![SBERT 孪生双塔架构](/images/sbert-arch-8bit.png)
*图:SBERT 用共享权重的两个 BERT 把句子各自编码成向量(arXiv:1908.10084,Figure 1)*

为什么非要"分开编码、最后比",而不是"拼一起一次算完"?因为这换来一个关键能力:**预计算**。2019 年之前,"两句话相不相似"用的是原生 BERT——把两句话拼成一个序列丢进去。它很准,但贵到离谱:在一万句话里找最相似的一对,要跑约 5000 万次两两拼接的推理,约 65 小时。SBERT(Sentence-BERT,2019,arXiv:1908.10084)用共享权重的双塔把它拆开:文档各自提前编码成向量存好(离线一遍,一万句约 5 秒),查询时只编查询、和存好的向量比(0.01 秒)。一句话:双塔换来文档可离线预计算、查询毫秒级——这正是 RAG 里"入库"这一步的由来。

池化是"把一串 token 向量并成一个向量"的动作,三种做法**任选其一**,选哪种不是随便的:
- **mean pooling**:把一句话 20 个 token 过 transformer 得到的 20 个 768 维上下文向量,按维度取平均,并成一个 768 维的句子向量;
- **取 [CLS]**:不平均,直接拿第 1 个位置那个 token 的向量([CLS] 是 BERT 在句首放的一个分类符,attention 让它看过整句);
- **取最后一个 token**:拿最后一个位置的向量,LLM 底座的惯例。

无论选哪种,结果都是"一串向量 → 一颗向量",这一颗就是"这句话的嵌入"。池化、损失、数据一起决定向量质量。

池化的选择跟着骨干走,而骨干这两年换血了。**2019～2023 年的主流是 BERT 类双向编码器**(SBERT、E5、bge、SimCSE 都是),池化走 [CLS] 或 mean;**2024 年起,E5-Mistral、GTE-Qwen2、Qwen3-Embedding 改用解码器 LLM**(Mistral、Qwen2/3)当骨干,池化改走最后一个 token。为什么要换?解码器 LLM 预训练规模大得多、会跟指令、支持 32K 长上下文,拿来当特征提取的起点就比 BERT 高一截。但解码器和编码器天生不同,两个适配坑必须知道:

- **因果 mask**:解码器里每个 token 只能看左边,整个序列只有最后一个 token 才"全看过"。所以解码器底座的嵌入取**最后一个 token(即 [EOS])**当整句表示——[CLS] 是编码器的概念,解码器没有。也有模型干脆把解码器改成双向注意力、整段编码后再加一个 latent pooling([NV-Embed](https://github.com/NVIDIA/NV-Embed) 就是这么干的)。
- **EOS-last 的 recency bias**:取最后一个 token,表示容易被靠近结尾的词主导(因果注意力下越靠后的词看到的越多)。工程上还有一坑:批内句子长度不一、要 left-padding,"取最后一个"必须按 attention mask 找真实位置,不能无脑取 [:, -1]。

注意 bge-m3 这类 2024 年仍用编码器的模型不踩这两个坑——坑只属于解码器骨干。

向量维度不是超参,是骨干模型的隐藏层宽度:BERT-base 是 768、bge-m3 是 1024、GTE-Qwen2-7B 是 3584、Qwen3-8B 是 4096。维度是"够用 vs 烧钱"的权衡——前一篇讲过,1024 维 float32 约 4GB/百万向量。MRL 在这里先埋一句:同样是改损失不改架构,可以逼着模型把判别信息挤进前几维,让你按需裁剪向量——细节留到训练那一节。

### 单塔:训练时学排序,推理时拼起来现算

"单塔"(cross-encoder)的名字来自结构:query 和 passage 拼成**同一个序列**([CLS] query [SEP] passage [SEP],[SEP] 是 BERT 用来隔开两段文本的分隔符),过同一个 transformer——两个文本并成了一个输入,所以是"一座塔"。这带来和双塔截然不同的一点:注意力跨两段文本互通,query 的每个 token 可以直接 attend 到 passage 的每个 token,"逐 token 互看"是字面意思,信息不需要先各自压缩。

**训练时**学的是"排序",不是向量。数据是:一个 query 配一组文档,加它们的相关标签或顺序。三种喂法:
- pointwise:每篇文档单独标"相关/不相关",当二分类训;
- pairwise:给一对(正, 负),学"A 比 B 相关";
- listwise:给一整组和它们的顺序,学"该排前面的排前面"。
三种的共同目标,都是让模型打出的分数顺序符合真实顺序。为什么按排序训?因为推理时它要面对的从来不是"这一个相关不相关",而是一整排候选要排出先后。

**推理时**就是字面的样子:每个 (query, 候选文档) 拼起来、过一遍,得一个相关分数。难点在"怎么打分"——transformer 输出的是每个 token 的向量,不是一个分数,得靠"打分头"决定读它的哪个输出、怎么把它变成一个数。两种姿势:

- **分类头**:拿序列里某一个 token 的向量(通常是 [CLS] 那个——它在句首,attention 看过整串,等于整串的浓缩意见),过一个很小的线性层,把 768 维映射成两个数:分给"相关"和"不相关"两个类别的 logits(logits 是模型最后一层输出的未归一化原始分数,数值越大越像那类)。[bge-reranker-v2-m3](https://github.com/FlagOpen/FlagEmbedding) 就取两类 logits 的差值当相关分。
- **生成头**:不给模型接任何新层,而是让它**继续生成下一个词**。MonoT5 训练时被告知"相关就输出 true、不相关就输出 false",所以推理时看模型在下一个词位置上分给 "true" 和 "false" 的概率(logits),取两者之差当分数。

为什么能设计出这两种头?因为 transformer 的输出本来就有两种读法:把最后一层的向量当特征、自己在后面接个分类器(分类头);或者把模型当文本生成器、直接从它的下一个词分布里读答案(生成头)。两条路问的是同一个问题——"这个 (query, 文档) 对,你觉得相不相关?"——只是换了个姿势问。

四种范式,值得放到一张图里看。ColBERT 论文这张图是神经 IR 匹配范式的全景图:(a) 表示式——查询和文档各编码成一个向量再比,本文的 embedding 双塔就属于这一路;(c) 全交互式——拼成一个序列过 transformer,就是本文的单塔;(d) 晚期交互——先各自编码成 token 向量、查询时逐对取最大相似度再求和(MaxSim),是 ColBERT 的贡献,正是下一节要讲的"曲线的中点";(b) 词级交互——给词和词建交互矩阵(DRMM/KNRM 那类经典 IR 模型),比 (a) 细但不是 transformer 系,本文不展开。主线上的三个主角都在图里:(a) 双塔、(c) 单塔、(d) 那个中点。

![神经 IR 匹配范式全景](/images/colbert-paradigms-8bit.png)
*图:神经 IR 的四种匹配范式——(a) 表示式、(b) 词级交互式、(c) 全交互式、(d) 晚期交互(arXiv:2004.12832,Figure 2)*

这就是全文的主线地图:别把 embedding 和 reranker 当成两个孤立工具,它们是同一条"何时判断 × 看得多细"的取舍曲线的两端——双塔把判断提前到入库(粗而快),单塔把判断留到查询时(细而慢)。而曲线中间还有一站——(d) 晚期交互,下一节讲。

## 曲线的中点:晚期交互

如果"一颗向量"太粗,"每对完整前向"太贵,中间还有第三条路:晚期交互(late interaction)。[ColBERT](https://github.com/stanford-futuredata/ColBERT)(2020,arXiv:2004.12832)的做法是:入库时不为整段存一个向量,而是为每个 token 存一个向量(预计算,偏晚);查询时 query 的每个 token 和 passage 的每个 token 逐对算相似度,取最大再求和(MaxSim)。既拿到双塔的"预计算",又拿到单塔的"逐 token 细粒度"。

![ColBERT 架构](/images/colbert-arch-8bit.png)
*图:ColBERT 的晚期交互,查询和文档的 token 向量逐对 MaxSim(arXiv:2004.12832,Figure 3)*

它不是第三种模型,是曲线上的中点:一套输出,两个角色都能当——token 向量可以像双塔一样预计算、建索引,当**检索器**用(全库捞 top-k);MaxSim 打分又可以像单塔一样对 top-k 逐对精排,当 **reranker** 用。注意它没有池化层、不产单向量,所以"粗筛"不是把文档 pool 成单向量再比,而是把 MaxSim 拆成两级:入库时先对文档的 token 向量做聚类(聚的是 token,不是 chunk;ColBERTv2 的 PLAID 索引),查询时每个 query token 先找到最近的簇、锁定候选文档集(近似,粗),再对候选逐对算精确 MaxSim 排序(精)。所以这个中点比想象中常用——Cohere 的商业 rerank 就以晚交互为主力;bge-m3 的"三合一"里也有一个 ColBERT 引擎,第六节会回来引用。

## 怎么训出这份判断

第一节把两个模型在训练、推理时分别长什么样讲清楚了——这一节钻进训练本身:损失、负样本、数据,怎么把"什么算相似"定义出来。一个容易被误解的点先讲清楚:"语义"不是模型先验定义好的,而是"你拿什么当正样本、什么当负样本"定义出来的。同样一段话,拿"意思相近的句子"当正样本训出来的,和拿"能回答问题的那段文档"当正样本训出来的,向量空间完全不同。所以损失、负样本质量、数据构造,比模型结构更决定效果。

embedding、reranker、ColBERT 的训练信号本质是同一个——让相关的 (query, 文档) 得分高于不相关的。区别只在两处:打分函数(单向量点积 / cross-attention / MaxSim),和损失的形态。"对比学习"这个词通常指 embedding 那边的 InfoNCE 或三元组;reranker 那边更多叫"排序学习"(learning to rank),其中 pairwise 和 listwise 也是对比思想,pointwise 则是纯分类。下面会看到,它们共用同一个底子。

### 双塔:把"语义"训进向量空间

第一节讲过双塔训练的目标——正对拉近、负对推远。这一节把它讲透。

先讲一个背景:BERT 本身的句向量其实是坏的。它训完之后,随便两句话的向量,不管相不相关,算出来余弦都差不多(约 0.42)——所有句子挤在空间里同一个区域,余弦分不开谁和谁更像。对比学习做的事,就是把这个"挤成一团"的空间撑开:相关的拉近、不相关的推远,让向量尽量均匀地铺开。撑开之后,余弦才有分辨力。

怎么撑?靠损失函数。现在最主流的是 InfoNCE(van den Oord 2018,arXiv:1807.03748),思想一句话:让每个 query 的正文档,在 batch 里排第一。具体说,一个 batch 里除了 query 自己和它该匹配的正文档,其余都是这个 batch 里其他样本(自动当负样本);把 query 和所有文档的相似度算出来,过 softmax,目标是正文档那一项分数最大。这本质是个分类问题:把"对的"从一堆里挑出来。

损失里有个温度参数 τ(检索任务常见 0.05～0.1):它决定分数被放大多少倍。τ 越小,模型被逼得越狠,正负样本的分数差必须拉得更大才满意;τ 太大,所有分数糊在一起,学不细。E5 预训练用 0.01、bge-m3 微调 0.05、SimCSE 0.05,量级都在这里。

一个值得记住的点:负样本越多,"把对的挑出来"越难,模型学得越好——这就是为什么大家都往死里加 batch,下面说负样本从哪来。

负样本从哪来?最省事的是批内负样本:同一个 batch 里其他样本的编码直接当负样本,零额外前向。SimCSE 无监督版 batch 只用 64,E5 预训练直接上到 32768——靠跨设备收集([FlagEmbedding](https://github.com/FlagOpen/FlagEmbedding) 的 negatives_cross_device 参数)。[sentence-transformers](https://github.com/UKPLab/sentence-transformers) 的 MultipleNegativesRankingLoss 就是 InfoNCE 的封装,手写一份只要十几行:

\`\`\`python
import torch
import torch.nn.functional as F

def contrastive_loss(q, k, temperature=0.05):
    # q, k: 同一 batch 的两个视角,已做 L2 归一化
    # 对角线是正样本对;同 batch 其它行自动成为负样本(in-batch negatives)
    logits = q @ k.T / temperature            # (batch, batch) 相似度矩阵
    labels = torch.arange(logits.shape[0])    # 正样本的位置在对角线上
    return F.cross_entropy(logits, labels)

# SimCSE 里"两个视角"是同一句话过两次编码器:
# q = encoder(sentences)   # 一次 dropout mask
# k = encoder(sentences)   # 另一次 dropout mask
# loss = contrastive_loss(q, k)
\`\`\`

这十几行值得拆开看:q @ k.T 算出一个 batch×batch 的相似度矩阵,第 i 行第 j 列是第 i 个查询和第 j 个文档的相似度;对角线是正样本对;cross_entropy 对每一行做 softmax,把"对角线尽量大、同行其他位置尽量小"当成目标。前面说的温度参数 τ,就在 temperature 这个参数里。

正样本对从哪来?2019 年之后最重要的一个技巧来自 SimCSE(2021,arXiv:2104.08821):把 dropout 当数据增强。同一句话过两次编码器,两次 dropout mask 不同,得到的两个表示互为"正对"。诀窍在于 dropout 必须开着——关掉(p=0)或固定 mask,模型会直接塌缩:所有向量变成同一个。无监督版在 STS-B 句子相似度上拿到 76.3 分,有监督版 81.6;有监督版还把 NLI 里语义相反(矛盾)的句子对当难负样本混进分母,把 STS-B 从 84.9 推到 86.2。

![SimCSE 无监督和有监督对比](/images/simcse-8bit.png)
*图:SimCSE——左为无监督(同一句两个 dropout mask 互为正对),右为有监督(蕴含对为正、矛盾对为负)(arXiv:2104.08821,Figure 1)*

#### 难负样本:质量的分水岭

负样本的质量,是 embedding 训练里最容易被低估的分水岭。随机负样本在向量空间里离基准样本(anchor)已经够远,几乎不产生任何训练信号。只有"看起来像正、其实是负"的样本才逼着模型做精细区分。比如基准样本是"狗追猫",随机负样本"股票今天大涨"离得十万八千里,学不到任何东西;难负样本"猫追狗"才逼模型学会区分"谁在追谁"。类比:分辨猫和汽车不需要训练,分辨猫和狗才需要。

三种挖法,从便宜到贵:
1. **数据里天然就有**:NLI 的矛盾对、问答数据里"答非所问"的文档。
2. **检索器挖**:[FlagEmbedding](https://github.com/FlagOpen/FlagEmbedding) 的 hn_mine.py 对全库建一个 FAISS 索引,取 top-2～200 区间当候选,避开与正样本太像的"伪负样本";挖负样本的老师模型要够强——"用底座模型挖、从底座微调"。
3. **LLM 生成**:E5-Mistral 让 GPT-3.5/4 生成约 50 万条合成数据(含 15 万条指令、约 1.8 亿 token),覆盖 93 种语言。

数据有三个来源,恰好是三代路线的缩影:弱监督 web 对([E5](https://github.com/microsoft/unilm) 的 CCPairs 从 13 亿对一致性过滤到 2.7 亿对,E5 论文声称零样本 BEIR 首次击穿 BM25)、LLM 合成(Qwen3-32B 合成 1.5 亿对)、公开标注(MS MARCO、NQ、SNLI·MNLI)。

这里要泼一盆现实的冷水:上面的规模是几十亿美元和几千张卡堆出来的。E5-Mistral 那约 1.8 亿 token 的合成数据,按当时的 API 价格粗算,光生成就要上百万美元量级,而且合成完还要一致性去重,实际有效率远低于生成量。对你我来说,记住一条务实路径——**约 1 万对标注 + 单卡,几天就能微调出一个够用的模型**,不需要从头训(想更省就用 LoRA 这类只调少量参数的方式)。想知道"够用"长什么样?拿你自己的语料,用现成模型生成正负对,微调一个小模型,量一下 Recall@5 有没有涨——这比花几周攒数据快得多。

#### 指令前缀:训练时学的,不是推理时的咒语

检索型嵌入训练时就把文本分成"查询侧"和"文档侧"两种角色。E5 给查询加 "query:"、文档加 "passage:";bge 只给查询加一句英文指令 "Represent this sentence for searching relevant passages:",文档侧绝不加;Qwen3 更进一步做指令感知,效果 +3%～5%。关键:前缀是训练时学进权重的,推理时必须照抄——训练和推理不一致,效果就会退化,这正好是前一篇"同模型同度量"铁律在提示词层的对应。

#### MRL:一颗向量,按需裁剪

维度不是越多越好,是"够用 vs 烧钱"。Matryoshka Representation Learning(MRL,2022,arXiv:2205.13147)的思路:训练时对不同长度的前缀各算一份损失(如 64/128/256/512/1024 维),逼判别信息挤进前几维。于是同一颗向量,你既可以只用前 128 维(省存储),也可以全用(更准)。

![MRL 嵌套表示](/images/mrl-nested-8bit.png)
*图:MRL 让表示按维度嵌套,前几维就能承载大部分判别信息(arXiv:2205.13147,Figure 1)*

两条铁律:**只有 MRL 训过的模型能截断**,没训过直接砍尾,等于扔掉信息;截完必须再做一次 L2 归一化。OpenAI text-embedding-3-large 官方声称 3072 维裁到 1024 维仍保有 95%～98% 的质量,省约 67% 的存储。

诚实局限:批内负样本有个坑——正样本可能混在 batch 里被当成负样本,模型学到错误信号;大 batch 也有代价,显存和训练速度都吃紧。

### 单塔:排序损失,和从老师那里偷师

第一节讲了单塔学排序、数据分三种喂法——这一节看具体怎么训。现在主流的 reranker 用两种:[bge-reranker](https://github.com/FlagOpen/FlagEmbedding) 家族用 pointwise——把"相关/不相关"当二分类训;Qwen3-Reranker 用 listwise——一个 query 配 1 个正样本和 n 个负样本,把 n+1 个分数做带温度的 softmax,目标是正样本排第一。listwise 最贴合真实检索,原因第一节讲过,这里不重复。

难负样本在这里同样关键:负例质量决定判别力。[FlagEmbedding](https://github.com/FlagOpen/FlagEmbedding) 的训练数据每行是一个 JSON:{"query", "pos", "neg", "prompt"},关键参数 train_group_size 8、query_max_len 32、passage_max_len 128、学习率 5e-6、5 个 epoch。

训练单塔的另一个趋势是 LLM 当老师。[RankRAG](https://github.com/zjukg/rankRAG)(NeurIPS 2024)把排序和生成训练进同一个 LLM,在 5 个 RAG 基准上追平 GPT-4;ReasonRank 用 DeepSeek-R1 生成带推理过程的排序数据,筛掉不稳定的再训。

#### 蒸馏闭环:老师学生,在此兑现

最后是全文的高潮,也是开头"老师和学生"的兑现——**把 cross-encoder 的判别力,蒸馏回 bi-encoder**。动机很朴素:单塔准但贵(每个候选都要拼起来现算),双塔便宜(文档能预计算),但双塔自己训出来的判断力不如单塔。能不能把单塔当老师,教出一个更聪明的双塔?

可以。做法是拿单塔给每个 (query, 文档) 对打分,把分数当"标准答案",去训练双塔,让双塔对同一对也打出接近的分数。细节上的关键:Hofstätter 2020(arXiv:2010.02666)发现,别让学生模仿老师的绝对分数,而是模仿"相关文档和不相关文档之间的分数差"——不同模型的分数天然不在一个刻度上,学相对差比学绝对值稳。

这里面有个反直觉的发现值得记住:[TCT-ColBERT](https://github.com/castorini/tct_colbert)(arXiv:2010.11386) 发现,只喂难负样本、没有老师把关,双塔反而变差——难负样本本身有噪声,必须有个更准的老师来"校对"哪些该分得开。[RocketQA](https://github.com/PaddlePaddle/RocketQA)(2021)用迭代去噪解决同一个问题。到了 2024 年,自蒸馏只用 13.5% 的数据就能达到完整蒸馏的水平。

读到这里,"老师学生"就完整了:评分函数从 cross-encoder 流向 bi-encoder,两个架构共享同一份判断力。说"双塔单塔是一家人"略过了严谨细节——更准确的说法是,蒸馏让它们共享同一份判断力。

诚实局限:bge-reranker-v2-m3 没训过跨语言数据,跨语言对很弱——这正好当"模型卡不等于真实能力"的例证。

## 推理:成本住在哪儿

训练讲完了,现在算账——推理时钱花在哪。

**双塔**:入库时把全库向量算完,查询时只剩"query 编码一次 + ANN 检索"(前一篇讲过)。文档只编码一次,之后每次查询都不再碰它们,成本不随查询次数涨。

**单塔**:每个 (query, 候选) 都要拼起来过一遍完整 transformer——有几个候选就算几次,而且没法提前存任何东西,因为分数是 query 和 passage 拼出来的,换一个 query 全部重算。所以生产里 reranker 只对第一阶段捞回来的 top-100 精排,延迟预算 100～400ms;bge-reranker-v2-m3 的输入上限 max_length=1024;[jina-reranker-v2](https://github.com/jina-ai/jina-embeddings) 对超长文档自动滑窗切块(overlap 默认 80)取各块最高分。为什么检索必须分两段、第一段宁多勿漏?前一篇的"人力粗筛 vs 面试官细聊"已经讲透,这里不重推。

L2 归一化值得补一个新角度:前一篇讲它为了能用内积索引,更本质的是——**扔掉长度,只留方向**。同一句语义,无论写多写少,方向应该一致。bge v1.5 专门为此重训,让相似/不相似的分数分布可分。

工程细节三条:
- **批量推理**:batch 摊薄 transformer 开销。
- **缓存**:块的哈希命中就跳过推理;注意改分块参数后要整体重新入库。
- **MRL 维度裁剪落地**:只有 MRL 训过的能裁(两条铁律训练节讲过,这里直接落地)。text-embedding-3-small 官方声称裁到 512 维保约 97.6% 质量,1536 维裁到 512 省 2/3 存储。

存储账:1024 维 float32 约 4GB/百万向量(前一篇讲过)。用 MRL 前 128 维建 ANN 索引,1000 万条约 5.1GB,全维要 41GB,约省 8 倍。注意细节:**索引建在截断维度上,全维向量另存,粗筛 top-200 后拿全维精打分**。FAISS 里就几句话:

\`\`\`python
import faiss
import numpy as np

dim = 128                          # 只取 MRL 的前 128 维做索引
index = faiss.IndexFlatIP(dim)     # 归一化后内积 = 余弦
index.add(full_vectors[:, :dim])   # 截断维度建 ANN;全维向量另存
_, I = index.search(query[:dim][None], 200)   # 粗筛 top-200
# 再用全维向量对 top-200 精打分、排序,取前几个
\`\`\`

## 谁说了算:MTEB,和榜单骗你的地方

搭好了、训好了,怎么知道它好不好?嵌入模型的通用榜单是 [MTEB](https://github.com/embeddings-benchmark/mteb)(2022,arXiv:2210.07316)。它把 8 类任务、58 个数据集凑在一起算平均分。第一件事要记住:**检索只是 8 类任务之一**,总分是等权平均——一个模型可能在聚类、重排上很强、检索很弱,总分照样好看。所以读榜必须看分项,尤其检索那一项(它直接复用 BEIR,主指标 nDCG@10,衡量"相关结果是不是排进前 10,而且排得越靠前分越高")。中文看 C-MTEB(bge 同篇发布,arXiv:2309.07597,35 个数据集、6 类任务);跨语言看 MIRACL;2025 年又出了 MMTEB,500+ 任务、250+ 语言。

为什么"MTEB 排前面"不等于"你的语料上好用"?几个坑:
1. **英语中心**:中文要用 C-MTEB 这类中文榜。
2. **零样本域外**:BEIR 基准建立的初衷就是暴露一个失败——只在 MS MARCO 训过的 dense 模型,一到域外就崩,**BM25 反而反超**。你的语料也是域外。这是"榜单骗你"最有力的一张牌。
3. **榜单污染与配置不一致**:换实现、换归一化方式,分数就有出入。

还有一条纪律要养成:凡 vendor 自报的数字(Qwen3-Reranker 对比 bge-reranker-v2-m3 的 +21.0% MTEB-R、bge-m3 三合一、OpenAI 的裁维数字),一律当"自家评测"看待,读完接一句"换到你的语料,自己量一遍才作数"。

务实建议:别迷信任何榜单,拿约 50 条真实 query,量 Recall@5 / nDCG@10——这正是前一篇"先量出来,再说好坏"的落点。所有报道出来的分数和区间,在这里都要被你自己的数据重新量一遍。

最后,几件容易踩的坑一并列出(前面讲过的只点一句):① 各向异性——句向量挤成一团、余弦分不开(训练节讲过)② 批内负样本会误伤、大 batch 代价高(训练节讲过)③ 维度不可解释(前一篇说过"每一维都不可解释,意义活在相对位置里")④ bge-reranker-v2-m3 跨语言弱 ⑤ 单向量召回天花板:512 维约 50 万篇、1024 维约 400 万篇文档起开始漏召回(Weller 等,arXiv:2508.21038,前一篇埋过钩子)。

## 2024-2026:这两个模型在怎么变

近三年的变化,可以压缩成三个因果。

**因果一:合成数据解除了数据瓶颈,decoder 骨干吃长上下文,MRL 成标配。** 2024 年 1 月 E5-Mistral(arXiv:2401.00368)证明用 GPT-4 合成数据 + 少于 1000 步微调就能刷榜(注意它几乎没有标注数据);同年 6 月 [GTE-Qwen2](https://github.com/Alibaba-NLP/gte) 把骨干换成 Qwen2(7B、3584 维、32K 上下文);8 月 [NV-Embed-v2](https://github.com/NVIDIA/NV-Embed) 证明只用公开数据也能达到同样高度;2025 年 6 月 [Qwen3-Embedding](https://github.com/QwenLM/Qwen3)(0.6/4/8B、32K 上下文、MRL 32～4096 维、Apache-2.0)把这套组合固定成新基线。

**因果二:评估压力倒逼变大变全。** 榜在涨,模型就必须跟着涨。reranker 同步变大:Qwen3-Reranker(0.6/4/8B)MTEB-R 8B 达 69.02(对比 bge-reranker-v2-m3 的 +21.0%,自家评测)。指令感知从 INSTRUCTOR(2022)到 bge-en-icl 再到 Qwen3,成了标配能力。

**因果三:蒸馏主流化,开源与 API 的差距基本弥合。** RAG-Retrieval 这类框架把 7B LLM reranker 蒸馏成边缘小模型;cross 是精排标杆、bi 是蒸馏出的廉价近似。开源侧 NV-Embed-v2 72.31、Harrier 27B 74.3、Jina v5 71.7(均为官方宣称),和商业 API 的差距已经不大。

还有一个重要的单模型形态:**bge-m3**(0.567B、1024 维、8192 上下文、100+ 语言)——一次推理同时产出 dense、sparse、ColBERT 三种表示(稀疏是对关键词检索的覆盖,ColBERT 引擎就是第二节那个"中点"),三合一在 MIRACL/MKQA/MLRB 上全面超单模式(自家评测)。这一代模型不再问你"选哪个范式",而是全都给。

MRL 变维常态化:OpenAI text-embedding-3 官方声称 3072 维裁到 256 维仍超 ada-002 完整的 1536 维(64.6 vs 61.0);Nomic 768 → 64(官方声称)。

多模态和 agent 各留一句:Cohere Embed v4 把文本和图像放进同一个空间,Gemini Embedding 2、Jina v5-omni 同路;agent 里 embedder 还被拿来选工具/函数——"工具描述"当文档、"用户意图"当查询,判断对象在变,判断方式的取舍没变。

诚实的展望:模型越来越大、越来越贵——4～8B 的 embedding 对个人开发者不现实,GTE-Qwen2-7B FP32 要约 28GB 显存,入库、存储、推理全涨。选型重心正在从"MTEB 分数"转向合规、延迟、运维;4-bit 量化 + MRL 成了新标配。

## 结论:判断发生在哪,决定模型长什么样

回头看,池化、维度、负样本、蒸馏、变大——一切都是"判断发生在何时 × 看得多细"这条取舍的推论。双塔把判断提前到入库,用一颗向量换速度;单塔把判断留到查询时,用逐 token 互看换精度;晚期交互站在中间,预计算和细粒度两头都要。而蒸馏这条线把两端缝在一起:cross 的判别力流向 bi,它们共享同一份判断力。

一句话选型:本地用 [bge-m3](https://github.com/FlagOpen/FlagEmbedding)(1024 维、MIT、三合一,前一篇讲过)或 Qwen3 小档(0.6B、1024 维、Apache);走 API 用 text-embedding-3(3072/1536 + MRL)或 Cohere/Gemini。2026 年的生产基线是混合检索 + 强制 rerank(前一篇讲过,和这里不打架)。

回到开头的翻车场景:答案在库里,却没被捞出来——问题出在两个裁判身上。好消息是,裁判是可以换、可以训、可以蒸馏的。先把天花板量出来(约 50 条真实 query),再把力气花在对的地方。LLM 一本正经地编,正是这两个裁判,让"别让它记,让它查"成立。`,
  takeaway: 'Embedding(bi-encoder)和 reranker(cross-encoder)回答同一个问题"这段文档跟你的问题相不相关",差别只在判断发生在什么时候(入库时 vs 查询时)和看得多细(一颗向量 vs 逐 token 互看),晚期交互是这条取舍曲线上的中点。训练的本质是"拿什么当正负样本就定义出什么语义":InfoNCE + 难负样本 + 合成数据塑造双塔,排序损失 + LLM 蒸馏塑造单塔;蒸馏最终让两个架构共享同一份判断力。榜单分数不等于你的语料效果,拿约 50 条真实 query 先量出来。',
}

export default article
