const article = {
  slug: 'embodied-ai',
  date: '2026-08-03 00:30',
  name: 'Embodied AI from Scratch: RL to Diffusion Policy',
  description: '具身智能主流方法论的两次范式切换：从 RL 奖励试错（REINFORCE → PPO）到模仿学习 + 扩散模型（Diffusion Policy）。两代方法怎么解决同一个问题，以及为什么后者成了主流。',
  tags: ['RL'],
  category: 'Project',
  author: 'Shannon',
  detail:
`零基础学具身智能，第一个接触的通常是强化学习（RL）。但也正是在这里有一个让人困惑的落差：RL 学完之后抬头一看，业界主流已经换成了模仿学习。特斯拉 Optimus、π0、OpenVLA——这些当下最前沿的机器人模型，没有一个靠奖励试错训练。

这篇文章梳理这两代范式：它们分别怎么解决"让机械臂学会做任务"这个问题，以及为什么第二代成了主流。

## 第一代：强化学习，靠奖励试错

RL 的核心循环一句话：看状态 → 按策略选动作 → 拿奖励 → 更新策略。策略就是"看到什么就做什么"的映射，它输出的不是固定动作，而是每个动作的概率分布——既偏向已知好的，又保留探索空间。

\`\`\`
        ┌─────────────────────────────────────────────────┐
        │                      Agent                      │
        │                   （智能体）                    │
        │                 Policy（策略）                  │
        └──────────────┬───────────────────┬──────────────┘
        │       action│          state s_t │              │
        │     （动作）│         reward r_t │              │
        │              │     （状态、奖励）│              │
        │              ▼                   ▲              │
        ┌──────────────┬───────────────────┬──────────────┐
        │                   Environment                   │
        │                    （环境）                     │
        └─────────────────────────────────────────────────┘
\`\`\`

循环就在两个实体之间：智能体按策略选动作（左边向下），环境返回新状态和奖励（右边向上），智能体根据奖励更新策略，然后再选下一个动作。两个实体、三个信号，就是 RL 的全部骨架。

入门环境是 CartPole（倒立摆）。动作空间只有两个离散值，但"动态平衡"这个难题逼你把算法写对。

**REINFORCE** 是最朴素的策略梯度，核心只有一行：

\`\`\`python
pi_loss = -torch.sum(log_probs * advantages)  # 回报高的动作，概率上调
\`\`\`

一个回合打完，回报高的动作概率就往上推。但它的毛病是**方差大**——一局运气好，梯度就乱跳，训练稳不下来。修法是减一个基线，用价值网络 V(s) 预测"这个局面本来就该值多少"，把运气分减掉：

\`\`\`
advantage = G_t − V(s_t)
\`\`\`

这就是 actor-critic 的雏形：actor 做决定，critic 打分。这一步修完，CartPole 从"学不进去"变成稳定收敛，实测第 471 回合触发 solved。

训练好的策略就是反复"杆往哪倒、往哪推"——看 GIF 里的小车始终保持平衡：

![CartPole 演示：倒立摆保持平衡](/images/cartpole-demo.gif)

**PPO** 补了 REINFORCE 剩下的两个洞：数据用一次就扔，步子迈太大容易崩。它做两件事——同批数据复习多遍（用重要性采样加权），和 clip 限速（每个动作概率一次最多改 20-30%）。

\`\`\`python
model = PPO("MlpPolicy", env, n_steps=512, gamma=0.9, clip_range=0.3)
\`\`\`

限速和复用互相支撑：正因为每次只改一点，新旧策略才不至于差太远，旧数据才敢放心复用。这套设计让 PPO 成了机器人学习里最常用的 RL 算法。

在 Reacher（平面双关节臂够目标）上实测：加上奖励工程——把控制惩罚从 1.0 降到 0.1——效果从 -6.1 提升到 -4.69。机械臂从"慢吞吞地挪"变成"果断扫向目标、稳稳停住"：

![Reacher 演示：双关节臂伸向目标](/images/reacher-demo.gif)

## RL 的三个难处

RL 能学，但代价不小：

- **奖励设计难**。奖励怎么定直接影响能不能学会，定得不好策略会钻奖励函数的空子。
- **试错成本高**。让真实机械臂瞎试，摔几次硬件就没了。仿真能缓解，但仿真和现实之间有 gap。
- **探索低效**。一个动作一个动作地试，学一个任务要几百万步。

## 第二代：模仿学习，看高手怎么做

模仿学习换了个思路：不靠奖励试错，直接给模型看"高手怎么完成任务"的演示，让它照着学。

核心算法是 **Diffusion Policy**。它不直接输出动作，而是**从纯噪声出发，看着画面，一小步一小步去噪，洗出一整串未来动作**：

- 训练：拿专家动作加噪声，教模型还原。
- 推理：给纯噪声，让模型一步步去噪成正确动作。

\`\`\`bash
lerobot-train --dataset.repo_id=lerobot/pusht --env.type=pusht \
  --policy.type=diffusion --steps=200000 --device=cuda
\`\`\`

注意去噪的对象是**动作数字**，不是画面——画面只是模型做判断的参考。

## 为什么是扩散

因为动作分布是**多模态**的。推一个 T 形方块，有多种"都对"的推法——左边推、右边推、先转再推。演示数据里天然包含这些不同做法，模型必须能表达整个分布。

普通回归网络做不到：它会把多种做法**平均成一种"谁都不像"的推法**。扩散模型能保留每一种——这不是锦上添花，是模仿学习的刚需。

## 用什么跑的：LeRobot

上面那条 \`lerobot-train\` 命令来自 **LeRobot**——HuggingFace 维护的开源机器人学习框架（Apache-2.0，GitHub 约 2.6 万 stars）。它把机器人学习标准化成一条流水线：

- **数据采集**：统一的 \`Robot\` 接口遥操作低成本机械臂（SO-100 等）
- **数据集**：统一成 LeRobotDataset 格式——同步 MP4 视频（视觉）+ Parquet（状态/动作），托管在 HuggingFace Hub 上，社区已共享 16000+ 个数据集
- **训练**：一条命令，内置 ACT、Diffusion Policy、VQ-BeT 等策略
- **评估 / 部署**：\`lerobot-eval\` 评估，推理只是一个"取观测 → 选动作 → 发动作"的循环

它常被比作"机器人领域的 transformers"——统一格式 + 共享数据 + 预训练模型，让同一套代码和模型格式在仿真与真机之间贯通。我们用的 PushT 就是它的官方入门任务：数据直接下载、训练一条命令、评估出成功率，整条流程就是 LeRobot 的日常用法。

## 论文里的三个关键设计

Diffusion Policy 论文（Chi et al., RSS 2023）真正管用，靠的是三个设计的组合。整条流程就是论文的架构图——观测序列 → 视觉编码器 → DDPM 去噪迭代 → 预测动作序列：

![Diffusion Policy 架构图](/images/diffusion-policy-arch-8bit.png)

**动作分块（Action Chunking）**——一次预测一整段未来动作（论文里多数任务预测 16 步），而不是一步。单步预测的毛病是相邻动作可能来自不同"模式"，造成抖动；预测整段能保证时序一致，而且对"停在原地"的空闲动作更鲁棒。代价是预测太长反应会变慢，16 步是平衡点。

**视觉条件化（Visual Conditioning）**——视觉特征只提取一次，不随去噪迭代重复计算，这让实时推理成为可能。用 ResNet-18 端到端训练（实测端到端 95% > 预训练 R3M 80% > ImageNet 15%），观测通过 FiLM（CNN 版）或交叉注意力（Transformer 版）注入去噪过程。

**重规划（Receding Horizon）**——预测 16 步，只执行前 8 步，然后重新看画面再规划。有点像开车看远 16 米、只开 8 米就重新看路——既保持前瞻性，又能对突发情况及时反应。真实世界跑 10Hz，线性插值到 125Hz 执行。

## 论文的实验结果

论文在 4 个 benchmark 的 12 个任务上对比行为克隆（LSTM-GMM）、隐式行为克隆（IBC）和行为 Transformer（BET），平均提升 **46.9%**。

Push-T 上（状态观测）：

| 方法 | 成功率（max / 最后平均） |
|---|---|
| **Diffusion Policy** | **0.95 / 0.91** |
| IBC | 0.90 / 0.84 |
| BET | 0.79 / 0.70 |
| LSTM-GMM | 0.67 / 0.61 |

真实 Push-T 上差距更大：Diffusion Policy **95% 成功率、0.80 IoU**，人类 100%、0.84 IoU——论文说它"接近人类水平"；而最强的 baseline（IBC）在真实环境是 0%。下图为真实 Push-T 上各方法的动作轨迹对比，Diffusion Policy 的轨迹最接近目标：

![真实 Push-T 各方法轨迹对比](/images/diffusion-policy-pusht-8bit.png)

其他任务：Kitchen 的最终成功率指标提升 **213%**（0.99 vs 0.44）；真实翻杯子 20 次试验 **90% 成功**，baseline 是 0%。多阶段任务（Block Push / Kitchen）的定量对比也一致地偏向 Diffusion Policy：

![Block Push / Kitchen 定量对比](/images/diffusion-policy-blockpush-kitchen-8bit.png)

论文把优势归结为三点：能表达多模态分布、能预测高维动作序列、训练稳定（扩散模型不需要像 IBC 那样估计归一化常数）。

## 两代对比

| | 强化学习 (RL) | 模仿学习 (Diffusion Policy) |
|---|---|---|
| 学什么 | 从奖励试错中学 | 从专家演示中学 |
| 需要奖励 | 是 | 否 |
| 数据 | 在线采集 | 离线数据集 |
| 动作生成 | 网络直接输出 | 从噪声去噪生成序列 |
| 分布假设 | 单峰 | 多峰 |

## 实测效果

在 PushT（2D 推 T 形方块进目标区）上实测，200k 步训练后：成功率 36%、重叠度 0.84。对照：500 步基线成功率 0%、重叠度 0.05——基本没碰到方块。

**500 步基线**——推杆基本在乱抖，碰不到目标：

![500步基线：随机乱推](/images/pusht-baseline.gif)

**200k 步训练后**——学会把 T 形方块推进目标区：

![200k步训练后：把T形推进目标区](/images/pusht-trained-1.gif)

官方同配置报 63.8%（500 回合评估，方差更小；我们 50 回合）。36% 不算高，但"从完全不会到会推"的跨越是实打实的。

模仿学习的瓶颈不在算法能不能学，在演示数据的**质量和数量**。数据越多、越多样，策略越稳；数据不足，策略就只能在见过的局面里打转。

## 结论

两代方法解决同一个问题，路径不同：RL 靠环境反馈自我改进，模仿学习靠数据直接告诉模型答案。当前主流是后者——代价是它不再需要奖励函数，需要的是高质量演示数据。

对学习者来说，两条线都值得跑一遍：RL 教的是"智能体-环境"的骨架，模仿学习教的是"数据驱动"。技术栈在变，骨架没变——状态、动作、反馈这三个词，两代方法都在用，只是反馈的来源从环境奖励变成了人类演示。`,
  takeaway: '具身智能的主流方法论已经从 RL 奖励试错切换到了模仿学习 + Diffusion Policy。切换的本质是反馈来源变了：从环境奖励变成人类演示。RL（REINFORCE→actor-critic→PPO）教的是"智能体-环境"的骨架，Diffusion Policy 解决的是多模态动作分布——后者正是真实操作里最普遍、也最难的那部分。学具身智能，两条线都值得跑一遍，纯仿真就够。',
}

export default article
