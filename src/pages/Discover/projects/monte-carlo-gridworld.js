const project = {
  slug: 'monte-carlo-gridworld',
  date: '2026-08-19 15:00',
  name: 'Model-Free Monte Carlo in a 3×3 Grid World',
  url: 'https://github.com/MathFoundationRL/Book-Mathematical-Foundation-of-Reinforcement-Learning',
  description: '第 5 课:无模型的蒙特卡洛。和上一篇的区别只有一个——模型没了。策略迭代、价值迭代每一步都要用到 P 和 R;MC 手里只有一条条 episode 数据。用同一个 grid world,把 MC 和"模型版"逐行对照:期望从"算"变"估"、Q 直接估、改进步一模一样。再沿 MC Basic → Exploring Starts → ε-Greedy 走一遍,包括"单条长回合就能学到最优"的 ε-Greedy 例子.',
  tags: ['RL'],
  author: 'shannon',
  takeaway: 'MC 和模型版的区别只有一处:期望从"代入 P、R 算"变成"用数据平均"。策略迭代→MC Basic(评估换成数据平均),MC Basic→Exploring Starts(把数据用透),Exploring Starts→ε-Greedy(软策略买探索)。改进步永远一样:argmax q。',
  detail: String.raw`

## 1. 上一篇和这一篇:区别就在"有没有模型"

上一篇(2×2 grid world)把策略迭代、价值迭代完整走了一遍——它们都是**模型版**:策略评估要解贝尔曼方程、价值迭代的公式里都有 $P(s'|s,a)$ 和 $R(s,a)$。每一步的期望都是"代入 $P$、$R$ 算"出来的。

这一篇:同一个 grid world(目标 $s_9$、禁区 $s_6$、$\gamma=0.9$、5 个动作 up/right/down/left/stay,规则详见上篇,不重复),**但把模型藏起来了**——不知道 $P$、不知道 $R$,手里只有智能体与环境交互产生的 **episode(回合)数据**:一条条 $(s,a,r,s')$ 组成的轨迹。

![3×3 grid world 与初始策略 π0](/discover/mc-3x3-map.png)

初始策略 $\pi_0$ 是任意给的(图上黄色箭头,在 $s_1$、$s_3$ 指向撞墙,不是好策略)。后面所有数字,都从 $\pi_0$ + 采样数据算出来。

> 整篇就一个区别:**期望从哪来**——模型版"算",MC 版"估"。三条算法、两个例子,全是围着这个区别转。

## 2. 区别之一:期望,从"算"变"估"

模型版怎么得到 $q_\pi(s,a)=\mathrm{E}[G_t|s_t{=}s,a_t{=}a]$?代入 $P$、$R$ 解方程(上篇的贝尔曼方程)。

MC 版换一个思路:$q$ 是个期望,而**期望可以用样本均值估**。这是蒙特卡洛的核心——用随机样本解决估计问题。抛硬币:$X$ 正面 $+1$、反面 $-1$,不知道分布,抛 $n$ 次取平均,就是 $\mathrm{E}[X]$ 的估计,越抛越准(大数定律)。样本要独立同分布。

所以 MC 的评估步:从 $(s,a)$ 出发按 $\pi$ 收集 $n$ 条 episode,回报取平均:

$$
q_\pi(s,a) \approx \frac{1}{n}\sum_{i=1}^{n} g^{(i)},\qquad g^{(i)}\ \text{是第}\ i\ \text{条 episode 的折扣回报}\ G_t
$$

**模型版"解方程",MC 版"收数据"。模型越完整,方程越精确;数据越多,估计越准。** 这就是 model-based 和 model-free 的分水岭。

## 3. 区别之二:MC Basic 只换"评估",不换"改进"

策略迭代有两步:

1. **策略评估(要模型)**:解 $V_\pi=r_\pi+\gamma P_\pi V_\pi$,再由 $q_\pi(s,a)=R(s,a)+\gamma\sum_{s'}P(s'|s,a)V_\pi(s')$ 转成 $q$。
2. **策略改进(不要模型)**:$\pi_{k+1}(s)=\arg\max_a q(s,a)$。

**问题出在"从 $V$ 转 $q$"那条公式上——里面有 $P$。** 没模型,解完 $V$ 也转不出 $q$。所以 MC Basic 的改法很直接:

- **策略评估(用数据)**:直接从 $(s,a)$ 出发采样、回报平均 → $q_k(s,a)\approx q_{\pi_k}(s,a)$。
- **策略改进(和以前一模一样)**:$\pi_{k+1}(s)=\arg\max_a q_k(s,a)$。

> 注意 MC Basic 是**直接估计行动值 $q$**,不是先估 $V$ 再转——因为 $V\to q$ 要 $P$,直接估 $q$ 绕开模型。**改进步没换:永远 $\arg\max q$。** 决策靠 Q 这条,和上篇完全一致,变的是"Q 从哪来"。

## 4. 例子:MC Basic 在 s1 上把策略改对

环境是确定性的,同一个 $(s,a)$ 永远走同一条轨迹,所以**每个 $(s,a)$ 收一条 episode 就够**。对 $s_1$ 的 5 个动作各出发一条、按 $\pi_0$ 走下去:

| 动作 | episode 内容(按 $\pi_0$ 走下去) | $q_{\pi_0}(s_1,a)$ |
|---|---|---|
| up | $s_1 \to s_1 \to s_1 \to\cdots$(每次撞墙 $-1$) | $-1/(1-\gamma)=$ **-10** |
| right | $s_1 \to s_2 \to s_5 \to s_8 \to s_9 \to s_9 \to\cdots$ | $\gamma^3/(1-\gamma)=$ **7.29** |
| down | $s_1 \to s_4 \to s_5 \to s_8 \to s_9 \to s_9 \to\cdots$ | **7.29** |
| left | $s_1 \to s_1 \to\cdots$(撞墙 $-1$) | **-10** |
| stay | $s_1 \to s_1 \to s_1 \to\cdots$(先 $0$,再被 $\pi_0$ 拖去撞墙) | $-\gamma/(1-\gamma)=$ **-9** |

表里每一行就是"收一条 episode、算折扣回报"——**没有用到任何 $P$、$R$ 的表格,只用了采样**。right 那条走到第 4 步进目标 $s_9$、之后 stay 每步 $+1$,所以从 $\gamma^3$ 开始累加,得 $7.29$。

![q_π0(s1,·) 的估计](/discover/mc-q-s1.png)

$\arg\max$ 是 **right / down(7.29)** → $\pi_1(s_1)=\text{right}$ 或 $\text{down}$。而 $\pi_0$ 在 $s_1$ 是 up(撞墙)。**只看 5 条 episode,策略就从"撞墙"改成了"朝目标走"。**

课程里 $s_3$ 同理:五个动作 $-10,-10,8,7.29,-9$ → $\pi_1(s_3)=\text{down}$。有意思:down 先踩一步禁区($-1$)再进目标($+1$),算下来 $8$,比绕远路的 left($7.29$)还高。这个简单例子里一次迭代就到最优;复杂问题要多轮。

## 5. 区别之三:数据效率与访问覆盖(MC Exploring Starts)

MC Basic 有两个软肋:

1. **数据浪费**:一条 episode 只用来估计它**开头的那个 $(s,a)$**(首次访问)。比如上一节的 $s_1\to s_2\to s_5\to s_8\to s_9$ 这条,只贡献给了 $(s_1,\text{right})$——$s_2$、$s_5$、$s_8$、$s_9$ 明明也在这条轨迹里,它们的行动值被白白浪费。
2. **访问覆盖**:要对每个 $(s,a)$ 都估,就得有 episode 从每个 $(s,a)$ 出发。**没访问到的 $(s,a)$ 永远估不到**,改进时可能被跳过——哪怕它其实是最优动作。

MC Exploring Starts 各修一半:

1. **每次访问(every visit)**:一条 episode 里的每个被访问的 $(s,a)$ 都算数。轨迹 $s_1\to s_2\to s_5\to s_8\to s_9$ 拆成五条子-episode,一条 episode 一次扫描(从后往前"回溯"算 return)就给五个 $(s,a)$ 各贡献一个样本:

![一条 episode 拆成五条子-episode](/discover/mc-episode-decomp.png)

2. **收一条就更新**:不等某个 $(s,a)$ 的所有 episode 收齐再平均,拿到一条就更新一次。单条估计当然不准,但和"截断策略迭代"一个道理——估不准也能往前走。

代价是加了一个条件——**Exploring Starts 条件**:要估出每个 $(s,a)$,必须保证每个 $(s,a)$ 都被足够多次访问(比如让每个 $(s,a)$ 都可能成为回合起点)。这个条件在真实环境里常常很难满足。于是有了第三个算法。

> 一句话:**MC Basic 是"一箭一靶"**(一条 episode 只估一个 $(s,a)$),**Exploring Starts 是"一箭多靶"**(一条 episode 估一串)。差别就在样本利用。

## 6. 区别之四:软策略买探索(MC ε-Greedy)

Exploring Starts 条件的麻烦在于要"特意保证"访问。换个思路:如果策略本身就是**软策略**——每个动作都有非零概率被选到——那一条足够长的 episode 自然会把所有 $(s,a)$ 都访问一遍,条件自动满足。

最常见的软策略是 $\varepsilon$-Greedy。设 $a^*=\arg\max_a q(s,a)$、$|\mathcal{A}(s)|$ 为该状态动作数:

$$
\pi(a|s) = \begin{cases} 1-\varepsilon+\dfrac{\varepsilon}{|\mathcal{A}(s)|}, & a = a^* \\[6pt] \dfrac{\varepsilon}{|\mathcal{A}(s)|}, & a \neq a^* \end{cases}
$$

采样:抽 $x\in[0,1]$,$x<\varepsilon$ 就均匀随机选一个动作,否则选贪心动作。以 $s_1$、$\varepsilon=0.2$、5 个动作为例:贪心动作概率 $1-0.2+0.04=0.84$,其余四个各 $0.04$。

把 MC Exploring Starts 的"改进选贪心"换成"改进选 $\varepsilon$-Greedy",就得到 **MC $\varepsilon$-Greedy 算法**,不再依赖 Exploring Starts 条件。

**代价:策略被限制在 $\varepsilon$-Greedy 这个集合里**,只能保证是该集合中的最优,不一定是全体策略里的最优——$\varepsilon$ 越大,离真正的最优越远,但探索越充分。这就是强化学习里最重要的权衡——**探索与利用**:探索(多试动作,才能评估好)vs 利用(多用当前最好的动作,才能拿高分)。$\varepsilon$ 是旋钮,实际应用常用"先大后小"。

## 7. 例子:一条长回合,ε=0.5,三轮学到最优

课程里最反直觉的演示:**MC $\varepsilon$-Greedy 每轮只需要一条回合。** 因为 $\varepsilon$-greedy 是随机的,一条足够长的回合会把所有 $(s,a)$ 对都访问到,不需要像 MC Basic 那样为每个 $(s,a)$ 单独开一条。

在我的 3×3 世界上模拟(每轮一条 60000 步回合、$\varepsilon=0.5$):

| 轮次 | 一条回合访问的 $(s,a)$ | 与最优策略一致的动作 |
|---|---|---|
| 1 | 45/45 | 6/9 |
| 2 | 45/45 | 8/9 |
| 3 | 45/45 | 9/9(全最优) |

(之后在 8–9 之间小幅波动,是 $\varepsilon$ 的随机性。) **每轮就一条回合,把 45 个 $(s,a)$ 的 $q$ 全估了一遍,三轮策略就到位了。** 对比 MC Basic:45 对要 45 条 episode(确定性环境)。$\varepsilon$-greedy 用一条长的换掉 45 条短的——**用随机性换采样效率**。

$\varepsilon$ 的代价同样看得见。用最优策略配不同 $\varepsilon$ 采样十万步,统计"访问覆盖"和"踩禁区比例":

![一条回合的访问覆盖 + ε 的权衡](/discover/mc-eps-cover.png)

| $\varepsilon$ | 访问覆盖 | 踩禁区比例 |
|---|---|---|
| 0.05 | 20/45 | 1.0% |
| 0.1 | 30/45 | 2.0% |
| 0.2 | 39/45 | 4.1% |
| 0.5 | 45/45 | 10% |
| 1.0 | 45/45 | 11% |

$\varepsilon$ 小:覆盖不全(20/45),那些没访问到的 $(s,a)$ 可能就错过了最优动作;$\varepsilon$ 大:覆盖全(45/45),但策略明显变差(10% 的时间在踩禁区)。**探索和利用就是这么互斥——这组数字就是代价的度量。**

## 8. 还有一条隐含规则:回合要够长

MC 的数据是"完整的回合",**回合长度本身就是个坑**——这背后是稀疏奖励(sparse reward)问题。回报只在到达目标 $s_9$ 之后才是正的;从 $s_1$ 到 $s_9$ 最少 4 步($s_1\to s_2\to s_5\to s_8\to s_9$)。回合长度不足 4,那条 episode 里永远看不到 $+1$,回报全是 $0$——数据收了等于白收。

| 回合长度 L | 谁拿到非零价值(能被 MC 看到) |
|---|---|
| 1 | 只有 $s_9$(目标)和 $s_6$、$s_8$(一步就能进目标) |
| 2 | 加上 $s_5$、$s_7$(两步内到目标) |
| 3 | 加上 $s_2$、$s_3$、$s_4$(三步内到目标) |
| ≥4 | 全部,包括最远的 $s_1$ |

而且"看得到"不等于"估得准":$L=4$ 时 $s_1$ 的估计才 $0.73$,真实最优值 $7.29$。**回合越长,估计越接近收敛值。** 真实 RL 里这更严重:状态空间大、随机性强时,一条回合碰到目标的概率很低,数据里全是 0。一个常用解法是**稠密奖励**——靠近目标就发小奖励,在目标周围造一个"吸引场"。

## 9. 小结:区别就在"期望从哪来"

- **模型版(上篇)**:期望靠 $P$、$R$ 算。策略评估=解方程;$q$ 由 $V$ 转(要 $P$)。价值迭代直接迭代 $V_*$。
- **MC(这篇)**:期望靠数据估。策略评估=采样平均;**直接估 $q$**。
- **改进步完全一样**:$\arg\max_a q$。

三条算法一条线,每步都是"换一个条件":

- **MC Basic** = 策略迭代把"解方程求期望"换成"样本均值求期望"。最朴素,每个 $(s,a)$ 都要有数据。
- **MC Exploring Starts** = Basic + 用透数据(每次访问 + 收一条就更新)。代价:Exploring Starts 条件。
- **MC $\varepsilon$-Greedy** = Exploring Starts + 贪心换 $\varepsilon$-greedy。代价:$\varepsilon$ 牺牲最优性。

两个现实约束:样本要够(访问覆盖所有 $(s,a)$)、回合要够长(数据里能看到目标)。

值得注意的两点(都和上篇呼应):

- **禁区 $s_6$ 的最优价值也是 $10$**:它一步 down 就能逃进目标。价值顺着"能到达目标的路径"流过去——和上篇 2×2 的禁区一模一样,"有价值"的是离目标的距离,不是格子本身。
- **决策靠 Q 这一点完全没变**:MC 只是换了"$Q$ 从哪来"(数据 vs 模型),没换"决策怎么做"($\arg\max_a q$)。`,
}

export default project
