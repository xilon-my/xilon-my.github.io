const project = {
  slug: 'monte-carlo-gridworld',
  date: '2026-08-19 15:00',
  name: 'Model-Free Monte Carlo in a 3×3 Grid World',
  url: 'https://github.com/MathFoundationRL/Book-Mathematical-Foundation-of-Reinforcement-Learning',
  description: '前一篇用 2×2 grid world 把策略迭代、价值迭代完整走了一遍——它们都要模型(已知 P 和 R)。这一篇是第 5 课:无模型的蒙特卡洛。没有模型怎么办?用数据。继续用一套完全相同的 grid world 套路,在 3×3 上把 MC Basic、MC Exploring Starts、MC ε-Greedy 三个算法过一遍:期望用样本均值近似,样本用一条 episode 走到底,探索用 ε 买.',
  tags: ['RL'],
  author: 'shannon',
  takeaway: '没有模型,就用数据:MC Basic 把策略迭代里"解期望方程"换成"样本均值近似",从精确变成近似;Exploring Starts 让一条 episode 里每个被访问的 (s,a) 都贡献一次估计,把数据用透;ε-Greedy 用软策略买探索,代价是牺牲最优性。三条算法层层加码,核心始终是行动值 q 和它的 argmax.',
  detail: String.raw`

## 1. 一个具体的 3×3 World:这次没有模型

![3×3 grid world 地图与初始策略 π0](/discover/mc-3x3-map.png)

规则沿用上一篇:

- 状态 $s_1,\ldots,s_9$(图中格子),共 9 个
- **目标** $s_9$:每一步结束时**停在它上面 +1**
- **禁区** $s_6$:每一步结束时**停在它上面 -1**
- **撞墙**(想走出界):**-1**
- 正常移动到其它格子:**0**
- **没有终止状态**:游戏不结束
- 动作 5 个: up / right / down / left / stay;撞墙(出界)→ 原地不动,但算撞墙、-1
- 折扣因子 $\gamma = 0.9$
- 初始策略 $\pi_0$:图里黄色箭头(在 $s_1$、$s_3$ 指向撞墙,是个**不好的**起点)

> **与前一篇唯一的区别:模型没了。** 前一篇的所有数字都是"知道 $P(s'|s,a)$ 和 $R(s,a)$ 才算得出来"的。这一篇我们假设**转移概率和奖励都未知**,手里只有智能体和环境交互产生的"数据"——一条条 $(s,a,r,s')$ 组成的轨迹,叫 **episode(回合)**。奖励约定和前一篇一样(定义在转移上:先看撞墙,再看落在哪个格子),但我们**故意不把这套规则告诉算法**,它只能从数据里猜。

## 2. 蒙特卡洛的核心思想:期望,用样本均值估计

先看一个最基本的估计问题。随机变量 $X$,取值有限,想算它的期望 $\mathrm{E}[X]$,有两种做法:

- **有模型**:知道 $X$ 的分布 $p(x)$,直接 $\mathrm{E}[X]=\sum_x p(x)\,x$。
- **无模型**:不知道分布,但有一批样本 $\{x_1,\ldots,x_n\}$。用样本均值近似期望 $\mathrm{E}[X]\approx\bar{x}=\frac{1}{n}\sum_{j=1}^{n}x_j$,$n$ 越大越准,当 $n\to\infty$ 时 $\bar{x}\to\mathrm{E}[X]$——这就是**大数定律**。

抛硬币:硬币落地正面 $X=1$、反面 $X=-1$,真实分布 $p(X{=}1){=}p(X{=}{-}1){=}0.5$。有模型直接算 $\mathrm{E}[X]=0.5\cdot1+0.5\cdot(-1)=0$;没模型就多抛几次取平均,100 次、1000 次……平均值往 0 靠。**这就是蒙特卡洛:用随机样本解决估计问题。**

样本还有个要求:要**独立同分布**(i.i.d.)。如果每次抛出的结果都和第一次强相关(极端情况:全一样),那无论抛多少次,平均永远等于第一个样本,学不到真期望。

## 3. MC Basic:数据版策略迭代

行动值的定义本身就是一个期望:

$$
q_\pi(s,a) = \mathrm{E}\big[\,G_t \mid s_t=s,\,a_t=a\,\big],\qquad G_t = r_t + \gamma r_{t+1} + \gamma^2 r_{t+2} + \cdots
$$

有模型时,前一篇的策略迭代分两步解决它:策略评估(解 $V_\pi=r_\pi+\gamma P_\pi V_\pi$,再由 $q_\pi(s,a)=R(s,a)+\gamma\sum_{s'}P(s'|s,a)V_\pi(s')$ 转成 $q$)、策略改进(贪心)。**问题就出在从 $V$ 转 $q$ 的那条公式上——里面有 $P$,没有模型就转不动。**

所以 MC Basic 换一条路:**直接用数据估计 $q_\pi(s,a)$**。策略迭代那两步变成:

**策略评估(用数据)**:从 $(s,a)$ 出发按 $\pi_k$ 走,收集 $n$ 条 episode,记第 $i$ 条的回报为 $g_{\pi_k}^{(i)}(s,a)$,取平均:

$$
q_{\pi_k}(s,a) \approx \frac{1}{n}\sum_{i=1}^{n} g_{\pi_k}^{(i)}(s,a)
$$

由大数定律,$n$ 足够大时这个平均接近真实行动值。

**策略改进(和以前一样)**:$\pi_{k+1}(s)=\arg\max_a q_k(s,a)$。

> 注意 MC Basic 是**直接估计行动值**。它不能先去估计状态值 $V$ 再转 $q$——那条公式要模型。数据给的是 $(s,a)$ 的回报,直接落在 $q$ 上。

这就是"数据版策略迭代":只要每轮能把 $q_{\pi_k}$ 估准,后面就和策略迭代一样收敛。MC Basic 是三条算法里最朴素的一个,刻意不用任何技巧,专门用来把"用数据代替模型"讲清楚。

## 4. 例子:把 s1 的五个动作全算一遍

我们的环境是确定性的:同一个 $(s,a)$ 永远走同一条轨迹,多次采样结果相同,所以**每个 $(s,a)$ 收一条 episode 就够**。对 $s_1$ 的 5 个动作,各从 $(s_1,a)$ 出发、按 $\pi_0$ 走下去:

| 动作 | episode 内容(按 $\pi_0$ 走下去) | 折扣回报 $G$ | $q_{\pi_0}(s_1,a)$ |
|---|---|---|---|
| up | $s_1 \to s_1 \to s_1 \to\cdots$(每次撞墙 $-1$) | $-1 - 0.9 - 0.9^2 - \cdots = \dfrac{-1}{1-\gamma}$ | **-10** |
| right | $s_1 \to s_2 \to s_5 \to s_8 \to s_9 \to s_9 \to\cdots$ | $0+0+0+\gamma^3(1+0.9+0.9^2+\cdots)=\dfrac{\gamma^3}{1-\gamma}$ | **7.29** |
| down | $s_1 \to s_4 \to s_5 \to s_8 \to s_9 \to s_9 \to\cdots$ | 同上 | **7.29** |
| left | $s_1 \to s_1 \to\cdots$(撞墙 $-1$) | $\dfrac{-1}{1-\gamma}$ | **-10** |
| stay | $s_1 \to s_1 \to s_1 \to\cdots$(先 $0$,然后 $\pi_0$ 在 $s_1$ 撞墙 $-1$) | $0 - \gamma - \gamma^2-\cdots = \dfrac{-\gamma}{1-\gamma}$ | **-9** |

逐行对一遍:up/left 一出发就撞墙,整条回报全是 $-1$,几何级数收敛到 $-10$;right 走 $s_1\to s_2\to s_5\to s_8$ 三步都是 $0$(撞墙 $0$ 次),第 4 步进目标 $s_9$ 拿 $+1$、之后 stay 每步 $+1$,所以从 $\gamma^3$ 开始累加,得 $\gamma^3/(1-\gamma)=7.29$;down 走另一条路也在第 4 步进目标,同样 $7.29$;stay 先原地 $0$、再被 $\pi_0$ 拖去撞墙,得 $-\gamma/(1-\gamma)=-9$。

比较五个估计值:

![q_π0(s1,·) 的估计](/discover/mc-q-s1.png)

$\arg\max$ 是 **right / down(7.29)** → 新策略 $\pi_1(s_1)=\text{right}$ 或 $\text{down}$。而 $\pi_0$ 在 $s_1$ 是 up(撞墙)。**只看 5 条 episode,MC Basic 就把 $s_1$ 从"撞墙"改成了"朝目标走"。**

课程里同样算过 $s_3$ 的五个动作:up:-10、right:-10、down:8、left:7.29、stay:-9 → $\pi_1(s_3)=\text{down}$。有意思的是 down 要先踩一步禁区($-1$)再进目标($+1$),算下来 $8$,比绕远路的 left($7.29$)还高。这个简单的例子里,**迭代一次就得到最优策略**;复杂问题需要多轮。

持续迭代、把估计做准之后,收敛到最优价值和最优策略:

![V* 与最优策略](/discover/mc-vstar-policy.png)

从 $s_1$ 出发:**right → $s_2$,down → $s_5$,down → $s_8$,right → $s_9$ 目标,然后 stay 待着,每步 +1**。注意禁区 $s_6$ 的价值也是 $10$——它一步 down 就能逃进目标,所以"有价值"的不是格子本身,而是它离目标的距离。

## 5. MC Basic 的软肋:没被访问的 (s,a)

MC Basic 有个隐藏前提:要对每个 $(s,a)$ 都收集足够多 episode。如果某个 $(s,a)$ **从没被访问过**,它的 $q$ 停留在初始值,改进时可能被跳过——哪怕它其实是最优动作。

它还有个更基础的低效:**一条 episode 只用来估计它开头的那个 $(s,a)$**(首次访问)。比如上一节那条轨迹 $s_1\to s_2\to s_5\to s_8\to s_9$,只贡献给了 $(s_1,\text{right})$ 的估计——可 $s_2$、$s_5$、$s_8$、$s_9$ 明明也在这条轨迹里,它们的行动值被白白浪费了。

## 6. MC Exploring Starts:把一条 episode 用到底

把上面两个问题各修一半,就得到第二个算法。

**修样本利用**:一条 episode 里的**每次访问**都算数。轨迹 $s_1\to s_2\to s_5\to s_8\to s_9$ 可以拆成五条子-episode,每条子-episode 估计一个 $(s,a)$:

![一条 episode 拆成五条子-episode](/discover/mc-episode-decomp.png)

具体做法是从后往前"回溯"算 return:先算最后访问的 $(s,a)$ 的 $G$,再逐步往前累加,一条 episode 一次扫描就能给所有被访问的 $(s,a)$ 各贡献一个样本。这就是**每次访问(every visit)**,对应的朴素做法叫首次访问(first visit)。一个 $(s,a)$ 在轨迹里出现几次就用几次(它本身是一种重复访问)。

**修更新效率**:MC Basic 要等某个 $(s,a)$ 的所有 episode 收齐、平均完,才更新一次策略。这里改成**收一条就更新一次**——单个 episode 的 return 当然不准,但没关系,和"截断策略迭代"一个道理:估不准也能往前走。

代价是加了一个条件——**Exploring Starts 条件**:要能估计出每个 $(s,a)$,必须保证每个 $(s,a)$ 都被足够多次访问。一种保证办法是让每个 $(s,a)$ 都有可能成为回合起点("探索性起始")。但这个条件在真实环境里常常很难满足:你不能让 agent 凭空从任意 $(s,a)$ 开始。于是有了第三个算法。

## 7. MC ε-Greedy:用软策略买探索

如果一个策略是**软策略**——每个状态、每个动作都有非零概率被选到——那么一条足够长的 episode 就能访问到所有 $(s,a)$,不再需要"从每个 $(s,a)$ 出发"了。Exploring Starts 条件被自动绕过。

最常见的软策略是 $\varepsilon$-Greedy。设 $a^*=\arg\max_a q(s,a)$,$|\mathcal{A}(s)|$ 是该状态的动作数:

$$
\pi(a|s) = \begin{cases} 1-\varepsilon+\dfrac{\varepsilon}{|\mathcal{A}(s)|}, & a = a^* \\[6pt] \dfrac{\varepsilon}{|\mathcal{A}(s)|}, & a \neq a^* \end{cases}
$$

采样时:先按均匀分布抽 $x\in[0,1]$,若 $x<\varepsilon$ 就均匀随机选一个动作,否则选贪心动作。选到贪心动作的总概率是 $1-\varepsilon+\frac{\varepsilon}{|\mathcal{A}(s)|}$,其它动作各 $\frac{\varepsilon}{|\mathcal{A}(s)|}$。以 $s_1$、$\varepsilon=0.2$、5 个动作为例:贪心动作(right)概率 $1-0.2+0.04=0.84$,其余四个各 $0.04$。

把 MC Exploring Starts 里"策略改进选贪心"换成"选 $\varepsilon$-Greedy",就得到 **MC $\varepsilon$-Greedy 算法**,不再依赖 Exploring Starts 条件。代价是:**策略被限制在 $\varepsilon$-Greedy 这个集合里**,只能保证是该集合中的最优,而不一定是全体策略中的最优——$\varepsilon$ 越大,离真正的最优越远,但探索越充分。

这就是强化学习里最重要的权衡——**探索与利用(exploration vs exploitation)**:探索是"多试动作,才能把每个动作评估好";利用是"多用当前看起来最好的动作,才能拿到高回报"。$\varepsilon$ 是个旋钮:大 $\varepsilon$ 探索强、最优性差;小 $\varepsilon$ 利用强、探索弱。实际应用里常用"一开始 $\varepsilon$ 大,后面逐渐调小"。

## 8. 回合太短,目标根本看不到

MC 的数据是"完整的回合"。但**回合长度本身就会影响结果**——这背后是稀疏奖励(sparse reward)问题。

我们的回报只在到达目标 $s_9$ 之后才变成正的。从 $s_1$ 出发到 $s_9$ 至少要走 4 步($s_1\to s_2\to s_5\to s_8\to s_9$)。如果回合长度不足 4,这条 episode 里永远看不到 $+1$,回报全是 0——数据收了等于白收。

用 MC 的视角看,回合长度 $L$ 决定"价值能往回传几步":

| 回合长度 L | 谁拿到非零价值(能被 MC 看到) |
|---|---|
| 1 | 只有 $s_9$(目标)和 $s_6$、$s_8$(一步就能进目标) |
| 2 | 加上 $s_5$、$s_7$(两步内到目标) |
| 3 | 加上 $s_2$、$s_3$、$s_4$(三步内到目标) |
| ≥4 | 全部,包括最远的 $s_1$ |

而且"看得到"不等于"估得准":$L=4$ 时 $s_1$ 的价值估计才 $0.73$(截断在进目标的那一步),而真实最优值是 $7.29$。**回合越长,估计越接近收敛值。** 真实 RL 任务里这更严重:状态空间大、随机性强时,一条回合里碰到目标的概率很低,数据里全是 0,学习效率极低。一个常用解法是**稠密奖励**——靠近目标就发小奖励,在目标周围造一个"吸引场",而不是只有踩中目标才 +1。

## 9. 小结:三条算法一条线

- **MC Basic** = 策略迭代把"解方程求期望"换成"样本均值求期望"。核心思想:期望可以用数据估计。
- **MC Exploring Starts** = MC Basic + 每次访问(一条 episode 拆多条子-episode)+ 收一条就更新。代价:需要满足 Exploring Starts 条件。
- **MC $\varepsilon$-Greedy** = MC Exploring Starts + 贪心换成 $\varepsilon$-Greedy。软策略自带探索,去掉 Exploring Starts 条件。代价:$\varepsilon$ 牺牲最优性。

三条算法层层递进,一条主线:**用数据代替模型**。以及两个现实约束:样本要够(访问要覆盖所有 $(s,a)$)、回合要够长(数据里要能看到目标)。

值得注意的三点:

- **行动值 $q$ 永远是核心**:有模型时可以解 $V$ 再转 $q$;没模型时数据直接落在 $q$ 上——两条路绕不开的中间量是 $q$,改进永远是 $\arg\max_a q$。
- **确定性环境一条 episode 就够**:因为反复采样结果相同;随机环境必须多条平均(样本 i.i.d.),还要靠 $\varepsilon$ 之类保证访问覆盖。
- **"有价值"的是离目标的距离,不是格子本身**:禁区 $s_6$ 的最优价值 $V_*(s_6)=10$ 和目标格一样,因为它一步 down 就能逃进目标。这和上一篇 2×2 里 $(0,1)$ 禁区有价值 10 完全同理——**价值顺着"能到达目标的路径"流过去,哪怕起点在禁区**。`,
}

export default project
