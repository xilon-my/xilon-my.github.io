const project = {
  slug: 'stochastic-approx-sgd',
  date: '2026-08-19 16:00',
  name: 'Stochastic Approximation: From Iterative Mean to SGD',
  url: 'https://github.com/MathFoundationRL/Book-Mathematical-Foundation-of-Reinforcement-Learning',
  description: '第 6 课:随机近似与随机梯度下降。这一课不是新的强化学习算法,而是数学工具课——回答"来一个样本更新一次"的增量式算法为什么能收敛。用同一个"估计均值"的例子,把迭代均值估计、Robbins-Monro(RM)算法、随机梯度下降(SGD)串成一条线:RM ⊃ SGD ⊃ 均值估计。顺便看清 BGD / MBGD / SGD 的区别,以及它们为什么是第 7 章时序差分(TD)的铺垫.',
  tags: ['RL'],
  author: 'shannon',
  takeaway: '三个算法,一个例子,一条线:RM 求黑盒方程 g(w)=0 的根,只要输入输出、带噪声也行;SGD 是 RM 的特例(真实梯度当 g、随机梯度是它的噪声观测);迭代均值估计又是 SGD 的特例(f(w,x)=½(w-x)²)。收敛全靠一条"温和条件":Σa_k=∞ 且 Σa_k²<∞——步长要慢慢缩小,但不能缩太快。',
  detail: String.raw`

## 1. 一个具体的例子:估计均值

考虑一个随机变量 $X$,想估计它的期望 $\mathrm{E}[X]$。手里只有独立同分布(i.i.d.)的样本序列 $\{x_1,x_2,\ldots\}$,**不知道分布**。

估计期望有两个姿势:

- **非增量式(non-incremental)**:先把所有样本收齐,再算平均 $\bar{x}=\frac{1}{n}\sum_{i=1}^{n}x_i$。缺点:数据多时要等很久,而且必须等收齐才能算一次。
- **增量式(incremental)**:来一个样本就更新一次估计,边收边算。

这篇文章用一组具体的样本贯穿全文:

$$
x_1=3,\quad x_2=-1,\quad x_3=2,\quad x_4=5,\quad x_5=1
$$

真实均值是 $2$,但我们假设不知道——后面的每个数字都从这 5 个样本算出来。

> 第 5 课的蒙特卡洛也是用样本均值估计期望,但那是"收齐一批、一次算完"。这一课换个角度:增量地、一步步逼近。别小看这个区别——第 7 章的时序差分(TD)走的就是增量路线。

## 2. 增量式均值估计:来一个更新一个

非增量式很简单:5 个样本收齐,平均 $(3-1+2+5+1)/5=2$,一步到位,但要等全部数据。

增量式想这样:已经有了"前 $k$ 个样本的均值" $w_k$,来一个新样本 $x_{k+1}$,能不能不动用之前的数据,只靠 $w_k$ 和 $x_{k+1}$ 算出"前 $k+1$ 个样本的均值" $w_{k+1}$?可以,推一下:

$$
w_{k+1} = \frac{1}{k+1}\sum_{i=1}^{k+1}x_i
= \frac{1}{k+1}(\,k\,w_k + x_{k+1}\,)
= w_k - \frac{1}{k+1}(w_k - x_{k+1})
$$

**来一个样本,就把估计往新样本的方向挪 $1/(k+1)$ 那么一小步。** 手算验证(和"直接平均"逐行对照):

| 新样本 | 增量式更新 | 新估计 $w_{k+1}$ | 直接平均 |
|---|---|---|---|
| $x_1=3$ | $w_1 = x_1$ | 3 | 3 |
| $x_2=-1$ | $w_2 = 3 - \frac{1}{2}(3-(-1))$ | 1 | $\frac{3-1}{2}=1$ |
| $x_3=2$ | $w_3 = 1 - \frac{1}{3}(1-2)$ | $\frac{4}{3}\approx1.33$ | $\frac{3-1+2}{3}=\frac{4}{3}$ |
| $x_4=5$ | $w_4 = \frac{4}{3} - \frac{1}{4}(\frac{4}{3}-5)$ | $\frac{9}{4}=2.25$ | $\frac{3-1+2+5}{4}=\frac{9}{4}$ |
| $x_5=1$ | $w_5 = \frac{9}{4} - \frac{1}{5}(\frac{9}{4}-1)$ | 2 | $\frac{3-1+2+5+1}{5}=2$ |

每一行,增量式算出来的都和直接平均一模一样——**增量式没有丢任何信息,只是不用等所有样本**。每收一个样本,估计就往真均值 2 挪一步:

![增量式均值估计收敛到 2](/discover/sa-incr-mean.png)

把系数 $1/(k+1)$ 推广成一般正数 $\alpha_k$:

$$
w_{k+1} = w_k - \alpha_k\,(w_k - x_{k+1})
$$

$\alpha_k$ 给不出来时,就没有 $w_k$ 的解析式了。它还收敛吗?下一节的主角——Robbins-Monro 算法——就是来回答这个问题的。

## 3. 为什么增量式能收敛?Robbins-Monro 算法

先把"估计均值"抽象成一个求根问题。解方程

$$
g(w) = w - \mathrm{E}[X] = 0
$$

的根就是 $\mathrm{E}[X]$。但 $g$ 是个**黑盒**:输入 $w$,只能拿到带噪声的输出

$$
\tilde{g}(w,\eta) = g(w) + \eta
$$

其中 $\eta$ 是观测噪声(可能不是高斯分布)。这种"只认输入输出、结构未知"的函数,正是神经网络的样子——我们不知道它的表达式,只能喂 $w$、看输出。

**Robbins-Monro(RM)算法**(1951)解决的就是这个问题:

$$
w_{k+1} = w_k - a_k\,\tilde{g}(w_k,\eta_k)
$$

RM 不需要 $g$ 的任何解析信息,只要反复"输入 $w$ → 拿到噪声观测 → 更新 $w$"。**每次挪的方向是 $g(w_k)$ 的相反方向:如果观测值为正,说明 $w_k$ 太大,往下挪;为负就往上挪。**(把步长 $a_k$ 记成 $\alpha_k$,你会发现它和第 2 节的系数是同一个角色。)

课程的例子:$g(w)=w^3-5$,根是 $5^{1/3}\approx1.71$。我们不知道 $g$ 的表达式,只能输入 $w$、得到 $g(w)+$ 标准差为 1 的正态噪声。从 $w_1=2$ 出发:

![RM 求解 w³-5](/discover/sa-rm-w3.png)

每步观测都带噪声,估计 $w_k$ 还是晃晃悠悠地收敛到 $1.71$。

**收敛直觉**(为什么"往观测的反方向挪"就行):设 $g$ 单调递增,根 $w^*$ 满足 $g(w^*)=0$。那么

- 当 $w_k>w^*$:有 $g(w_k)>0$,$w_{k+1}=w_k-a_k g(w_k)<w_k$,往下走,更靠近 $w^*$;
- 当 $w_k<w^*$:有 $g(w_k)<0$,$w_{k+1}>w_k$,往上走,也更靠近 $w^*$。

只要步长 $a_k\,g(w_k)$ 足够小,每次都比上一步更接近根。

## 4. RM 定理:收敛靠"温和条件"

严格的收敛条件(RM 定理)有三条:

1. **$0<c_1\le\nabla_w g(w)\le c_2$**:$g$ 单调递增(根存在且唯一),斜率有界(不能无限陡)。
2. **$\sum_{k=1}^{\infty}a_k=\infty$ 且 $\sum_{k=1}^{\infty}a_k^2<\infty$**:这是核心,课程叫"温和条件"。
3. **$\mathrm{E}[\eta_k|\mathcal{H}_k]=0$ 且 $\mathrm{E}[\eta_k^2|\mathcal{H}_k]<\infty$**:噪声零均值、方差有限(不要求高斯!)。

条件 2 最有味道,拆开看:

- **$\sum a_k^2<\infty$ 要求 $a_k\to 0$**:因为 $w_{k+1}-w_k=-a_k\tilde{g}(w_k,\eta_k)$,如果 $a_k$ 不缩到 0,$w_k$ 永远在跳、停不下来。步长必须**缩得够快**。
- **$\sum a_k=\infty$ 要求 $a_k$ 不能缩太快**:把每一步加起来,$w_1-w_\infty=\sum_{k=1}^{\infty}a_k\tilde{g}(w_k,\eta_k)$。若 $\sum a_k$ 有限,右边有上界 $b$,那么初始 $w_1$ 一旦离根比 $b$ 还远,就永远够不着根。步长必须**缩得够慢**。

典型的满足条件 2 的序列是 $a_k=1/k$:调和级数 $\sum 1/k=\infty$($H_n$ 是调和数),而平方和 $\sum 1/k^2=\pi^2/6<\infty$(巴塞尔问题)。**步长要慢慢缩小——保证能走到根;但不能缩太快——保证能停下来。**

> **反例就是 $w^3-5$**:$\nabla_w g=3w^2$ 没有上界,不满足条件 1。所以 $a_k=1/k$ 时初值稍不合适就直接发散(从 $w_1=2$ 出发,第 7 步 $w$ 就爆到上万)。不是算法错了,是条件不满足。所以上面那张图用了更稳的 $a_k=0.5/k$——把第一步的步长压小,就能救回来。

> **实践里的取舍**:常把 $a_k$ 取成一个小的常数(如 0.1)。此时 $\sum a_k^2<\infty$ 不再成立,严格收敛保证没了,但好处是后面的数据还"有份量",能应对环境缓慢变化($g$ 在缓慢移动)。用收敛性换适应性。

## 5. 回头看:均值估计 = RM 的特例

把第 2 节的均值估计套进 RM:

$$
g(w) = w - \mathrm{E}[X],\qquad \tilde{g}(w,\eta) = w - x
$$

其中 $x$ 是 $X$ 的一个随机样本。验证它确实"等于 $g$ 加零均值噪声":

$$
\tilde{g}(w,\eta) = w - x = \big(w-\mathrm{E}[X]\big) + \big(\mathrm{E}[X]-x\big) = g(w) + \eta,\qquad \eta=\mathrm{E}[X]-x
$$

因为 $\mathrm{E}[x]=\mathrm{E}[X]$,所以 $\mathrm{E}[\eta]=0$。✓

RM 更新变成:

$$
w_{k+1} = w_k - a_k\,(w_k - x_k)
$$

**正是第 2 节的增量式均值估计。** 也就是说:均值估计问题 = 求 $g(w)=w-\mathrm{E}[X]$ 的根;增量式均值算法 = 对这个 $g$ 跑 RM。第 2 节的直觉(来一个更新一个)和这里的收敛定理(温和条件)对上了。而且收敛不依赖 $X$ 的分布假设——只要方差有限。

## 6. SGD:随机梯度下降

现在来最常用的一个。考虑优化问题:

$$
\min_w J(w) = \mathrm{E}\big[\,f(w,X)\,\big]
$$

常规**梯度下降**需要真实梯度:

$$
w_{k+1} = w_k - \alpha_k\,\nabla_w J(w_k) = w_k - \alpha_k\,\mathrm{E}\big[\nabla_w f(w_k,X)\big]
$$

但期望难算:要么不知道 $X$ 的分布,要么样本太多用不过来。**随机梯度下降(SGD)**的办法:用单个样本的梯度 $\nabla_w f(w_k,x_k)$ 代替真实梯度:

$$
w_{k+1} = w_k - \alpha_k\,\nabla_w f(w_k,x_k)
$$

为什么"一个样本的梯度"也能用?把它拆开:

$$
\nabla_w f(w_k,x_k) = \underbrace{\mathrm{E}[\nabla_w f(w_k,X)]}_{\text{真实梯度}} + \underbrace{\big(\nabla_w f(w_k,x_k)-\mathrm{E}[\nabla_w f(w_k,X)]\big)}_{\eta_k}
$$

样本 i.i.d.,所以 $\mathrm{E}[\eta_k]=0$——**SGD = 梯度下降 + 零均值噪声**。噪声不偏不倚,平均起来不碍事。

这也说明 **SGD 就是 RM 的特例**:把 RM 里的 $g$ 取成真实梯度 $\mathrm{E}[\nabla_w f(w,X)]$,随机梯度 $\nabla_w f(w_k,x_k)$ 就是它的噪声观测。

**均值估计 = SGD 的特例**:把均值问题写成优化问题

$$
\min_w J(w) = \mathrm{E}\!\left[\tfrac{1}{2}\|w-X\|^2\right],\qquad f(w,X)=\tfrac{1}{2}\|w-X\|^2,\quad \nabla_w f(w,X)=w-X
$$

解 $\nabla_w J(w)=0$ 得 $w^*=\mathrm{E}[X]$——正是均值。对它跑 SGD:

$$
w_{k+1} = w_k - \alpha_k\,(w_k - x_k)
$$

**又是它。** 三个算法在这里汇合,更新公式长得一模一样,只是出发的角度不同:RM 从"解方程"出发,SGD 从"优化目标"出发,均值估计是最简单的具体例子。

## 7. SGD 的有趣性质:远则稳,近则抖

"随机梯度 = 真实梯度 + 噪声",听起来很随机。但 SGD 的收敛其实很有纪律。定义随机梯度的**相对误差**:

$$
\delta_k = \frac{\big|\nabla_w f(w_k,x_k)-\mathrm{E}[\nabla_w f(w_k,X)]\big|}{\big|\mathrm{E}[\nabla_w f(w_k,X)]\big|}
$$

对均值估计,$\nabla_w f(w,x)=w-x$、$\mathrm{E}[\nabla_w f(w,X)]=w-\mathrm{E}[X]=w-w^*$,于是

$$
\delta_k = \frac{|x_k-\mathrm{E}[X]|}{|w_k-w^*|}
$$

分子是样本的波动(由 $X$ 的方差决定,固定),分母是"现在离最优解还有多远"。

- **$w_k$ 离 $w^*$ 远**:分母大,$\delta_k$ 小。随机梯度 ≈ 真实梯度,SGD 表现得像普通梯度下降——**大步、稳定地朝 $w^*$ 冲**。
- **$w_k$ 靠近 $w^*$**:分母小,$\delta_k$ 大。随机噪声主导,SGD 开始**抖动**(在 $w^*$ 附近晃悠)。

算一下:设 $x_k=3$、$\mathrm{E}[X]=2$。当 $w_k=8$(远)时 $\delta=\frac{1}{6}\approx0.17$,稳;当 $w_k=2.5$(近)时 $\delta=\frac{1}{0.5}=2$,抖。**所以 SGD 的轨迹是"远处很干净、近处开始抖"**——这不是缺陷,是单个随机样本的固有代价;想少抖,就用更多样本(小批量)或更小的学习率。

## 8. BGD / MBGD / SGD:批量越大,越接近"一步到位"

同样是解 $\min_w J(w)=\mathrm{E}[f(w,X)]$,手里的样本 $\{x_1,\ldots,x_n\}$ 怎么用,分出三个算法:

$$
\begin{aligned}
&w_{k+1}=w_k-\alpha_k\frac{1}{n}\sum_{i=1}^{n}\nabla_w f(w_k,x_i), && \mathrm{(BGD)}\\
&w_{k+1}=w_k-\alpha_k\frac{1}{m}\sum_{j\in\mathcal{I}_k}\nabla_w f(w_k,x_j), && \mathrm{(MBGD)}\\
&w_{k+1}=w_k-\alpha_k\,\nabla_w f(w_k,x_k), && \mathrm{(SGD)}
\end{aligned}
$$

三种算法的差别只在"每步用几个样本":BGD 用全部 $n$ 个、MBGD 用随机 $m$ 个、SGD 用 1 个。

对均值估计($f=\tfrac{1}{2}\|w-x\|^2$、$\alpha_k=1/k$)三种算法长这样:

- **BGD**:$w_{k+1}=w_k-\frac{1}{k}(w_k-\bar{x})$,$\bar{x}$ 是全部样本均值。**第一步就一步到位**:$w_2=0-\frac{1}{1}(0-2)=2$。
- **MBGD**:$w_{k+1}=w_k-\frac{1}{k}(w_k-\bar{x}^{(m)}_k)$,其中 $\bar{x}^{(m)}_k$ 是随机抽的 $m$ 个样本的均值。它近似 $\bar{x}$,几步后到 2。
- **SGD**:$w_{k+1}=w_k-\frac{1}{k}(w_k-x_k)$,单个样本,要多步才接近 2,而且一直在抖。

![BGD vs MBGD vs SGD](/discover/sa-bgd-sgd.png)

| 算法 | 每步用样本数 | 梯度质量 | 均值估计收敛 |
|---|---|---|---|
| BGD | $n$(全部) | 最接近真实 | 一步到位 $2$ |
| MBGD | $m$(小批量) | 中等 | 几步到 $2$ |
| SGD | 1 | 噪声最大 | 多步、且抖 |

MBGD 是中间态:批量大,每步准但贵;批量小,每步便宜但噪。两个边界:若 $m=1$,MBGD 就退化成了 SGD;若 $m=n$,MBGD 仍不完全等于 BGD——MBGD 是随机抽取的 $n$ 个样本(可能重复),BGD 是把 $\{x_i\}$ 全部用一遍。实践里 MBGD 是常态(深度学习的"batch"就是它)。

## 9. 小结:一条线串起三个算法

这一课不是新的强化学习算法,是数学工具。一条线:

- **Robbins-Monro**:求黑盒方程 $g(w)=0$ 的根,只要输入输出、带噪声也能收敛。
- **SGD** = RM 的特例:真实梯度当 $g$,随机梯度是它的噪声观测。
- **增量式均值估计** = SGD 的特例:$f(w,x)=\frac{1}{2}(w-x)^2$。

三个算法的更新公式是同一个 $w_{k+1}=w_k-\alpha_k(w_k-x_k)$,只是出发点不同。

收敛的核心是那条"温和条件":$\sum a_k=\infty$(**步长别缩太快**,保证走得到根)且 $\sum a_k^2<\infty$(**步长要缩到 0**,保证停得下)。噪声只要零均值、方差有限,不需要高斯——RL 里的"噪声"远非高斯,这个条件很现实。

为什么要学这些?第 7 章的**时序差分(TD)**更新长得和 $w_{k+1}=w_k-\alpha_k(w_k-x_k)$ 几乎一样。现在再看到那个公式就不会觉得陌生了——它就是"来一个样本,把估计往目标挪一点"。增量式、来一个更新一个,是 TD 的基因。`,
}

export default project
