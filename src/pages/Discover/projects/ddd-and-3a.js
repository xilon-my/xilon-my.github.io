const project = {
  slug: 'ddd-and-3a',
  date: '2026-08-14 10:00',
  name: 'Domain-Driven Design: 域、子域、限界上下文,再到 3A 架构',
  url: 'https://martinfowler.com/bliki/DomainDrivenDesign.html',
  url2: 'https://www.infoq.cn/article/4UZyz4FL36xf4BhQM0mv',
  description: '一家外卖公司的订单出了事故,三个人嘴里的"订单"却是三个东西——这既是 DDD 的出发点,也是企业架构的出发点。文章从域、子域、限界上下文、模块讲到聚合和服务,再到微服务拆分,最后往上到企业架构,顺带拆穿"3A 架构":它不是 DDD 的术语,而是企业架构圈"业务+数据+应用"的民间简称。',
  tags: ['Architecture'],
  author: 'Shannon',
  takeaway: 'DDD 从头到尾只回答一个问题:边界在哪。往下,值对象、聚合、限界上下文划出"什么和什么必须长在一起";往上,企业架构的业务/数据/应用/技术划出"整个公司怎么对齐战略"。3A 不是 DDD 的术语,它是企业架构圈的民间简称——业务+数据+应用(技术当底座砍掉);如果你所在的公司内部叫"业务/系统/技术",那只是把数据+应用并成了"系统",同一个东西的另一种切法。DDD 管的是一个个限界上下文,企业架构管的是它们怎么拼成一个公司:DDD 消费业务架构的产物,而不是生产它们。',
  detail: `一家外卖公司出了个事故:线上订单大面积卡住。开复盘会,产品说"订单没有支付成功",履约的同事说"订单早就派给骑手了",财务说"订单的钱根本没到账"。三个人嘴里的"订单"都不是一个东西——一个说的是金额和优惠券,一个说的是骑手和地址,一个说的是流水和账期。谁都没错,只是每个人站在不同的边界里。

这篇文章要把"边界到底画在哪"这件事讲清楚,从代码里的一个对象,到整个公司的架构。DDD(领域驱动设计)是这套东西的地基。

## 1. DDD 是什么

DDD 的主张一句话:软件难的地方不是框架、数据库、网络这些技术活,而是把"这家公司到底怎么运转"讲明白,并且让代码长成和业务一致的样子。埃里克·埃文斯(Eric Evans)2003 年出的蓝皮书《领域驱动设计:软件核心复杂性应对之道》——书名副标题已经点题:**软件复杂性的核心在领域逻辑,不在技术框架**。

书里说的"模型"不是画几张 UML 图。Martin Fowler 的定义:模型是对领域过程和规则的**丰富理解**;DDD 要求把这个模型直接做进代码里、随产品一起演进,而不是留在纸上。模型同时是三样东西:业务概念的形式化、和领域专家沟通的通用语言、代码划分的依据。

所以 DDD 不是一套分层脚手架(controller/service/dao 模板),也不是"从数据库表出发设计"——它恰恰是批评以数据表为中心的老办法才出现的。它也不绑定语言和部署形态,单体里能用,微服务里也能用。

## 2. 域:问题空间

DDD 的第一个词是"域",也叫领域。人话:领域就是**这家公司/这个行业到底在干什么**的全部——所有业务规则、流程、参与者,合起来就是领域。它是问题空间(业务现实),在软件之外;软件是来解决这个领域里的问题的。

外卖公司的领域是整摊外卖生意:顾客怎么选餐、商家怎么接单、骑手怎么取餐送餐、超时怎么赔、退款怎么走、补贴怎么发——这些规则和流程合起来就是领域。注意两个点:领域比任何单个系统都大,一家公司往往横跨好几个领域(外卖之外还有账务、风控);领域也不等于需求文档——领域是客观存在的业务现实,需求文档只是对它的不完整转述,这正是为什么 DDD 要求写代码的人整天围着业务方问。

## 3. 子域:按业务能力切

领域太大,没法整体分析,就按业务能力把它切成一块块,每块叫一个**子域**。怎么切基本由业务本身决定,不是开发想怎么切就怎么切。切完给每块贴个标签——这块是不是让公司赚钱、有竞争力的东西?分三类:

- **核心子域**:让公司区别于竞品、能赚到钱的地方,建模精力砸在这。
- **支撑子域**:重要但不差异化;找不到现成方案只能自己定制开发,离了它核心子域也转不起来。
- **通用子域**:谁都有的通用问题,直接买现成方案、用开源、外包(登录、短信、支付)。

判据就一个决策树:**这是核心竞争力吗?** 是,核心子域。不是,那外面有现成方案吗?有,通用子域;没有,支撑子域。一个反直觉点:这家公司的通用子域,可能是另一家供应商的核心子域——这就是为什么能买到现成方案。

用外卖平台看子域划分:

    # 外卖平台的领域(问题空间),按业务能力切成子域
    点餐/下单   → 核心子域  (推荐、拼单、补贴 = 差异化,钱在这)
    配送调度    → 支撑子域  (自家规则,无现成方案,决定口碑)
    支付        → 通用子域  (接微信/支付宝)
    用户认证    → 通用子域  (用现成登录方案)

子域是问题空间里的分析边界,它天然暗示"这块业务该独立进化"——这正是后面微服务拆分的起点。但要把一块业务做成软件,还有个更麻烦的问题,就是下一节要说的。

## 4. 限界上下文:词义的边界

同一个词,在不同业务环节里意思不一样。回看开头的"订单"——交易里它是"金额+优惠券",履约里它是"骑手+地址",财务里它是"流水+账期"。如果全公司强行用一个统一的订单模型,这个模型会同时背上三套含义,谁用都得猜。

**限界上下文**就是来解决这个的:划一个圈子,圈子里每个词的含义明确、一致、不打架;出了圈,含义不作数,要用别的模型。一个上下文一套自己的通用语言(ubiquitous language),一个上下文只有一个领域模型。

"订单"在两个上下文里是两棵不同的模型树:

    # 交易上下文里的 Order                # 履约上下文里的 Order
    orderId                                orderId
    itemIds                                商家地址 / 用户地址
    金额 / 优惠券 / 折扣                    骑手 id / 配送时段
    行为:下单、取消、改价                   行为:派单、接单、签收

所以"订单"在交易和履约里含义不同,**不是设计错误,这正是限界上下文存在的意义**。它划出了词义边界,你在这个上下文里说 Order,别人不会误会你在说骑手的那个 Order。

把子域和限界上下文放一起看:**子域是问题空间(业务现实,"给定的"),限界上下文是解空间(软件模型,"设计的")**。理想情况一一对应,但这是理想映射、不是铁律:一个限界上下文可能横跨多个子域(比如一个 ERP 上下文同时服务库存和采购),一个子域也可能由多个限界上下文实现(比如遗留系统和新系统并存)。

还有一个关键点要记住:**限界上下文是逻辑/语义边界,不绑定部署形态**——一个上下文可以是单体里的一个模块,也可以拆成多个服务。这是后面模块和微服务两节反复用到的点。

边界之间的通信也有讲究。两个上下文要对接,别让一方的模型污染另一方,常用**防腐层(ACL)**在门口翻译——下游把上游的术语翻译成自己的模型;还有共享内核、客户-供应商这些套路,合起来叫**上下文映射(context map)**。一句话:边界划清楚之后,跨边界的每个词都要有明确的翻译处。

## 5. 模块:限界上下文内部的目录

战略层划出限界上下文,战术层要在里面组织代码。**模块**就是战术层的组织工具:让领域模型内部"同类归堆、概念内聚",代码好找好懂。一个限界上下文可以由多个模块组成。

新手最大的坑:**用模块当限界上下文**。只按 order/user/payment 分几个目录,就以为做了战略设计——但模块是战术组织,它回答不了"这些模块之间业务语义冲不冲突、该不该合并"这种战略问题。

    # 战略层:一个限界上下文一个目录
    bounded-contexts/
    ├── trade/                    # 交易上下文
    │   ├── application/
    │   └── domain/
    │       ├── order/            # 模块:订单
    │       └── pricing/          # 模块:计价/优惠
    ├── fulfillment/              # 履约上下文
    │   ├── application/
    │   └── domain/
    │       ├── rider/            # 模块:骑手
    │       └── dispatching/      # 模块:派单
    └── account/                  # 账户上下文

注意 fulfillment 里有 rider、dispatching 两个模块,它们共享履约上下文这套通用语言;而 trade 里的 Order 和 fulfillment 里的 Order 是两回事——这就是模块和限界上下文的分工:模块负责把上下文内部收拾整齐,限界上下文负责定义这个上下文的语义边界。

## 6. 战术:实体、值对象、聚合、仓储

边界定完了,进到限界上下文内部,怎么写代码才算"领域驱动"?最常用的四个战术构件:实体、值对象、聚合、仓储。

**实体**是有身份的对象,靠一个唯一的 ID 认出"是哪一个"。订单从下单到发货状态一直在变,但它还是那个订单,因为订单号不变。**值对象**没有身份,只有"是什么",属性相同就相等、可以互换。判据就一条:把所有属性改成一样之后,它还是不是"同一个东西"?是,值对象;不是(必须靠 ID 区分),实体。判据是"有没有身份",不是"能不能变"。

    // Money 值对象:不可变,相等按属性,加法返回新对象
    public sealed record Money(decimal Amount, string Currency)
    {
        public Money Add(Money other)
        {
            if (other.Currency != Currency) throw new CurrencyMismatch();
            return new Money(Amount + other.Amount, Currency);  // 旧对象没被动过
        }
    }

    // Order 实体:有身份,属性可变,相等比 ID
    public class Order
    {
        public OrderId Id { get; }          // 身份,只读
        public OrderStatus Status { get; private set; }
        public void MarkPaid() => Status = OrderStatus.Paid;
    }

值对象最常见的用处,是把裸的 decimal、字符串电话号码、地址这些包成带行为的类型——精度校验、币种判断、大小比较都长在类型自己身上,而不是散落在调用方。

**聚合**把"同生共死"的一组对象打成包:订单和订单项是一组,订单作废、订单项就没意义,它们必须在一个事务里一起改、一起存。这个"必须一起保持一致"的范围叫**一致性边界**,聚合就是这条边界。**聚合根**是这个包裹的唯一入口,外面要动里面任何一个零件,都得通过聚合根。

    public class Order : AggregateRoot
    {
        private readonly List<OrderLine> _lines = new();
        public Money Total => _lines.Aggregate(Money.Zero, (sum, l) => sum + l.Subtotal);

        public void AddLine(ProductId product, int qty, Money unitPrice)  // 唯一入口
        {
            if (qty <= 0) throw new ArgumentException();
            _lines.Add(new OrderLine(product, qty, unitPrice));
        }
    }
    // 外部只能 order.AddLine(...),拿不到 _lines

聚合根保证不变量永远成立:订单金额 = 各订单项之和。如果有人能绕过聚合根直接改 OrderLine 的价格,这个等式就保不住了。

**仓储**让领域代码觉得聚合根"就在内存里放着",要拿就拿、要存就存,不用管背后是 SQL、NoSQL 还是外部 HTTP。一个聚合根配一个仓储,接口定义在领域层、用业务语言命名,实现放在基础设施层。

    // 领域层:只定义接口,用业务语言
    public interface IOrderRepository
    {
        Order FindByOrderId(OrderId id);
        void Save(Order order);
    }
    // 基础设施层:具体实现(SQL / ORM / Redis)藏在这里

## 7. 服务:领域服务、应用服务、基础设施服务

边界内部搞清楚了,但业务里有些行为不属于任何单个对象——转账牵涉两个账户,塞给任意一边都是让账户"转自己",不对。这种跨聚合的"过程"抽出来,就是**领域服务**。判断顺序:能不能放值对象 → 能不能放实体/聚合根 → 都不行、确实是多个领域对象的协作,才用领域服务。

服务的"三兄弟"经常被搞混,分工其实很清晰:

| 服务 | 在哪层 | 干什么 | 特征 |
|---|---|---|---|
| 应用服务 | 应用层(最外圈) | 接请求、开事务、调领域对象、存数据、组装返回值 | 编排用例,几乎不含业务规则 |
| 领域服务 | 领域层(中间) | 跨聚合的业务"动词",如转账、计价 | 无状态,是例外不是常态 |
| 基础设施服务 | 基础设施层(最底) | 发邮件、发短信、连数据库 | 接口定义在领域,实现接具体技术 |

调用链是单向向下的,基础设施反过来依赖领域接口:

    POST /transfer
      → TransferAppService.Execute(cmd)                # 应用服务:编排用例
          source = accountRepo.FindBy(cmd.FromId)      #   仓储接口 → 基础设施实现(SQL)
          transfer.Transfer(source, target, amount)    #   领域服务:跨聚合业务过程
          accountRepo.Save(source); accountRepo.Save(target)
          eventBus.Publish(new MoneyTransferred(...))  #   领域事件

**领域服务是例外不是常态。** 如果业务逻辑大量堆在服务里,那是贫血模型的信号。

**贫血模型**:实体只剩 getter/setter,是数据袋,业务逻辑全搬进一堆 Service。Fowler 的原话:它乍一看像真的领域模型,但点进去几乎没有任何行为,只是一袋 getter 和 setter。本质是过程式设计——用对象的外壳写面向过程。**充血模型**:数据和行为长在一起,校验、计算、业务规则写在实体自己身上,Service 只做实体放不下的跨聚合协调。

    // 贫血:实体是数据袋,规则在外面
    public class Account
    {
        public decimal Balance { get; set; }     // 谁都能改
    }
    public class AccountService
    {
        public void Withdraw(Account a, decimal amt)
        {
            if (a.Balance < amt) throw new Exception();
            a.Balance -= amt;                     // 规则在 Service 里
        }
    }

    // 充血:规则长在实体里,谁也绕不过
    public class Account
    {
        public decimal Balance { get; private set; }
        public void Withdraw(decimal amt)
        {
            if (amt > Balance) throw new InsufficientFunds();
            Balance -= amt;                       // 透支规则在实体上
        }
    }

对纯 CRUD 后台,Fowler 自己也承认贫血/事务脚本够用且简单。判据是业务复杂度:规则一多,贫血模型的 Service 无限膨胀、逻辑重复、没法复用;更糟的是实体自己没法保证正确——谁都能乱 set Balance。

## 8. 微服务:限界上下文该不该拆

现在往上看一层:这些限界上下文,怎么变成部署出去的微服务?

微服务拆分的本质是**按业务边界拆,不是按技术层拆**。DDD 给了一条很实用的尺度(微软 Azure 架构中心):**一个微服务不小于一个聚合,不大于一个限界上下文**。下限:聚合是事务一致性边界,比它更小就没有独立的意义;上限:限界上下文是词义边界,比它更大就开始模型打架。所以限界上下文是微服务的天然候选。

但注意是"候选"——**一个限界上下文 = 一个微服务,只是默认起点,不是铁律**。拆不拆是组织 + 运维决策,不是建模决策,要看这些:

| 因素 | 拆的理由 | 不拆的理由 |
|---|---|---|
| 团队 | 两个团队要各自独立发版 | 单团队,微服务运维成本远超收益 |
| 变更频率 | 两块功能总不同步变 | 总绑在一起变,拆了反而要联动发版 |
| 一致性 | 能接受最终一致 | 必须强一致跨服务 ACID——说明边界画错了,该合回去 |
| 吞吐 | 一块负载画像差异巨大,需独立扩 | 各块扩容画像相近 |
| 部署 | 有具体的独立部署理由 | 拆了还是共享库/深调用链→分布式单体 |

默认答案往往是"先不拆"。Fowler 的 MonolithFirst 和**模块化单体**:在同一个进程、一次部署里,用强制的模块边界把限界上下文立起来(每个模块有自己的数据表、只暴露公开接口),哪天真要拆,把模块内部接口换成 HTTP/gRPC 就行,模块内部代码几乎不动——代码层面像微服务,部署层面还是单体。什么时候出现拆分信号?某模块部署频率远超他人、某模块负载特征差异巨大、团队大到需要两个团队独立交付。

拆分反模式几乎都和 DDD 背道而驰:按技术层拆(controller/service/dao 各成一个"服务",每次请求变成一串网络跳)、共享数据库(代码分了家、表还共用,改个字段互相弄坏)、分布式事务当银弹(用 Saga + Outbox 接受最终一致,别上两阶段提交)。这些的共性是:**边界形同虚设**。

有一种建模手段值得一提:**事件风暴**。它是 Alberto Brandolini 发明的一种工作坊,不写代码,一群人围着一面墙用便利贴把"业务里发生了哪些事"排出来,从事件反推命令、圈聚合、划限界上下文。它是 DDD 从业务现场长出来的常用入口——下次被拉去梳理一块陌生业务,可以试试这个。

## 9. 3A 架构:整个公司的边界

到这里,我们已经把"单个系统内部"和"几个系统之间"讲完了。再往上一步:整个公司呢?一家公司有几十上百个系统,它们怎么组织、怎么对齐业务战略?这一层叫**企业架构(Enterprise Architecture)**。

公认标准是 TOGAF 提出的四层:**业务架构、数据架构、应用架构、技术架构**(缩写 BDAT)。每层管什么、产出什么,一张表:

| 架构域 | 人话 | 管什么 | 典型产出 |
|---|---|---|---|
| 业务架构 | 公司要做什么、怎么赚钱 | 能力、流程、组织、价值 | 业务流程模型、业务能力地图 |
| 数据架构 | 业务产生的信息怎么理 | 数据定义、关系、归属、一致性 | 数据模型、数据字典、数据所有权 |
| 应用架构 | 用哪些系统承载业务 | 系统清单、功能边界、接口 | 应用系统清单、系统间接口图 |
| 技术架构 | 系统怎么跑起来 | 服务器、网络、云、中间件 | 基础设施图、部署与运维规范 |

现在说 3A。**3A 不是 DDD 的术语,是中文企业架构圈的民间简称**——指业务架构、数据架构、应用架构这三个,把"技术"当成纯底座砍掉。出处是付晓岩(资深企业业务架构师)文章里的"**3A 架构的拉通,也就是业务、数据、应用架构的拉通**"。TOGAF 官方没有"3A"这个词,只有四层 BDAT。

这里要校准一下叫法。国内不少团队把"数据 + 应用"并成一桶,叫**系统架构**——这其实和 TOGAF 官方一致:TOGAF 的阶段 C 就叫"信息系统架构",内容就是数据 + 应用两个子阶段。所以"业务/系统/技术"三分和"业务/数据/应用/技术"四分是同一套东西的两种切法:**系统 = 数据 + 应用**。如果你所在的公司内部讲"业务架构、系统架构、技术架构",那和外界说的 3A 是同一个东西,只是切法不同。

四层怎么拉通?自上而下,一层喂一层:

    业务战略:为什么做
      │ 战略喂进去
      ▼
    业务架构:做什么            ← 能力地图 / 流程 / 业务对象
      │ 业务需求
      ▼
    数据架构 + 应用架构:用什么系统承载    ← 数据模型 / 系统清单 / 服务目录
      │ 需求
      ▼
    技术架构:怎么跑            ← 云 / 网络 / 中间件 / 部署

上层驱动、下层支撑:业务架构是灵魂,定义需求;数据架构是业务到 IT 的翻译层;应用架构是战术,把业务能力落成系统;技术架构是装备,让系统跑起来——技术不直接服务业务,它通过支撑应用间接服务业务。付晓岩一句话:**业务架构设计的最终结果,是一个业务能力的布局**。他举过一个真实的银行案例:建行新一代核心系统,从战略出发梳理 6 条价值链 → 26 个业务方向 → 102 个转型举措 → 2000 余项战略型业务需求 → 114 个企业级公用业务组件,然后才顺着业务架构去做应用架构和技术架构,前后六年半。这就是"3A 拉通"的真实体量。

企业架构和 DDD 不是两套无关的东西,它们接得上:

| 企业架构 | DDD |
|---|---|
| 业务能力(Business Capability) | 子域(Subdomain) |
| 应用系统 | 限界上下文 / 微服务 |
| 数据域 / 数据所有权 | 聚合的单一数据所有权 |

前两行比较好懂:业务能力地图的识别方式和子域一样——业界有个说法,"识别子域的方式跟识别业务能力一样,分析产出的子域定义结果也会跟业务能力非常接近";应用系统是较粗的部署单元,一个应用系统能容纳多个限界上下文,更细的对齐是"一个限界上下文 ≈ 一个微服务"。第三行要小心别混:**企业数据域的所有权是治理层的责任归属(这块数据归谁),DDD 聚合的所有权是运行时的强一致边界(一个事务只改一个聚合)**——同一个"单一所有权"思想,在治理层和代码层的两个投影。

一句话总结这层关系:**DDD 消费业务架构的产物,而不是生产它们**。业务架构告诉你公司有哪些业务能力、边界大致在哪;DDD 在某个具体系统里把边界落成限界上下文和聚合。反过来,事件风暴这种自下而上的手段,又能用 DDD 的产出反哺企业架构的能力地图。两条路都对得上。

中台也在这个话题里,顺带说一句:业内的共识是"所有的中台都是业务中台"——中台能力必须从企业级业务架构的标准化与去重中长出来,没有企业架构撑腰的中台,就是一堆没人维护的共享服务。"中台不是 IT 项目,是治理工程。"

## 10. 结尾

把这条线收起来:从一行代码到一家公司,每一层都在回答同一个问题——**边界在哪**。值对象和聚合划出"什么和什么必须长在一起",限界上下文划出"哪个词义归谁",子域划出"哪块业务值得投",企业架构划出"整个公司怎么对齐战略"。DDD 从中间往上往下都能接:往下是代码里的聚合和服务,往上是微服务和 3A。

最后记住一件事:下次有人跟你说"3A 架构",它来自企业架构,不是 DDD 的术语。如果你所在的公司内部叫它"业务/系统/技术",那只是把数据 + 应用并成了"系统"——同一个东西,另一种切法。`,
}

export default project
