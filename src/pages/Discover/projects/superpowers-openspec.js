const project = {
  slug: 'superpowers-openspec',
  date: '2026-08-14 16:30',
  name: 'Superpowers + OpenSpec',
  url: 'https://github.com/obra/superpowers',
  url2: 'https://github.com/Fission-AI/OpenSpec',
  description: 'Superpowers 是一套给 AI 编程代理用的结构化方法论，OpenSpec 是一个 AI 原生的 spec 驱动开发框架。一个管过程一个管契约，放在一起看才完整；在存量系统上，它们补的是坐标到手之后到上线的全过程。',
  tags: ['agent'],
  stars: '237k+ / 56k+',
  author: 'Obra (Prime Radiant) / Fission AI',
  detail:
`Agent 写代码的能力在快速提升，但一个根本问题始终没有解决：**你没法相信它输出的质量是稳定的**。同一个模型这次写得好，下次可能就跑偏了；在 Claude Code 上表现好的流程，换到 Codex 上又不一样。这个问题的杀伤力，在**改一个大型存量系统**的时候最大——改动横跨很多文件、很多人在同时改、改坏了的代价很高。

两个项目从不同方向在回应这个问题。一个是 **OpenSpec**，一个是 **Superpowers**。它们属于不同作者、不同社区，但解决的问题恰好互补。

这篇文章不按"先介绍工具、再讲用法"来组织，而是按**改一次大型存量系统的顺序**走一遍。场景是：一个 RR（需求单）进来，先定位到要改哪里——找到一个"坐标"（哪段源码、哪张表、跑在哪台机器），那是这个系列前一篇讲的事。坐标到手之后，还剩五步：**契约、隔离、拆解、验证、上线**。每一步该用什么，就介绍什么。

## 第一步，契约先行：OpenSpec 管"要改什么"

定位到要改哪几行之后，第一件事不是动手，是**把"这次改什么、改到哪为止"写成白纸黑字**。这一步是 **OpenSpec** 的主场。

OpenSpec 来自 Fission AI，是一家 AI 原生的 spec 驱动开发框架。它的核心概念是 **spec（规范）**：代码告诉你系统**现在**是怎么跑的，spec 告诉你系统**应该**怎么跑。两者的区别在于意图——读代码你只能推导出作者实际做了什么，读 spec 你知道作者**想**做什么。

为什么要为 Agent（和人）写 spec？因为没有 spec 的时候，每件事都要靠猜。AI 拿到一个 Issue，只能从对话历史里拼凑上下文，同一个需求不同会话可能做出完全不同的实现。Spec 把"预期行为"变成白纸黑字——所有人都看同一份文件，不用猜。

但传统 spec 的问题是太重了。写一份完整的软件需求规格说明可能要几周，写完已经过时了。OpenSpec 的解法是**增量**：不为整个项目写 spec，只为正在改的东西写。对大型存量系统，这一点尤其值钱——**五年前的老代码没 spec 没关系，你只为你这次碰的这一块写 spec**，不用给五年历史补课。

一次改动的契约，落在 OpenSpec 的 change 里。四个文件各管各的事：

\`\`\`
openspec/
├── specs/               # 当前系统的事实——现在长什么样
│   └── cli-init/spec.md
├── changes/             # 正在做的事——要改什么
│   └── add-dark-mode/
│       ├── proposal.md  # 为什么和要什么
│       ├── specs/       # 增量差异（新增了啥、改了啥、删了啥）
│       ├── design.md    # 技术方案
│       └── tasks.md     # 实施清单
└── config.yaml          # 配置 + 项目规则
\`\`\`

- **proposal.md——为什么干和干什么**：锁定方向和范围，只回答"当前有什么问题、改的范围多大、不改什么"。目的是让一个人不读代码就能批准或否掉整个 change。
- **specs/——具体改了什么行为**：change 的核心，每条 requirement 用 SHALL 句式定义行为。它写的是 delta——新增、修改、删除了哪些行为。
- **design.md——怎么实现**：架构怎么调整、依赖怎么加、有没有兼容性风险。可选的——只改一个字符串常量就不用写。
- **tasks.md——实施清单**：给 Agent 执行的 checklist，按依赖顺序列出这次改动的每一块。

一篇 spec 文件长这样——每条 requirement 用 SHALL 句式定义行为，后面跟 scenario（Given/When/Then）：

\`\`\`markdown
### Requirement: 目录已存在时跳过
The system SHALL skip directory creation when openspec/ already exists.
#### Scenario: 已初始化过
- **WHEN** user runs \`openspec init\` in a project with openspec/
- **THEN** the system prints a message and exits
\`\`\`

这是 OpenSpec 的"真理来源"（source of truth）。当 AI 准备改代码时，读 spec 知道当前应该有什么行为，然后决定改哪条。

为什么大型存量系统特别需要这份合同？

- **防 scope creep（范围蔓延）**。存量系统处处是"顺手就能改"的诱惑——旁边那行也乱，一起改了呗。合同把范围写成白纸黑字，改着改着越界，一眼看得见。
- **审计与回溯**。大系统是很多人、很多 agent 在改，每笔改动都要有据可查。哪天线上出了问题，要能倒查到"这笔改动当时批准的范围是什么、谁批的"。
- **验证有对账单**。后面所有测试、门禁、verify，都是拿代码对着这份合同比。

实际用起来，OpenSpec 在你的项目里加一个 \`openspec/\` 目录，同时注册一组命令：\`/opsx:propose\`（生成 change 的四个文件，你逐份审查，不对就跟 AI 来回改）、\`/opsx:apply\`（按 tasks.md 逐项实施）、\`/opsx:archive\`（delta specs 并回主 specs）、\`/opsx:verify\`（核对代码和合同）。安装就一条命令：\`npm install -g @fission-ai/openspec\`，然后 \`openspec init\`。

这一步上，OpenSpec 的契约和 Superpowers 的纪律是同一个方向——Superpowers 里也有一道同样的规矩（brainstorming）：Agent 不能直接写代码，先探索项目、读文档、把方案写成设计文档让你确认。两个项目在"先想清楚再动手"上是一致的。

一个设计取舍要记住：OpenSpec 的 artifact 依赖是"使能"关系，不是"闸门"——proposal 没写完也能开始写 design。好处是灵活，代价是没人拦着你跳过 propose 直接改代码。跳过之后 spec 就跟代码脱节了，系统退化成"一堆没人看的 Markdown"。**这份合同要生效，靠的是纪律**——而纪律，正好是下一步的主角。

## 第二步，隔离与基线：Superpowers 管"别动到别人的"

合同签完，动手之前还有一件事：**别在主分支上直接改。** 大型存量系统同时有很多人、很多 agent 在改；在主分支上直接改，改到一半别人的提交进来，不是冲突就是覆盖，你的半成品还可能被当成新代码误合并上线。

这一步是 **Superpowers** 的主场。Superpowers 不是一个新的 Agent 框架或模型，而是一组 **SKILL.md 文件** 加一个启动注入机制，让 Agent 启动后走一套强制工程流程。作者是 Jesse Vincent（Obra）。它的核心判断是：Agent 的能力已经够用了，问题出在**工程纪律**上——默认情况下，Agent 拿到任务就写代码，没有 spec、没有计划、没有测试、没有 review。Superpowers 把这些步骤全部变成强制性的，而且让 Agent 无法绕过去。

它的答案是 worktree 隔离：从主代码仓另开一个独立的副本，在里面改，主分支不受影响，改完再合并回去。但这一步对存量系统最重要的，不是隔离本身，是进隔离区之前那道手续：**基线测试**。

**基线测试** = 改动开始前，先把系统跑一遍测试，确认"现在就是绿的"。为什么在存量系统里特别值钱？因为它是后面所有失败唯一的归因锚点：**有了基线，之后全量测试哪一项红了，你都能回答"是我这次改出来的，还是本来就烂的"；没有基线，每次失败都说不清是谁引入的。** Superpowers 的规则是这步不能跳过，而且基线测不过时，继续还是先查，决定权在人，Agent 不能自己拍板。

## 第三步，拆解实施：一次一口，每口都可验证

契约有了，隔离区有了，开始改。但一次改动横跨多个文件、依赖很深，"改哪一行"不能一口吞。OpenSpec 的 change 里有一份 tasks.md——合同级的实施清单，按依赖顺序列出这次改动的每一块，但每项是一块改动，不是能直接动手的小步。**Superpowers 不直接执行这份清单**：它有"写计划"一步，从契约（proposal 和 specs）出发，把每一块再细化成**原子任务**——一个 2-5 分钟、精确到文件、带验证标准，然后**子 Agent 驱动开发**逐条执行、TDD 先红再绿。所以这里是**两层计划**，不是一套：tasks.md 管合同里"有哪些事"，Superpowers 的计划管"每件事怎么拆开做"。两层之间不原生打通——靠社区桥（后面讲到的 SpecPilot 那类）翻译衔接；做完一块在 tasks.md 打一个勾，会话中断了重开还能断点续跑。

每一口怎么改？两道纪律叠着用：

**SDD（Subagent-Driven Development，子 Agent 驱动开发）**——注意别和本文另一个主角 OpenSpec 的 spec-driven（spec 驱动）混了：SDD 是派子 Agent 去干活，spec-driven 是写行为契约。每项任务派一个全新的子 Agent，不继承主会话的记忆，只对着任务描述干活。对存量系统的意义是防"凭记忆改"——主会话记得的东西可能已经过时，全新子 Agent 只信文档。每个任务过两道审查：先查有没有满足 spec（要精确到文件名和行号），再查代码质量。**对到行号这一条，意味着超范围改动在任务级就被拦下**，而不是攒到最后一次大 review 才爆——这正是"坐标"思想在"改"阶段的延伸。审查不过就重来，最多 5 轮。

**TDD（测试驱动开发）**：先写一个会失败的测试，跑一遍确认它真的失败（RED），再写最少代码让它通过（GREEN），然后重构。铁律只有一条：没有失败的测试，不许写生产代码；发现先写了代码，删掉重来。为什么？**没亲眼看着测试失败，你就不知道它测的是不是对的东西。** 先写代码再补测试，只是在确认代码已经在做的事，不是验证需求。

"先写测试"不是测试，是**设计**——你在定义接口和行为契约，确定函数接受什么、返回什么、边界在哪。Agent 特别擅长这个把戏：先写代码，再写一个只覆盖 happy path 的测试，然后说"全绿了"。那不是测试，是走形式。所以规矩是：先红再绿，顺序不能倒。

对存量系统，TDD 还有一条专属规则：**存量代码没有测试？那就边改边补。** 改一个十年没测过的老函数，第一件事是给它的现有行为写个测试钉住，再动手——不然你根本不知道改坏了没有。

为什么 Superpowers 敢把纪律定这么死？因为 Agent 有一整套"跳过流程"的借口，而它的 bootstrap 里有一张对照表，把每个借口都打脸：

| 想法 | 现实 |
|------|------|
| "这就是个简单问题" | 问题就是任务，检查有没有 skill 可用 |
| "我先看看代码" | Skill 会告诉你**怎么**看，先检查 skill |
| "我知道那个意思" | 知道概念不等于用了 skill，调它 |
| "这 skill 杀鸡用牛刀" | 简单的事情也会变复杂，用 |
| "我先做这一件小事" | 做什么之前都先检查 |

核心规则叫 **1% Rule**：只要你觉得有 1% 的可能某个 skill 能用，就必须调它。Agent 没有选择权。规则前面用 \`<EXTREMELY-IMPORTANT>\` 标签括起来，语气是"YOU DO NOT HAVE A CHOICE"——这不是建议，是声明。

## 第四步，验证：改对了，和没改坏，是两件事

改完，怎么证明"这次改对了，而且没改坏别的"？三样叠着用：

1. **TDD 的绿色测试**——每个原子任务都验证过，证明"改对了"。
2. **OpenSpec 的 \`/opsx:verify\`**——机器搜代码，核对合同和代码是否一致：合同里承诺的每条 requirement 有没有实现、有没有被破坏。**注意，verify 检查的是"文档有没有跟上代码"，不是"代码有没有跑对"**——它搜代码找证据，发现问题按 CRITICAL / WARNING / SUGGESTION 三级摆出来，但不阻塞归档，只把问题亮出来。OpenSpec 还有一个全自动的 \`openspec validate\` 命令做结构性校验：检查合同本身有没有写坏（格式对不对、被替换的旧条款在不在、归档的任务有没有打勾）——它比的是 spec 和 spec，不是 spec 和代码；和代码对账，是 verify 的活。
3. **五道门禁**——Lint（代码风格和明显错误的自动检查）、编译（能不能编成可运行的程序）、测试、覆盖率（测试跑到了多少比例的代码）、安全。**门禁（gate）** 就是一道机器判红绿的闸，不过就不能往下走。要说清楚：OpenSpec 和 Superpowers 自己都不发这道闸——官方承认"只检查产物在不在，闸门要你自己用 CI 或 hook（一段自动触发的脚本）去守"，门禁的裁判是你的 CI，不是工具。

测试本身还要分两层看。**单元测试**测一个函数、一个类——数据库、网络、别的模块全都 mock 掉，快、稳、每次改动都跑，答的是"这段代码写对没有"；**集成测试**测多个单元真的接起来——用真的数据库、真的网络，慢、要搭环境，答的是"整个系统合起来对不对"。单元测试把边界 mock 掉了，边界本身的错它验不到——那是集成测试的活。谁写？单元测试 agent 基本全包（Superpowers 的 TDD 就是单元层的，先红再绿、自己跑自己验）；集成测试 agent 也能写，但"能跑"的前提是环境在——测试库、种子数据、服务怎么起，这是基建，不是写测试。所以答案还是那句：**写测试是 agent 的活，"有地方跑测试"是你的。** 人呢？单元测试 agent 全绿基本可信，人抽查有没有走形式（只测 happy path）；集成测试更需要人——环境要人搭，存量系统的真实行为要人讲清楚 agent 才写得对，测试 flaky 了要人判断是测试写坏了还是真有 bug。

为什么存量系统特别需要这一步？"改对"和"没改坏"是两件事：**TDD 证明前者，门禁证明后者。** 存量系统几百个既有场景，不可能人工逐一核，门禁把"红没红"变成机器判的布尔。而 verify 防的是另一件存量系统的老大难：**文档和代码脱节**——没人更新，spec 退化成"一堆没人看的 Markdown"。verify 用机器逼 spec 和代码对账，漂移才会被看见。

## 第五步，上线：交给人审，给历史留痕

验证都过了，最后一步是把改动安全地并回主干、并且让这次改动以后查得到。这一步三样东西：

- **评估报告**——这次改动的验证总结：改了哪些文件、测试结果、门禁通过情况。
- **PR Gate（合并门禁）**——把评估报告和 diff（改动前后的对比）一起交给人，人批准才合并。**PR（Pull Request，合并请求）** 就是"把隔离区里的改动申请并回主代码仓"的那一步。
- **archive（归档）**——把 delta specs 并回主 \`openspec/specs/\`，change 移进 \`openspec/changes/archive/\`，成为精确的变更历史：哪天改了哪条 requirement、为什么改。

Superpowers 的**收尾**规矩也一样：rebase、跑全量测试，然后给你四个选项——merge、开 PR、保留分支、删 worktree。改完不是直接并回去，是交给你决定怎么收。

为什么存量系统特别需要？**大系统上线影响面大，不能只靠 agent 自证**——agent 的职责是给出证明，人的职责是审查这个证明可不可信，PR Gate 就是这最后一关。而 archive 是给历史留痕：出问题能倒查，也让"当前系统长什么样"永远有一份最新的、机器可读的说明。

## 边界：开发、测试、部署，到底谁来管

五步走完，回到开头那个问题：开发、测试、部署，规范到哪一步了？把话说明白——**开发，两个工具一起管；测试，管了一半；部署，一个都没管。**

- **开发**：契约（OpenSpec）+ 纪律（Superpowers），上面讲完了。
- **测试**：OpenSpec 把"该测什么"写成 scenario——一条条 GIVEN / WHEN / THEN，官方原话是"**能变成一条自动化测试**"的原料；Superpowers 逼你把 scenario 写成真测试、先红再绿。**但"有地方跑这些测试"不是它们的事。** TDD 的 RED 测试在存量代码上第一次挂，往往不是因为实现没写，而是**周边代码根本编译/运行不过**——让一段五年没人动过的代码可测，得先凿一个**接缝**（隔离出被改的那一块，切断对全局状态、时间、数据库、网络的依赖）。这个逆向工程和凿接缝的活，两个工具都不替你干。
- **部署**：Superpowers 的收尾停在 merge / 开 PR 的门口，门口之后（构建打包、发布、灰度（先给一小部分用户用新版，没问题再放开）、回滚）它明确不碰；OpenSpec 的 CLI 里没有任何部署命令。**所以部署的闸门（测试过了才能发、灰度、回滚）全在你自己的 CI/CD 里。** 工具能做的，只是把你写的测试挂到 CI 上，让闸门有东西可跑。

还有一个诚实的账要算清：**spec 漂移（文档和代码慢慢脱节）没有完全解决。** OpenSpec 自己承认——没有持续扫描，没人拦着你跳过 propose 直接改代码。增量 delta 只保证"你这次改的这块"有 spec，不保证整块区域五年前的历史旧账对得上；漂移的兜底，最后靠的是代码审查关和归档时的人工确认，不是工具。

把谁管什么列成一张表，边界就清楚了：

| 环节 | OpenSpec 干 | Superpowers 干 | 还得自己搭 |
|---|---|---|---|
| 定目标 | proposal + 增量 spec（SHALL 定义行为） | brainstorming 的产出 | — |
| 定做法 | design.md | 计划（原子任务 + 怎么验证） | — |
| 定位 | tasks.md 列出改动块 | 计划细化成原子任务（精确到文件） | 前一篇 OKF 的坐标 |
| 改代码 | — | SDD 子 Agent + 两道审查 | — |
| 测试 | scenario 提供 GIVEN/WHEN/THEN 骨架 | TDD 强制先红再绿 | 测试框架、测试基线、可测的接缝 |
| 部署 | — | 收尾只到 merge / PR | CI/CD、灰度、回滚闸门 |
| 防漂移 | verify 只查 spec 和代码一致 | 审查关卡 | 归档时人工确认（没人强制） |

## 放在一起看

Superpowers 管"怎么做"，OpenSpec 管"做什么"。没有 OpenSpec，Agent 按 Superpowers 的流程跑完 brainstorming 和 planning，它的"计划"还是基于对话历史里那些零散的描述——不是基于一份结构化的、可审计的 spec。反过来也一样：OpenSpec 给了一份漂亮的 spec 文档，但如果没有 Superpowers 那样的流程约束，Agent 可能在写了三行代码之后就跑偏了，或者跳过了测试，或者做了 scope creep——spec 还在那里，但代码已经不是 spec 的样子了。

上面那五步流水线，每一步都是这两件事在配合：契约先钉"写的是什么"，SDD 和 TDD 守"怎么写"，verify 和门禁做验证。

回头看 Superpowers 的完整流程——文章里它被拆进各个步骤，这里把一整条补上。它的七步是：**brainstorming**（先探索项目、写设计文档，不许直接写代码）→ **worktree 隔离**（另开副本 + 跑基线测试）→ **写计划**（拆成原子任务，每个精确到文件、带验证标准）→ **子 Agent 驱动开发（SDD）**（每条任务派一个全新子 Agent 执行，过两轮审查）→ **TDD**（先红再绿）→ **代码审查**（Critical / Important 阻塞进度）→ **收尾**（rebase、全量测试、merge / 开 PR / 保留 / 删 worktree）。对应到本文：第一步点过它的 brainstorming，第二到第五步就是它的 worktree、写计划、SDD/TDD、收尾——它是一条完整的流水线，不是零散规矩。

两个项目虽然独立发展，但各自的生态也在印证它们的角色：Superpowers 目前支持 11 个平台（Claude Code、Cursor、Codex、Gemini CLI 等），跨平台靠三层架构——skills/（纯 Markdown 行为描述，不提具体工具 API）→ tool mapping（每个平台一个映射文件）→ bootstrap（启动时注入）。OpenSpec 支持 32 个 AI 编码工具的 slash commands。社区里也已经有人在串这两个项目——有个叫 **SpecPilot** 的扩展把 OpenSpec 和 Superpowers 接成了一条五阶段流水线，还有个叫 **spec-superflow** 的项目更进一步，在源层面融合成了一套 9-skill、8-state 引擎，加上了 intent-lock 和 SHA256 hash 校验，保证 spec 不会被改掉而不被发现。

Superpowers 最知名的产出 **chardet 7.0.0** 的完全重写也值得记一笔——性能提升 41 倍、准确率 96.8%，94% 的 PR 被拒率反向验证了它对纪律的坚持不是口头上的。

没有流程的 spec 变成没人看的 Markdown，没有 spec 的流程基于猜测写代码。一个成熟的项目两个都需要。

简言之：**Superpowers 保证"改的过程不失控"，OpenSpec 保证"改的对象是对的"；但测试要跑得起来、代码要发得上去，靠的是你自己的基建。工具给的是中间一截，头和尾是你的。**`,
  takeaway: 'Superpowers 从外部约束 Agent 的行为，OpenSpec 从内部约束 Agent 的目标。一个管流程一套管契约。在存量系统上，坐标到手之后，它们补的是"改这一次"的全过程：契约先行定范围，worktree 加基线防回归，SDD 加 TDD 拆着改，verify 加门禁做验证，最后交给人审、archive 留痕。但它们管的是过程，不是基建——测试要跑得起来、代码要发得上去，靠你自己的测试基线和 CI/CD。没有流程的 spec 变成没人看的 Markdown，没有 spec 的流程基于猜测写代码。',
}

export default project
